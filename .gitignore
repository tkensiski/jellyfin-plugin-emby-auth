bin/
obj/
artifacts/
TestResults/
node_modules/

# GSD runtime mirror, synced from the installed package on each run.
.gsd/

# Planning artifacts. They drove development and stay on the maintainer's disk;
# the repository does not carry them. Earlier commits still contain them, which
# is why .gitleaks.toml keeps its ^\.planning/ allowlist for history scans.
.planning/
