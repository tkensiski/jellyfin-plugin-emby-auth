---
phase: 03-migration-status-and-target
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoublesTests.cs
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/DefaultLoginMethodTests.cs
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyLoginMethodUsersTests.cs
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthControllerTests.cs
  - src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs
  - src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs
  - src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html
  - tests/js/testHelpers.js
  - tests/js/configPage.test.js
autonomous: true
requirements: [TEST-02, TEST-03, FPRT-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A unit test builds a real JellyfinDbContext on SQLite in-memory, writes a User row through one context, and reads that row back through a second context from the same factory."
    - "A unit test runs the single-column ExecuteUpdateAsync move against that context and observes the changed AuthenticationProviderId."
    - "GET /EmbyAuth/Migration reports RecordsUnavailable true while Jellyfin cannot read the fingerprint file, and false once a read succeeds."
    - "The Migration section of the settings page shows a message naming the Jellyfin log while RecordsUnavailable is true, and shows no such message when it is false."
    - "A read failure is not sticky: EmbyVerifiedPasswords reads the file again on the next call, so RecordsUnavailable returns to false with no Jellyfin restart."
    - "Each test class that uses the SQLite seam constructs its own factory instance; no factory instance is shared between test classes."
    - "The fingerprint read-failure message shown on the settings page names no file system path."
    - statement: "The migration list is ordered by user name; the relative order of two accounts whose names compare equal is not specified."
      verification: backstop
  artifacts:
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoublesTests.cs
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/DefaultLoginMethodTests.cs
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyLoginMethodUsersTests.cs
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthControllerTests.cs
    - src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs
  key_links:
    - "SqliteJellyfinDbContextFactory's single held-open SqliteConnection -> every JellyfinDbContext it hands out"
    - "EmbyVerifiedPasswords.RecordsAvailable -> EmbyAuthController.GetMigrationStatus -> MigrationStatus.RecordsUnavailable -> the Migration summary element on configPage.html"
  prohibitions:
    - "No log message, exception message, or API response added by this plan contains a password, a password hash, or the Emby API key."
    - "No page content built from a user-derived string uses innerHTML; textContent only."
    - "No analyzer or compiler warning is suppressed without a Justification."
    - "No message shown on the settings page repeats a configured setting value."
  flagged_assumptions:
    - "TEST-02 edge probe returned category `unclassified` with no usable probe question. Recorded as a flagged assumption rather than resolved with a backstop: the edge-coverage report gives no edge to author a predicate for, and inventing one would assert a check the source never asked for."
---

<objective>
Build the two unit-test seams this phase's remaining work depends on — a real `JellyfinDbContext` on SQLite in-memory, and a hand-written `ITaskManager` / `IScheduledTaskWorker` double — and then drive one thin path end to end through every layer the phase touches: an unreadable fingerprint file reaching a message in the Migration section of the settings page.

The test seam is Task 1 rather than the tracer because the ROADMAP goal for this phase orders it first ("The database test seam and the `ITaskManager` fake come before the fixes"), and because the tracer's `<automated>` verify is a `dotnet test` run that cannot execute until the seam exists. Task 2 is the tracer.

Purpose: prove the whole stack — file I/O, the query helper, the controller, the response record, and the page — before any of the four expansion plans build on it. If the SQLite seam or the response reshape is wrong, this plan finds it after one commit instead of after six.

Output: a test project that can stand up `JellyfinDbContext` and Jellyfin's task manager; `EmbyVerifiedPasswords.RecordsAvailable()`; `MigrationStatus.RecordsUnavailable`; the page message; and TEST-02 baseline coverage of the two move-query classes as they stand today, so the renames in plan 04 are protected by tests that already pass.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-migration-status-and-target/03-CONTEXT.md
@.planning/phases/03-migration-status-and-target/03-RESEARCH.md
@.planning/phases/03-migration-status-and-target/03-PATTERNS.md
@.claude/rules/plugin.md
@.planning/codebase/TESTING.md
</context>

## Artifacts this phase produces

Symbols and files this plan creates. The plan-review source-grounding pass must treat these as new, not as drift.

| Artifact | Kind | Replaces |
|---|---|---|
| `SqliteJellyfinDbContextFactory` (in `TestDoubles.cs`) | new test class | — |
| `FakeJellyfinDatabaseProvider` (in `TestDoubles.cs`) | new test class | — |
| `FakeTaskManager` (in `TestDoubles.cs`) | new test class | — |
| `FakeScheduledTaskWorker` (in `TestDoubles.cs`) | new test class | — |
| `FakeUserManager.AuthenticationProviders` | new settable property | the `NotImplementedException` body of `FakeUserManager.GetAuthenticationProviders()` |
| `EmbyVerifiedPasswords.RecordsAvailable()` | new public method | — |
| `MigrationStatus.RecordsUnavailable` | new record property | — |
| `tests/.../TestDoublesTests.cs` | new test file | — |
| `tests/.../DefaultLoginMethodTests.cs` | new test file | — |
| `tests/.../EmbyLoginMethodUsersTests.cs` | new test file | — |
| `tests/.../EmbyAuthControllerTests.cs` | new test file | — |

Later plans in this phase rename `DefaultLoginMethod` to `LoginMethodMove`, `MoveToDefaultLoginMethod` to `MoveAfterLogin`, and `MoveEmbyUsersToDefaultTask` to `EmbyMigrationTask`; `DefaultLoginMethodTests.cs` is renamed with its subject in plan 04.

<!-- planner-discipline-allow: ReadyToMove, EntityFrameworkCore.Sqlite, UseInMemoryDatabase -->

<tasks>

<task type="auto">
  <name>Task 1: The database and task-manager test seams</name>
  <files>tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj, tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs, tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoublesTests.cs</files>
  <read_first>
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs` — the whole file. `FakeUserManager` (from line 94) is the shape every new double copies: fill in only the members the plugin calls, every other member throws `NotImplementedException`.
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj` — two `PackageReference` entries today, both exact-pinned.
    - `.planning/phases/03-migration-status-and-target/03-RESEARCH.md` §Architecture Patterns Pattern 1 and Pattern 2 — the drafted `SqliteJellyfinDbContextFactory`, `FakeJellyfinDatabaseProvider`, `FakeTaskManager`, and `FakeScheduledTaskWorker` code, and §Common Pitfalls 2, 3, 4 (why InMemory cannot be used, why the connection must stay open, why `JellyfinDbContext` needs four constructor arguments).
    - `.planning/phases/03-migration-status-and-target/03-RESEARCH.md` §Package Legitimacy Audit — the `Microsoft.EntityFrameworkCore.Sqlite` verdict.
    - `src/Jellyfin.Plugin.EmbyAuth/DefaultLoginMethod.cs` — the `ExecuteUpdateAsync` call the seam must be able to execute.
    - `.planning/codebase/TESTING.md` — unit test conventions and file naming.
  </read_first>
  <action>
Add `Microsoft.EntityFrameworkCore.Sqlite` version `10.0.11` as a `PackageReference` to the test project only. Pin the exact version: `Jellyfin.Database.Implementations 12.1.0` already resolves `Microsoft.EntityFrameworkCore` and `.Relational` to `10.0.11`, and a floating reference would drag the whole EF Core set past the version that ships inside the Jellyfin packages this plugin targets. Do not add it to `src/Jellyfin.Plugin.EmbyAuth.csproj`.

Add four doubles to `TestDoubles.cs`, beside `FakeUserManager`, following the one-shared-doubles-file convention. The four constructor arguments and every interface member below were reflected against the installed `Jellyfin.Database.Implementations 12.1.0` and `MediaBrowser.Model 12.1.0` assemblies during planning; treat them as the contract to satisfy.

`FakeJellyfinDatabaseProvider` implements `Jellyfin.Database.Implementations.IJellyfinDatabaseProvider`: property `IDbContextFactory<JellyfinDbContext>? DbContextFactory { get; set; }`; no-op `void Initialise(DbContextOptionsBuilder, DatabaseConfigurationOptions)`, `void OnModelCreating(ModelBuilder)`, `void ConfigureConventions(ModelConfigurationBuilder)`; completed-task `RunScheduledOptimisation(CancellationToken)`, `RunShutdownTask(CancellationToken)`, `RestoreBackupFast(string, CancellationToken)`, `DeleteBackup(string)`, `PurgeDatabase(JellyfinDbContext, IEnumerable<string>)`; and `Task<string> MigrationBackupFast(CancellationToken)` returning an empty string. Document on the class that `OnModelCreating` and `ConfigureConventions` are deliberately no-ops: Jellyfin's real SQLite provider sets a default `DateTimeKind` and disables the `RETURNING` clause, and no test in this phase reads or writes a `DateTime` column on `User` or asserts on a value returned from `ExecuteUpdateAsync`.

`SqliteJellyfinDbContextFactory` implements `IDbContextFactory<JellyfinDbContext>` and `IDisposable`. Its constructor opens one `Microsoft.Data.Sqlite.SqliteConnection` with connection string `DataSource=:memory:`, calls `Open()`, builds `DbContextOptions<JellyfinDbContext>` with `UseSqlite(connection)` — the connection object, never the string — and calls `Database.EnsureCreated()` once through a context it then disposes. `CreateDbContext()` constructs `JellyfinDbContext(options, NullLogger<JellyfinDbContext>.Instance, new FakeJellyfinDatabaseProvider(), new NoLockBehavior(NullLogger<NoLockBehavior>.Instance))`. `NoLockBehavior` is `Jellyfin.Database.Implementations.Locking.NoLockBehavior`, is public, and takes only `ILogger<NoLockBehavior>` — reuse it rather than writing an `IEntityFrameworkCoreLockingBehavior` fake. `Dispose()` disposes the connection. The single held-open connection is load-bearing: a SQLite in-memory database dies with its last connection, and production creates a fresh context per unit of work through the factory.

`FakeTaskManager` implements `MediaBrowser.Model.Tasks.ITaskManager`. Fill in only `IReadOnlyList<IScheduledTaskWorker> ScheduledTasks` (backed by a settable `List<IScheduledTaskWorker> Tasks`) and `void QueueIfNotRunning<T>()` (appending `typeof(T)` to a public `List<Type> QueuedTypes`). Every other member throws `NotImplementedException`: `QueueScheduledTask<T>()`, `QueueScheduledTask<T>(TaskOptions)`, `QueueScheduledTask(IScheduledTask, TaskOptions)`, `CancelIfRunning<T>()`, `CancelIfRunningAndQueue<T>()`, `CancelIfRunningAndQueue<T>(TaskOptions)`, `AddTasks(IEnumerable<IScheduledTask>)`, `Cancel(IScheduledTaskWorker)`, `Execute(IScheduledTaskWorker, TaskOptions)`, `Execute<T>()`, and the `TaskExecuting` and `TaskCompleted` events.

`FakeScheduledTaskWorker` implements `MediaBrowser.Model.Tasks.IScheduledTaskWorker` over a constructor-supplied `IScheduledTask`: `ScheduledTask` returns it; settable `TaskState State` defaulting to `TaskState.Idle`; settable `double? CurrentProgress`; settable `TaskResult LastExecutionResult`; `Name`, `Description`, `Category` delegating to the wrapped task; a constant `Id`; settable `IReadOnlyList<TaskTriggerInfo> Triggers`; a no-op `ReloadTriggerEvents()`; and an empty-accessor `TaskProgress` event. `TaskResult` carries `StartTimeUtc`, `EndTimeUtc`, `Status`, `Name`, `Key`, `Id`, `ErrorMessage`, `LongErrorMessage`; `TaskState` has `Idle`, `Cancelling`, `Running`; `TaskCompletionStatus` has `Completed`, `Failed`, `Cancelled`, `Aborted`.

Replace `FakeUserManager.GetAuthenticationProviders()`'s `NotImplementedException` body with a return of a new settable `NameIdPair[] AuthenticationProviders { get; set; }` property defaulting to an empty array. Leave `GetPasswordResetProviders()` throwing.

Write `TestDoublesTests.cs` proving each seam works, so a broken seam fails here rather than inside a feature test: one test writes a `User` row through one context from the factory and reads it back through a second context from the same factory; one test calls `DefaultLoginMethod.MoveAsync` against a seeded row and asserts the row's `AuthenticationProviderId` changed, which is what proves `ExecuteUpdateAsync` is translatable on this provider; one test asserts `FakeTaskManager.QueueIfNotRunning` records the type argument and `ScheduledTasks` returns the workers it was given; one test asserts `FakeUserManager.GetAuthenticationProviders()` returns what `AuthenticationProviders` was set to.

Each test class that needs the database constructs its own `SqliteJellyfinDbContextFactory` and disposes it — never a static or shared instance — so two test classes running in parallel cannot see each other's rows.
  </action>
  <acceptance_criteria>
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj` contains `Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.11"`.
    - `src/Jellyfin.Plugin.EmbyAuth/Jellyfin.Plugin.EmbyAuth.csproj` contains no reference to `Microsoft.EntityFrameworkCore.Sqlite`.
    - `TestDoubles.cs` declares types named `SqliteJellyfinDbContextFactory`, `FakeJellyfinDatabaseProvider`, `FakeTaskManager`, and `FakeScheduledTaskWorker`.
    - `TestDoubles.cs` contains the literal `DataSource=:memory:` exactly once.
    - `TestDoubles.cs` contains no occurrence of `UseInMemoryDatabase`.
    - `FakeUserManager.GetAuthenticationProviders()` no longer throws: a test sets `AuthenticationProviders` to a two-element array and reads the same two elements back.
    - A test writes a `User` row through one context from a factory instance and reads it back through a second context from that same instance.
    - A test calls `DefaultLoginMethod.MoveAsync` against the SQLite context and observes `AuthenticationProviderId` changed on the row — proving `ExecuteUpdateAsync` executes, which is exactly what the EF Core InMemory provider cannot do.
    - `dotnet test` reports a non-zero number of tests in `TestDoublesTests`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx --filter "FullyQualifiedName~TestDoublesTests"</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests run / "no test matches"</fails_when>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx</automated>
    <fails_when>non-zero exit, or any line containing "error CS" or "error CA" in the build output</fails_when>
    <automated>rg -q 'Microsoft\.EntityFrameworkCore\.Sqlite" Version="10\.0\.11"' tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj</automated>
    <fails_when>non-zero exit — the pinned version string is absent or the version drifted</fails_when>
    <automated>! rg -q 'EntityFrameworkCore\.Sqlite' src/Jellyfin.Plugin.EmbyAuth/Jellyfin.Plugin.EmbyAuth.csproj</automated>
    <fails_when>non-zero exit — the test-only package leaked into the shipping project file</fails_when>
  </verify>
  <done>`dotnet test` passes, `TestDoublesTests` runs a non-zero number of tests, and a test has observed a row change made by `ExecuteUpdateAsync` against the SQLite seam.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: One end-to-end path — an unreadable fingerprint file reaches the Migration section</name>
  <files>src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs, src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs, src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html, tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthControllerTests.cs, tests/js/testHelpers.js, tests/js/configPage.test.js</files>
  <read_first>
    - `src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs` — the whole file. `Load()` (line 99) already returns `null` only on a failed read and caches otherwise; `Matches()` (line 78) collapses "unreadable" and "no record" into the same `false`. That collapse is the gap this task closes.
    - `src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs` — the whole file. The `[Authorize(Policy = Policies.RequiresElevation)]` attribute at line 23 and the create-context-per-call shape at lines 41-42.
    - `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` — `showEmbyAuthMessage` (lines 75-85) and `loadEmbyAuthMigration` (lines 87-104). Every new message renders through `showEmbyAuthMessage`; every user-derived string uses `textContent`.
    - `tests/js/testHelpers.js` — `stubApiClient`'s `getJSON` at line 73 returns `{ Users: api.users }`; the stub's response shape must grow with the record.
    - `tests/js/configPage.test.js` — the existing migration-list tests, to see which assertions the new field touches.
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyVerifiedPasswordsTests.cs` — how a read failure is provoked today (a real file under `Path.GetTempPath()`).
    - `.claude/rules/plugin.md` §API and settings page, §Settings.
    - `.planning/phases/03-migration-status-and-target/03-CONTEXT.md` D-02, D-07.
  </read_first>
  <behavior>
    - `EmbyVerifiedPasswords.RecordsAvailable()` returns `true` when the fingerprint file is absent (an absent file is an empty record set, not a failure).
    - `RecordsAvailable()` returns `true` when the file holds valid JSON.
    - `RecordsAvailable()` returns `false` while the file cannot be read or parsed.
    - `RecordsAvailable()` returns `true` again on the next call once the file becomes readable, with no restart and no new instance.
    - `GET /EmbyAuth/Migration` returns `RecordsUnavailable` `false` on a readable file and `true` on an unreadable one.
    - With `RecordsUnavailable` true, the settings page's Migration summary shows a message that names the Jellyfin log.
    - With `RecordsUnavailable` false, that message is absent.
    - The message contains no file system path.
  </behavior>
  <action>
Per D-02, `GET /EmbyAuth/Migration` is reshaped once in this phase to carry the read failure, the task state, and the per-user state together. This task adds the first of those three fields and wires it end to end; plan 03 completes the record. The shipped response carries all three, which is what ROADMAP criterion 2 asks for.

Write the tests first and watch them fail, per CLAUDE.md. For behavior that already works, break the code once and watch the test go red, following the precedent recorded in STATE.md for phases 01 and 02.

In `EmbyVerifiedPasswords`, add a public method `bool RecordsAvailable()` that takes the same `_lock` and returns whether `Load()` returned a dictionary. It must not change `Matches(Guid, string?)`'s signature or behavior — `EmbyAuthenticationProvider` and `MoveToDefaultLoginMethod` call `Matches` and do not care about availability. Because `Load()` caches a successful read and retries after a failed one, `RecordsAvailable()` is cheap and is called once per `GetMigrationStatus` request, never once per user. Give it an XML `<summary>` stating that a `false` result means the file could not be read on this call and that the next call reads the file again.

In `EmbyAuthController`, add a `bool RecordsUnavailable` as the first positional parameter of the `MigrationStatus` record, with an XML `<param>` doc, and populate it from `!verifiedPasswords.RecordsAvailable()` inside `GetMigrationStatus`. Leave the `MigrationUser` record and the per-user readiness field alone in this task — plan 03 replaces them. Keep `[Authorize(Policy = Policies.RequiresElevation)]` on the controller unchanged.

In `configPage.html`, inside `loadEmbyAuthMigration`, render the records-unavailable condition through the existing `showEmbyAuthMessage(summary, text, true)` helper so it gets the warning icon, and keep the existing counts message for the normal case. The text must tell the administrator that Jellyfin cannot read the record of passwords Emby verified, that the readiness of each account is unknown until it can, and that the Jellyfin log has the detail. Per `.claude/rules/plugin.md` §Settings and the threat register below, the text must not contain a file system path or any configured setting value.

In `tests/js/testHelpers.js`, add a `recordsUnavailable` option to `stubApiClient` and include `RecordsUnavailable` in the object `getJSON` resolves with, so the stub's shape matches what the controller now returns. Keep the existing `users` option working unchanged.

Add `EmbyAuthControllerTests.cs` using the Task 1 seam: construct `EmbyAuthController` with an `SqliteJellyfinDbContextFactory`, a real `EmbyVerifiedPasswords` over a temp path, and a `FakeTaskManager`, and assert `GetMigrationStatus` reports `RecordsUnavailable` `false` for a readable file and `true` for an unreadable one. Provoke the read failure the same way `EmbyVerifiedPasswordsTests` already does. Add a test that the same instance reports `true` and then `false` across two calls once the file becomes readable, which is what proves the failure is not sticky.

Add jsdom tests in `tests/js/configPage.test.js` for the message present and absent cases, and one asserting the rendered text contains no `/` path segment from the temp directory — assert on the absence of a path-shaped substring rather than on an exact sentence, so wording can change without churning the test.
  </action>
  <acceptance_criteria>
    - `src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs` declares `public bool RecordsAvailable()`.
    - The signature `public bool Matches(Guid userId, string? passwordHash)` is unchanged in that file.
    - `src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs` declares `MigrationStatus` with a `bool RecordsUnavailable` parameter.
    - A unit test forces a fingerprint read failure and asserts `GetMigrationStatus` returns `RecordsUnavailable` true; another asserts false on a readable file.
    - A unit test calls the same `EmbyVerifiedPasswords` instance twice across a file becoming readable and observes `RecordsAvailable()` go from false to true.
    - A jsdom test drives a `pageshow` with `RecordsUnavailable` true and finds a message under `#EmbyAuthMigrationSummary` whose text matches `/log/i`.
    - A jsdom test drives a `pageshow` with `RecordsUnavailable` false and finds no such message.
    - A jsdom test asserts the records-unavailable text contains no substring matching a file system path.
    - `configPage.html` still contains `[Authorize`-free client code only — the controller file still contains `[Authorize(Policy = Policies.RequiresElevation)]`.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx --filter "FullyQualifiedName~EmbyAuthControllerTests"</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests run / "no test matches"</fails_when>
    <automated>npm --prefix tests/js ci</automated>
    <fails_when>non-zero exit, or npm reports it could not install from the lockfile</fails_when>
    <automated>node --test</automated>
    <fails_when>non-zero exit, or the TAP summary reports `fail 0` absent / any `not ok` line</fails_when>
    <automated>rg -q 'public bool RecordsAvailable\(\)' src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs</automated>
    <fails_when>non-zero exit — the method is absent or its signature differs</fails_when>
    <automated>rg -q 'public bool Matches\(Guid userId, string\? passwordHash\)' src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs</automated>
    <fails_when>non-zero exit — the existing Matches signature changed, which would break its two callers</fails_when>
    <automated>rg -q 'RecordsUnavailable' src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html tests/js/testHelpers.js</automated>
    <fails_when>non-zero exit — the field is missing from the controller, the page, or the stub, so the path is not wired end to end</fails_when>
    <automated>mise run test</automated>
    <fails_when>non-zero exit from any of the four steps the task runs</fails_when>
  </verify>
  <done>One path runs end to end and is committed: an unreadable fingerprint file produces `RecordsUnavailable` true from the controller and a warning message in the Migration section, a readable file produces neither, and the condition clears on the next call without a restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TEST-02 baseline coverage of the two move-query classes, before they are renamed</name>
  <files>tests/Jellyfin.Plugin.EmbyAuth.Tests/DefaultLoginMethodTests.cs, tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyLoginMethodUsersTests.cs</files>
  <read_first>
    - `src/Jellyfin.Plugin.EmbyAuth/DefaultLoginMethod.cs` — the whole file. The `WHERE` clause guards on three columns at once: the user ID, the Emby provider ID, and the exact password hash. Each guard needs its own test.
    - `src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs` — the whole file. `ListAsync` filters on `AuthenticationProviderId`, orders by `Username`, and computes readiness from `Password is not null && verifiedPasswords.Matches(...)`.
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs` — the `SqliteJellyfinDbContextFactory` this task consumes (added in Task 1).
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyVerifiedPasswordsTests.cs` — how a real `EmbyVerifiedPasswords` is stood up over a temp file.
    - `.planning/codebase/TESTING.md` — the unit test conventions.
  </read_first>
  <behavior>
    - `DefaultLoginMethod.MoveAsync` returns true and changes `AuthenticationProviderId` when the user is on the Emby method and the saved hash matches.
    - It returns false and changes nothing when the user is already on another login method.
    - It returns false and changes nothing when the saved hash differs from the one passed.
    - It returns false when no user has that ID.
    - It is parameterized over at least two distinct target provider IDs — Jellyfin's Default and one other — and writes whichever it was given.
    - `EmbyLoginMethodUsers.ListAsync` returns only users whose `AuthenticationProviderId` is the Emby provider ID.
    - It returns an empty list when no user is on the Emby method.
    - It orders the result by user name.
    - It marks a user ready only when the saved hash is non-null and `EmbyVerifiedPasswords.Matches` accepts it.
    - It marks a user not ready when the saved password is null.
  </behavior>
  <action>
Write these tests now, against the class names as they stand, so plan 04's renames and target-parameter change are refactors under a green suite rather than unprotected edits. Plan 04 renames this file with its subject.

`DefaultLoginMethodTests.cs` uses the Task 1 `SqliteJellyfinDbContextFactory`. Seed `User` rows directly through a context, call `DefaultLoginMethod.MoveAsync`, and read the row back through a second context to assert the write landed. Cover each of the five behaviors above.

The target-provider-id parameterization matters beyond coverage: this phase generalizes the move target from a constant to a setting, and a test that only ever moves a user to Jellyfin's Default cannot tell a target-agnostic implementation from one that still hardcodes Default. `MoveAsync` currently writes the `DefaultLoginMethod.ProviderId` constant, so today the second target case is expected to be red — write it as an `[Fact(Skip = ...)]` naming plan 04's task 2 as the task that unskips it, matching the RED-phase convention STATE.md records for phase 01, and leave the other four green. This is the invariant test for the assumption-delta decision recorded in `<assumption_delta_decision>` below: it goes red the moment a later phase reintroduces a hardcoded Default.

`EmbyLoginMethodUsersTests.cs` uses the same seam plus a real `EmbyVerifiedPasswords` over a temp file. Seed a mix of users — on the Emby method with a verified hash, on the Emby method with an unverified hash, on the Emby method with a null password, and on another login method — and assert the filter, the ordering, and the readiness for each. The sort assertion is on names that differ; the relative order of two accounts whose names compare equal is not specified by `ListAsync` and this plan does not assert it.

Each test class constructs and disposes its own `SqliteJellyfinDbContextFactory`; no instance is shared across classes.
  </action>
  <acceptance_criteria>
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/DefaultLoginMethodTests.cs` exists and contains at least five test methods.
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyLoginMethodUsersTests.cs` exists and contains at least five test methods.
    - Both files construct `SqliteJellyfinDbContextFactory` and neither references a static or shared factory instance.
    - `DefaultLoginMethodTests.cs` contains exactly one `Skip =` attribute, and its text names plan 04 as the task that unskips it.
    - A test seeds a user on a non-Emby login method, calls `MoveAsync`, and asserts the returned value is false and the row is unchanged.
    - A test seeds a user whose saved hash differs from the argument, calls `MoveAsync`, and asserts the returned value is false and the row is unchanged.
    - A test seeds users on and off the Emby login method and asserts `ListAsync` returns only the ones on it.
    - A test asserts `ListAsync` returns an empty list when no user is on the Emby login method.
    - A test asserts `ListAsync` returns users in ascending name order.
    - `dotnet test` passes with no failing test and no skipped test other than the one above.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx --filter "FullyQualifiedName~DefaultLoginMethodTests|FullyQualifiedName~EmbyLoginMethodUsersTests"</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests run / "no test matches"</fails_when>
    <automated>test "$(rg -c 'Skip = ' tests/Jellyfin.Plugin.EmbyAuth.Tests/DefaultLoginMethodTests.cs)" = "1"</automated>
    <fails_when>non-zero exit — there is no skipped test, or more than one, meaning a red test was left unaccounted for</fails_when>
    <automated>mise run test</automated>
    <fails_when>non-zero exit from any of the four steps the task runs</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit — `dotnet format --verify-no-changes` found a formatting difference, or a shell or workflow linter reported a finding</fails_when>
    <automated>mise run e2e</automated>
    <fails_when>non-zero exit, or any `not ok` line in the bats output</fails_when>
  </verify>
  <done>TEST-02 covers `DefaultLoginMethod` and `EmbyLoginMethodUsers` against a real `JellyfinDbContext`, the full unit suite is green with exactly one deliberately skipped test, and `mise run e2e` still passes against the reshaped response.</done>
</task>

</tasks>

<assumption_delta_decision>
The phase-scope detector fired on two `pluralization` signals ("a second login method", "also for a run that takes longer than 3 seconds") and one `chosen` signal ("Every move uses the chosen method").

- **Noun now primary:** the login method a user moves to — the *migration target* — generalized from the single constant `Jellyfin.Server.Implementations.Users.DefaultAuthenticationProvider`.
- **Decision: promote.** `DefaultLoginMethod`, a type named after one specific destination, is promoted to `LoginMethodMove`, which takes the destination as a parameter; Jellyfin's Default is demoted to one value of `MigrationTarget` among several, surviving only as the default *value* on a fresh install. The old type is not kept alongside the new one — plan 04 renames it in place, per D-20.
- **Invariant test (accepted):** `DefaultLoginMethodTests` — renamed `LoginMethodMoveTests` in plan 04 — is parameterized over at least two distinct target provider IDs. Task 3 above writes that case skipped, and plan 04 task 2 unskips it. It goes red the moment a future phase reintroduces a hardcoded Default in the move path.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `GET /EmbyAuth/Migration` | An authenticated Jellyfin session reaches the plugin's own API; the response now carries a new field. |
| plugin -> file system | `EmbyVerifiedPasswords` reads a file whose failure state is now reported to a page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Elevation of Privilege | `EmbyAuthController.GetMigrationStatus` | high | mitigate | `[Authorize(Policy = Policies.RequiresElevation)]` stays on the controller; Task 2 changes the response body only, never the attribute. The existing e2e 403 test in `e2e/30-migration-modes.bats` continues to cover the route, and plan 07 extends 403 coverage to every surface this phase adds. |
| T-03-07 | Information Disclosure | the records-unavailable message on `configPage.html` | low | mitigate | The message names the Jellyfin log, never the fingerprint file's path and never a configured setting value, per `.claude/rules/plugin.md` §Settings. A jsdom acceptance criterion asserts the rendered text contains no path-shaped substring. |
| T-03-SC | Tampering | `Microsoft.EntityFrameworkCore.Sqlite` install | high | mitigate | Audited in `03-RESEARCH.md` §Package Legitimacy Audit: NuGet, first-party Microsoft owners, 289M downloads, source `github.com/dotnet/efcore`, verdict OK — not `[ASSUMED]` or `[SUS]`, so no human legitimacy checkpoint is required. Pinned to the exact `10.0.11` that `Jellyfin.Database.Implementations 12.1.0` already resolves, and confined to the test project so it never ships in the plugin zip. NuGet has no postinstall-script concept, so that check is not applicable. |
</threat_model>

<verification>
- `mise run test` is green: unit tests, script tests, and settings-page tests.
- `mise run lint` is green.
- `mise run e2e` is green — CLAUDE.md requires an end-to-end run for every change under `src/`, and this plan changes `src/`.
- `prek run` is green before each commit.
</verification>

<success_criteria>
- A unit test stands up a real `JellyfinDbContext` on SQLite in-memory and observes a row changed by `ExecuteUpdateAsync`.
- `ITaskManager` and `IScheduledTaskWorker` have hand-written doubles in `TestDoubles.cs` with no mocking library added.
- `GET /EmbyAuth/Migration` reports `RecordsUnavailable`, and the Migration section shows a message pointing at the Jellyfin log while it is true.
- The read failure clears on the next call with no Jellyfin restart.
- `DefaultLoginMethod` and `EmbyLoginMethodUsers` have unit tests against the SQLite seam, with exactly one deliberately skipped test that plan 04 unskips.
</success_criteria>

<output>
Create `.planning/phases/03-migration-status-and-target/03-01-SUMMARY.md` when done.
</output>
