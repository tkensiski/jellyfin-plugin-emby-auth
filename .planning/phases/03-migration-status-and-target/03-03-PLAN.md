---
phase: 03-migration-status-and-target
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs
  - src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs
  - src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyLoginMethodUsersTests.cs
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthControllerTests.cs
  - tests/js/testHelpers.js
  - tests/js/configPage.test.js
  - e2e/30-migration-modes.bats
autonomous: true
requirements: [AUTH-06, FPRT-03, UI-03, MIGR-01, TEST-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each user in the migration response carries exactly one of four states: Ready, NeedsEmbyLogin, NoPassword, Unknown."
    - "A user with no saved password is reported NoPassword even while the fingerprint file is unreadable — the database fact wins over the file fact."
    - "A user who has a saved password is reported Unknown, not NeedsEmbyLogin, while the fingerprint file is unreadable."
    - "GET /EmbyAuth/Migration reports the migration task's state, its current progress, the end time of its last run, and that run's result."
    - "The response reports the task as absent, rather than as an idle task that never ran, when no scheduled-task worker for the migration task is registered."
    - "GET /EmbyAuth/Migration lists the login methods Jellyfin reports as enabled, with this plugin's own Emby method removed."
    - "The migration list names every account on the Emby login method that has no saved password."
    - "A user name is rendered with textContent exactly as the database holds it: no case folding, no normalization, no truncation."
    - "The migration list is empty and the summary says no users are on the Emby login method when no account is on it."
    - statement: "The migration list is ordered by user name; the relative order of two accounts whose names compare equal is not specified."
      verification: backstop
    - statement: "The dropdown offers the two fixed entries first and then the enabled login methods in the order Jellyfin's GetAuthenticationProviders returns them; that order is not contractually specified by Jellyfin and is not asserted."
      verification: backstop
  artifacts:
    - src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs
    - src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyLoginMethodUsersTests.cs
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthControllerTests.cs
  key_links:
    - "EmbyVerifiedPasswords.RecordsAvailable + User.Password -> EmbyLoginMethodUsers.ListAsync -> MigrationUserState -> MigrationUser.State -> the migration list on configPage.html"
    - "ITaskManager.ScheduledTasks -> the worker whose ScheduledTask is the migration task -> MigrationTaskInfo -> MigrationStatus.Task"
    - "IUserManager.GetAuthenticationProviders -> the Emby-method filter -> MigrationStatus.AvailableTargets"
  prohibitions:
    - "No log message, exception message, or API response added by this plan contains a password, a password hash, or the Emby API key."
    - "No page content built from a user-derived string uses innerHTML; textContent only."
    - "No analyzer or compiler warning is suppressed without a Justification."
    - "No message shown on the settings page repeats a configured setting value."
    - "The scheduled-task worker is never matched by its Id, which is a Jellyfin-assigned REST-route identifier and not the key the plugin defines."
---

<objective>
Finish the single `GET /EmbyAuth/Migration` reshape that plan 01's tracer started. After this plan the response carries everything the Migration section needs in one payload, which is what D-02 asks for and what ROADMAP criterion 2 measures: the read failure (added by plan 01), the per-user state, the task state, and the list of login methods an administrator may pick as the migration target.

This plan also settles RESEARCH.md's Open Question 1 and Open Question 2 in code rather than leaving them for code review — see the two decision notes in the task actions.

Purpose: the page can only render what the server sends. Plan 05 rewrites the Migration section against this response, and plan 04's move logic reads the same per-user state the task and the API already share through `EmbyLoginMethodUsers.ListAsync`. Both are blocked until the response is complete.

Output: a `MigrationUserState` enum, a `MigrationTaskInfo` record, `MigrationStatus.AvailableTargets`, TEST-03 coverage of all three, and a migration list on the page that renders four states instead of a boolean.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-migration-status-and-target/03-CONTEXT.md
@.planning/phases/03-migration-status-and-target/03-RESEARCH.md
@.planning/phases/03-migration-status-and-target/03-PATTERNS.md
@.claude/rules/plugin.md
@.claude/rules/e2e.md
@.planning/phases/03-migration-status-and-target/03-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Replaces |
|---|---|---|
| `Jellyfin.Plugin.EmbyAuth.MigrationUserState` (values `Ready`, `NeedsEmbyLogin`, `NoPassword`, `Unknown`) | new public enum | the boolean readiness field on `EmbyLoginMethodUser` and `MigrationUser` |
| `EmbyLoginMethodUser.State` | new record property | `EmbyLoginMethodUser.ReadyToMove` |
| `MigrationUser.State` | new record property | `MigrationUser.ReadyToMove` |
| `Jellyfin.Plugin.EmbyAuth.Api.MigrationTaskInfo` | new public record | — |
| `MigrationStatus.Task` | new record property | — |
| `MigrationStatus.AvailableTargets` | new record property | — |
| `EmbyAuthController`'s `IUserManager` constructor parameter | new dependency | — |

<!-- planner-discipline-allow: ReadyToMove, innerHTML -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One state per account, with the database fact winning over the file fact</name>
  <files>src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs, tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyLoginMethodUsersTests.cs</files>
  <read_first>
    - `src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs` — the whole file. The record at line 18 and the projection at lines 46-52 are what change; the query at lines 39-44 stays as it is.
    - `src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs` as plan 01 left it — `RecordsAvailable()` is the new signal, and `Load()` caches, so one call per `ListAsync` invocation is correct and one call per user is waste.
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyLoginMethodUsersTests.cs` as plan 01 task 3 left it — the seeding helpers and the `SqliteJellyfinDbContextFactory` usage to extend.
    - `.planning/phases/03-migration-status-and-target/03-CONTEXT.md` D-01 (one state enum instead of a boolean) and D-07 (every user renders `Unknown` while the file cannot be read).
    - `.planning/phases/03-migration-status-and-target/03-RESEARCH.md` §Open Questions 1 — the precedence question and its recommendation.
    - `.planning/REQUIREMENTS.md` AUTH-06 — the plugin does not invent a password and does not refuse the move.
  </read_first>
  <behavior>
    - A user on the Emby login method with a saved password that `Matches` accepts is `Ready`.
    - A user on the Emby login method with a saved password that `Matches` rejects is `NeedsEmbyLogin`.
    - A user on the Emby login method with no saved password is `NoPassword`.
    - A user on the Emby login method with a saved password, while the fingerprint file cannot be read, is `Unknown`.
    - A user on the Emby login method with no saved password, while the fingerprint file cannot be read, is still `NoPassword` — never `Unknown`.
    - No user is ever reported `NeedsEmbyLogin` while the fingerprint file cannot be read.
    - `ListAsync` asks `RecordsAvailable()` once per call, not once per user.
    - `ListAsync` returns an empty list when no user is on the Emby login method, and asks nothing of the fingerprint file in that case beyond the single availability call.
  </behavior>
  <action>
**Open Question 1 is resolved here: `NoPassword` takes precedence over `Unknown`.** A missing saved password is a fact the database answers on its own; readiness is the only thing the fingerprint file can make unknowable. AUTH-06 requires the settings page to name every account with no saved password, and an account that disappeared into `Unknown` because a file was locked would not be named. Record this decision in the XML documentation on the enum so the next reader does not rediscover it, and cover it with the fifth behavior above.

Declare `public enum MigrationUserState { Ready, NeedsEmbyLogin, NoPassword, Unknown }` in `EmbyLoginMethodUsers.cs`, in namespace `Jellyfin.Plugin.EmbyAuth`, with an XML `<summary>` on the enum and on each member. It lives here, beside the code that computes it, rather than in the `Api` namespace, so the dependency runs from the API to the domain and not back. It must be public: Jellyfin serializes it into the migration response. Plan 02's `TypeVisibilityTests` allowlist already carries `Jellyfin.Plugin.EmbyAuth.MigrationUserState` for exactly this reason.

Replace the boolean readiness field on the `EmbyLoginMethodUser` record with `MigrationUserState State`, and update its XML `<param>` doc. Keep `Id`, `Username`, and `PasswordHash` as they are — the migration task still needs the hash to pass to the move.

In `ListAsync`, call `verifiedPasswords.RecordsAvailable()` once before projecting, then compute each user's state in this order: no saved password gives `NoPassword`; otherwise records unavailable gives `Unknown`; otherwise `Matches` gives `Ready` or `NeedsEmbyLogin`. Leave the `Where`, the `OrderBy`, and the `Select` projection over `Id`, `Username`, and `Password` untouched.

Extend `EmbyLoginMethodUsersTests.cs` with the eight behaviors. Provoke the unreadable-file condition the same way plan 01 and `EmbyVerifiedPasswordsTests` already do. Assert the one-call-per-invocation property by counting calls — either through a counting subclass or by seeding several users and asserting the observable result is consistent for all of them; pick whichever reads more plainly and say which in the SUMMARY.

The sort assertion uses names that differ. The relative order of two accounts whose names compare equal is not specified by `ListAsync` and this plan does not assert it.
  </action>
  <acceptance_criteria>
    - `src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs` declares `public enum MigrationUserState` with exactly the members `Ready`, `NeedsEmbyLogin`, `NoPassword`, `Unknown`.
    - `src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs` contains no occurrence of `ReadyToMove`.
    - The enum's XML documentation states that `NoPassword` takes precedence over `Unknown` and why.
    - A test seeds a user with a null password and an unreadable fingerprint file and asserts the state is `NoPassword`.
    - A test seeds a user with a saved password and an unreadable fingerprint file and asserts the state is `Unknown`.
    - A test seeds several users with saved passwords and an unreadable fingerprint file and asserts none of them is `NeedsEmbyLogin`.
    - A test seeds a user with a verified password and a readable file and asserts the state is `Ready`.
    - A test seeds a user with an unverified password and a readable file and asserts the state is `NeedsEmbyLogin`.
    - A test asserts `ListAsync` returns an empty list when no user is on the Emby login method.
    - `dotnet test` passes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx --filter "FullyQualifiedName~EmbyLoginMethodUsersTests"</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests run / "no test matches"</fails_when>
    <automated>rg -q 'public enum MigrationUserState' src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs</automated>
    <fails_when>non-zero exit — the enum is missing, not public, or in another file</fails_when>
    <automated>! rg -q 'ReadyToMove' src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs</automated>
    <fails_when>non-zero exit — the boolean readiness field survived beside the enum instead of being replaced</fails_when>
    <automated>test "$(rg -c 'RecordsAvailable\(\)' src/Jellyfin.Plugin.EmbyAuth/EmbyLoginMethodUsers.cs)" = "1"</automated>
    <fails_when>non-zero exit — availability is asked more than once per call, or not at all</fails_when>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx</automated>
    <fails_when>non-zero exit, or any line containing "error CS" or "error CA" in the build output</fails_when>
  </verify>
  <done>Every account on the Emby login method carries exactly one of four states, a missing saved password is reported as such whatever the fingerprint file is doing, and no account is reported as needing an Emby login while the file cannot be read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The response carries the task state and the login methods an administrator may pick</name>
  <files>src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs, tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthControllerTests.cs</files>
  <read_first>
    - `src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs` as plan 01 left it — the whole file. The primary constructor at lines 26-29, the `[Authorize(Policy = Policies.RequiresElevation)]` attribute, and the record declarations at the end of the file.
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthControllerTests.cs` as plan 01 left it — the construction of the controller over the SQLite seam.
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs` — `FakeTaskManager`, `FakeScheduledTaskWorker`, and `FakeUserManager.AuthenticationProviders`, all added by plan 01.
    - `.planning/phases/03-migration-status-and-target/03-RESEARCH.md` §Architecture Patterns Pattern 2 (the reflected `ITaskManager` and `IScheduledTaskWorker` member lists), Pattern 4 (the drafted record shapes), Pattern 5 (the provider filter), §Common Pitfalls 5 (the worker Id trap), and §Open Questions 2.
    - `.planning/phases/03-migration-status-and-target/03-CONTEXT.md` D-02, D-03, D-11, D-13.
    - `.claude/rules/plugin.md` §API and settings page — the authorization rule and the PascalCase response convention.
  </read_first>
  <behavior>
    - `GET /EmbyAuth/Migration` reports the migration task's `State`, `Progress`, `LastEndTimeUtc`, and `LastResult` when a worker for that task is registered.
    - It reports the task as absent when no registered worker's scheduled task is the migration task.
    - It reports `LastEndTimeUtc` and `LastResult` as absent when the worker has no last execution result.
    - It reports progress while the worker's state is `Running`.
    - It lists every login method `IUserManager.GetAuthenticationProviders()` returns except this plugin's own Emby method.
    - It lists an empty set of targets when Jellyfin reports only this plugin's own Emby method.
    - `POST /EmbyAuth/Migration/Run` still queues the migration task and returns 204.
    - Both routes still require elevation.
  </behavior>
  <action>
**Open Question 2 is resolved here: the enabled-login-method list rides on `GET /EmbyAuth/Migration` rather than a second endpoint.** D-02 states the principle — one change carries everything the Migration section needs — and D-16 puts the target dropdown inside the same section this endpoint already feeds on the same page load. A second endpoint would add a second round trip and a second failure-message path for data the page needs at exactly the same moment. Record this in the XML documentation on `MigrationStatus`.

Add a public record `MigrationTaskInfo` in the `Api` namespace carrying `TaskState State`, `double? Progress`, `DateTime? LastEndTimeUtc`, and `TaskCompletionStatus? LastResult`, each with an XML `<param>` doc. `TaskState`, `TaskResult`, and `TaskCompletionStatus` all live in `MediaBrowser.Model.Tasks`, which this file already imports; `TaskState` has `Idle`, `Cancelling`, `Running` and `TaskCompletionStatus` has `Completed`, `Failed`, `Cancelled`, `Aborted` — both reflected against `MediaBrowser.Model 12.1.0` during planning.

Grow the `MigrationStatus` record to `(bool RecordsUnavailable, MigrationTaskInfo? Task, IReadOnlyList<MigrationUser> Users, IReadOnlyList<NameIdPair> AvailableTargets)`. `Task` is nullable and is null when no worker is registered for the migration task — a null is honest, where a fabricated idle-never-ran record would make the page render a stale list as if it were current. `NameIdPair` is `MediaBrowser.Model.Dto.NameIdPair` and carries `Name` and `Id`, both strings; add the `using` for it.

Replace the boolean readiness field on the `MigrationUser` record with `MigrationUserState State`, mapping straight through from `EmbyLoginMethodUser.State`. Update its XML `<param>` doc to describe all four states.

Find the task's worker with `taskManager.ScheduledTasks.FirstOrDefault(worker => worker.ScheduledTask is MoveEmbyUsersToDefaultTask)` — a type check. Never match on `IScheduledTaskWorker.Id`: that is a Jellyfin-assigned identifier for the `/ScheduledTasks/{id}` REST route, not the key the plugin defines on `IScheduledTask`, so an Id comparison silently matches nothing. Plan 04 renames the task type; this line changes with it. Read `LastEndTimeUtc` and `LastResult` through `worker.LastExecutionResult?.` — Jellyfin returns no result object until the task has run once.

Add `IUserManager userManager` to the controller's primary constructor with an XML `<param>` doc, and import `MediaBrowser.Controller.Library`. `.claude/rules/plugin.md` forbids taking `IUserManager` in a *login method's* constructor because `IUserManager` depends on every login method; a controller is built per request by ASP.NET Core, long after the provider graph is constructed, so there is no cycle here. Populate `AvailableTargets` from `userManager.GetAuthenticationProviders()` filtered to drop the entry whose `Id` equals `EmbyAuthenticationProvider.ProviderId`. Do the filtering server-side: hiding an option in the browser is not a control, and plan 06's save-time refusal validates against the same live list.

Leave `[Authorize(Policy = Policies.RequiresElevation)]`, the `[HttpGet]`/`[HttpPost]` routes, and `RunMigration`'s `QueueIfNotRunning` call exactly as they are.

Extend `EmbyAuthControllerTests.cs` with the eight behaviors, using `FakeTaskManager` seeded with a `FakeScheduledTaskWorker` over a real migration task instance, and `FakeUserManager.AuthenticationProviders` seeded with a list that includes an entry whose `Id` is `EmbyAuthenticationProvider.ProviderId`, so the filter has something to remove. Add a test that a worker for some *other* scheduled task does not satisfy the lookup — that is the test that would have caught an Id-based match.
  </action>
  <acceptance_criteria>
    - `src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs` declares `public sealed record MigrationTaskInfo` with parameters named `State`, `Progress`, `LastEndTimeUtc`, and `LastResult`.
    - `MigrationStatus` declares parameters named `RecordsUnavailable`, `Task`, `Users`, and `AvailableTargets`.
    - `MigrationStatus`'s `Task` parameter type is nullable.
    - `MigrationUser` declares a `State` parameter and the file contains no occurrence of `ReadyToMove`.
    - The controller's constructor takes `IUserManager`.
    - The file contains `worker.ScheduledTask is` and contains no comparison against a worker's `Id` property.
    - `[Authorize(Policy = Policies.RequiresElevation)]` is still present exactly once.
    - A test seeds a `FakeScheduledTaskWorker` in `Running` state with a progress value and asserts the response reports both.
    - A test seeds a `FakeTaskManager` whose only worker wraps a different scheduled task and asserts the response's task field is null.
    - A test seeds a worker with no last execution result and asserts `LastEndTimeUtc` and `LastResult` are both absent.
    - A test seeds `AuthenticationProviders` with three entries, one of them the Emby method, and asserts `AvailableTargets` holds the other two.
    - A test asserts `RunMigration` returns 204 and `FakeTaskManager.QueuedTypes` holds the migration task type.
    - `dotnet test` passes.
  </acceptance_criteria>
  <verify>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx --filter "FullyQualifiedName~EmbyAuthControllerTests"</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests run / "no test matches"</fails_when>
    <automated>rg -q 'record MigrationTaskInfo' src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs</automated>
    <fails_when>non-zero exit — the task record is absent, so UI-03 has no server-side data to render</fails_when>
    <automated>rg -q 'AvailableTargets' src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs</automated>
    <fails_when>non-zero exit — the target list is absent, so the dropdown in plan 05 has no source</fails_when>
    <automated>rg -q 'worker\.ScheduledTask is' src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs</automated>
    <fails_when>non-zero exit — the worker is matched some other way, most likely by the Id that never matches</fails_when>
    <automated>! rg -q 'ReadyToMove' src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs</automated>
    <fails_when>non-zero exit — the boolean readiness field survived in the response alongside the state</fails_when>
    <automated>test "$(rg -c 'Authorize\(Policy = Policies\.RequiresElevation\)' src/Jellyfin.Plugin.EmbyAuth/Api/EmbyAuthController.cs)" = "1"</automated>
    <fails_when>non-zero exit — the elevation policy was removed or duplicated while the response was reshaped</fails_when>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx</automated>
    <fails_when>non-zero exit, or any line containing "error CS" or "error CA" in the build output</fails_when>
  </verify>
  <done>`GET /EmbyAuth/Migration` returns the read-failure flag, the task state, the per-user state, and the pickable login methods in one payload, all four covered by unit tests, and both routes still require elevation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The migration list renders four states, and the end-to-end suite reads them</name>
  <files>src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html, tests/js/testHelpers.js, tests/js/configPage.test.js, e2e/30-migration-modes.bats</files>
  <read_first>
    - `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` as plan 01 left it — `loadEmbyAuthMigration` (lines 87-104), which counts a boolean and renders one of two strings per user. `showEmbyAuthMessage` (lines 75-85) is the only way a message is written.
    - `tests/js/testHelpers.js` as plan 01 left it — `stubApiClient.getJSON` and the `recordsUnavailable` option plan 01 added.
    - `tests/js/configPage.test.js` — the existing migration-list tests, which assert on the rendered `<li>` text through `listItemTexts`.
    - `e2e/30-migration-modes.bats` — `migration_ready_state` at line 17 reads the boolean readiness field with `jq`; three tests compare its output against `true`, `false`, and the empty string.
    - `.claude/rules/e2e.md` §Conventions — each file owns its user names; a file must not depend on another file.
    - `.claude/rules/plugin.md` §API and settings page — `textContent`, never `innerHTML`; Jellyfin serializes with PascalCase property names.
  </read_first>
  <behavior>
    - A `Ready` account renders with wording that says it will move on the next run.
    - A `NeedsEmbyLogin` account renders with wording that says it needs one login while Emby runs.
    - A `NoPassword` account renders with wording that says it has no saved password.
    - An `Unknown` account renders with wording that says its readiness is unknown.
    - The summary counts accounts by state rather than by a single ready-or-not split.
    - An empty user list renders the existing no-users summary and no list items.
    - A user name containing characters that are meaningful in HTML is rendered literally, not as markup.
    - The end-to-end suite reads each account's state from the response and still distinguishes a ready account from one that is not.
  </behavior>
  <action>
This task is the minimum rendering change that keeps the page and the end-to-end suite truthful against the reshaped response. Plan 05 rewrites the section around it with the target dropdown, the polling loop, and the no-password warning; do not start those here.

In `loadEmbyAuthMigration`, replace the boolean split with a switch over the four `MigrationUserState` values. Jellyfin serializes the enum into the JSON, so the page compares against the member names as strings: `Ready`, `NeedsEmbyLogin`, `NoPassword`, `Unknown`. Build each list item's text with `textContent` on the `<li>`, exactly as line 98 does today — never `innerHTML`, and never a template that concatenates a user name into markup. A user name is rendered as the database holds it, with no case folding, normalization, or truncation.

Replace the summary's ready-count with a count per state, so an administrator can see at a glance how many accounts are in each condition. Keep the existing no-users summary for an empty list. Keep the records-unavailable message plan 01 added: when that flag is set every account is `Unknown` anyway, so the two agree rather than compete.

In `tests/js/testHelpers.js`, update `stubApiClient.getJSON` to resolve with the full response shape — the read-failure flag, a task object, the users, and the available targets — with options for each and defaults that make an ordinary successful load the zero-configuration case. Keep the existing `users` and `recordsUnavailable` options working.

In `tests/js/configPage.test.js`, add the eight behaviors. For the HTML-meaningful-characters case, seed a user name containing angle brackets and assert the rendered element has no child elements and its text equals the seeded name exactly.

In `e2e/30-migration-modes.bats`, change `migration_ready_state` to read `.Users[] | select(.Name == $n) | .State` and update the three call sites: the account that logged in through Emby now expects `Ready` where it expected the boolean true; the two that did not now expect `NeedsEmbyLogin` where they expected false; the account that has moved off the Emby method still expects the empty string, because it is no longer in the list at all. Rename the helper to match what it now reads. Touch nothing else in that file — it owns its own user names and must not start depending on another file.
  </action>
  <acceptance_criteria>
    - `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` contains the literals `NeedsEmbyLogin`, `NoPassword`, and `Unknown`.
    - `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` contains no occurrence of `ReadyToMove`.
    - `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` contains no occurrence of `innerHTML`.
    - `e2e/30-migration-modes.bats` contains no occurrence of `ReadyToMove`.
    - `e2e/30-migration-modes.bats` reads `.State` from the migration response.
    - A jsdom test seeds one account in each of the four states and asserts four distinct rendered strings.
    - A jsdom test seeds a user name containing `<` and `>` and asserts the rendered `<li>` has zero element children and text equal to the seeded name.
    - A jsdom test seeds an empty user list and asserts the summary says no users are on the Emby login method and the list has no items.
    - `node --test` passes.
    - `mise run e2e` passes, including `e2e/30-migration-modes.bats`.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix tests/js ci</automated>
    <fails_when>non-zero exit, or npm reports it could not install from the lockfile</fails_when>
    <automated>node --test</automated>
    <fails_when>non-zero exit, or any `not ok` line in the TAP output</fails_when>
    <automated>rg -q 'NeedsEmbyLogin' src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html</automated>
    <fails_when>non-zero exit — the page still renders a boolean split rather than the four states</fails_when>
    <automated>! rg -q 'innerHTML' src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html</automated>
    <fails_when>non-zero exit — a user-derived string is being written as markup</fails_when>
    <automated>! rg -q 'ReadyToMove' src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html e2e/30-migration-modes.bats tests/js/testHelpers.js</automated>
    <fails_when>non-zero exit — a consumer of the removed boolean field was left behind and will read undefined at runtime</fails_when>
    <automated>rg -q '\.State' e2e/30-migration-modes.bats</automated>
    <fails_when>non-zero exit — the end-to-end suite is not reading the new per-user state</fails_when>
    <automated>mise run test</automated>
    <fails_when>non-zero exit from any of the four steps the task runs</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit — a formatting, shellcheck, shfmt, actionlint, or zizmor finding</fails_when>
    <automated>mise run e2e</automated>
    <fails_when>non-zero exit, or any `not ok` line in the bats output</fails_when>
  </verify>
  <done>The migration list shows one of four conditions per account, no consumer of the removed boolean field is left anywhere in the repository, and the end-to-end suite passes against a real Jellyfin server returning the reshaped response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `GET /EmbyAuth/Migration` | The response now carries the task state and the list of login methods an administrator may pick. |
| Jellyfin -> plugin | `IUserManager.GetAuthenticationProviders()` and `ITaskManager.ScheduledTasks` are Jellyfin-owned data the plugin now reflects to a page. |
| database -> page | User names cross from the database into the DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Elevation of Privilege | `MigrationStatus.AvailableTargets` and `MigrationStatus.Task` | high | mitigate | Both ride on the existing `GET /EmbyAuth/Migration` route, which keeps `[Authorize(Policy = Policies.RequiresElevation)]`; an acceptance criterion asserts the attribute appears exactly once after the reshape. No new route is added, so no new surface needs its own 403 test; the existing e2e 403 test in `e2e/30-migration-modes.bats` covers it, and plan 07 re-confirms 403 coverage for the phase as a whole. |
| T-03-02 | Tampering | this plugin's own Emby method appearing as a migration target | medium | mitigate | The filter that removes `EmbyAuthenticationProvider.ProviderId` runs server-side in `GetMigrationStatus`, not in the browser, and a unit test seeds the Emby method into `FakeUserManager.AuthenticationProviders` and asserts it is absent from `AvailableTargets`. Plan 06 validates the saved value against the same live list, so a client that submits the filtered-out id directly is still refused. |
| T-03-10 | Cross-Site Scripting | the migration list on `configPage.html` | high | mitigate | Every user-derived string is written through `textContent`, never `innerHTML`, per `.claude/rules/plugin.md`. A jsdom acceptance criterion seeds a user name containing angle brackets and asserts the rendered element has zero element children, and a verify gate greps the page for `innerHTML`. |
| T-03-05 | Information Disclosure | the reshaped response body | medium | mitigate | `AvailableTargets` carries only the `Name` and `Id` Jellyfin already publishes for each login method; `MigrationTaskInfo` carries a state, a progress number, a timestamp, and a completion status. No password, hash, Emby URL, or API key enters the response, and no new message repeats a configured setting value. |
</threat_model>

<verification>
- `mise run test` and `mise run lint` green.
- `mise run e2e` green, including the updated `e2e/30-migration-modes.bats`.
- `prek run` green before each commit.
- `scripts/dev-env.sh up` and `down` are unaffected — this plan changes no file `dev-env.sh` reuses.
</verification>

<success_criteria>
- One `GET /EmbyAuth/Migration` response carries the read failure, the per-user state, the task state, and the pickable login methods.
- A missing saved password is reported as such whatever the fingerprint file is doing.
- The task's worker is found by type, never by the Jellyfin-assigned route identifier.
- Nothing anywhere in the repository still reads the removed boolean readiness field.
- `mise run e2e` passes against a real Jellyfin server.
</success_criteria>

<output>
Create `.planning/phases/03-migration-status-and-target/03-03-SUMMARY.md` when done.
</output>
