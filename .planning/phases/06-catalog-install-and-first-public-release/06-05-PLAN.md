---
phase: 06-catalog-install-and-first-public-release
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - CHANGELOG.md
  - Directory.Build.props
  - e2e/compose.yaml
autonomous: false
requirements: [PUB-05, PUB-04, REL-04]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`CHANGELOG.md` holds a dated `## [1.0.0.0] - <YYYY-MM-DD>` section written from what the `0.9.0.0` catalog install and update actually turned up, plus a line stating this is the first stable release; nothing in it was invented to make the entry look substantial (REL-04, D-12)"
    - "`Directory.Build.props` reads `1.0.0.0` in all three version properties, and that change, the dated changelog section, and the end-to-end plugin mount folder are in the same commit"
    - "The maintainer, and nobody and nothing else, merges the version change and pushes `v1.0.0.0` (PUB-05, D-13)"
    - "After the tag push, the manifest at `https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json` lists both `0.9.0.0` and `1.0.0.0`, in ascending version order, with `0.9.0.0` still present and unchanged (PUB-02's keep-every-version rule, D-10)"
    - "Each of the two published entries carries the `CHANGELOG.md` section for its own version, byte-for-byte; the `0.9.0.0` entry still carries the text that shipped with `0.9.0.0` (REL-04, D-06's frozen-at-release-time property)"
    - "`scripts/manifest.sh verify` against the live manifest URL exits 0 and reports two version entries, so both zips were re-checked against the checksums the document records (D-05)"
    - "The same server that installed `0.9.0.0` from the catalog receives and applies the update to `1.0.0.0` (PUB-04, roadmap criterion 5)"
    - statement: "On the same clean Jellyfin 12.1 server that installed 0.9.0.0, the dashboard offers the update to 1.0.0.0 and the update installs and loads"
      verification: backstop
  artifacts:
    - path: "CHANGELOG.md"
      provides: "the dated 1.0.0.0 section written after the rehearsal, and a fresh empty Unreleased heading"
      contains: "## [1.0.0.0] - "
    - path: "Directory.Build.props"
      provides: "the three version properties back at 1.0.0.0"
      contains: "1.0.0.0"
  key_links:
    - from: ".planning/phases/06-catalog-install-and-first-public-release/06-04-SUMMARY.md"
      to: "CHANGELOG.md"
      via: "the 1.0.0.0 section is written from what the rehearsal's catalog walkthrough recorded"
      pattern: "1.0.0.0"
    - from: "the tag v1.0.0.0"
      to: "the published manifest"
      via: "release.yml creates the release, pages.yml rebuilds from every release and republishes"
      pattern: "workflow_run"
  prohibitions:
    - requirement_id: PUB-05
      category: safety
      verification: judgment
      statement: "MUST NOT run, script, schedule, or suggest an agent-run path for `git push origin v1.0.0.0`, `gh release create`, or the merge of the version change to the default branch, and MUST NOT treat a checkpoint answer, a plan approval, or a prior conversation as the maintainer's consent to publish. This is the release the milestone is named for; it cannot be unpublished from anyone who downloads it."
    - requirement_id: REL-04
      category: transparency
      verification: judgment
      statement: "MUST NOT invent content for the `1.0.0.0` changelog entry, and MUST NOT hold back content from `0.9.0.0` so that `1.0.0.0` has more to say. If the rehearsal was clean, the entry says so plainly and is short. A changelog padded to look substantial misrepresents what changed between two versions an administrator can install side by side."
    - requirement_id: PUB-02
      category: transparency
      verification: judgment
      statement: "MUST NOT remove, hide, or filter the `0.9.0.0` entry from the published manifest once `1.0.0.0` exists. Dropping it breaks Jellyfin's update comparison and removes the only downgrade path an administrator has, and it would do so silently — a catalog that lists less than exists shows no error anywhere."
---

<objective>
Publish `1.0.0.0` through the same release workflow the rehearsal just proved, with a changelog written from what the rehearsal actually found, and confirm the manifest then lists both versions and the server that installed the first receives the second.

Purpose: this is the release the milestone is named for, and it is deliberately the least novel step in the phase. Every mechanism it uses ran once already, four days or four minutes ago, against a release nobody had to be told about. The only genuinely new thing here is the update path — two versions in one manifest, and a server comparing what it has against what the catalog offers.

Output: a dated `1.0.0.0` changelog section, the version restored in `Directory.Build.props`, a published `v1.0.0.0` release, a manifest listing both versions, and an observed update on a real server.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-catalog-install-and-first-public-release/06-CONTEXT.md
@.planning/phases/06-catalog-install-and-first-public-release/06-04-SUMMARY.md
</context>

<flagged_assumptions>
One item the deterministic edge probe surfaced for this phase could not be classified and is carried forward here rather than resolved or dropped:

- **PUB-05, category `unclassified`** — the probe emitted `unclassified — review manually` with no predicate. PUB-05 states that v1.0.0 is tagged and published through the release workflow. It has no data shape for an edge taxonomy to reach: there is no input to be empty, no elements to compare equal, no encoding, and no ordering beyond the one this plan's `depends_on` already fixes. The requirement is closed by a human act that no execution run performs, and its verification is `gh release list` plus the manifest listing both versions — both of which are acceptance criteria in this plan's Task 2. This item stays `unresolved` by design: it is recorded as an explicit assumption that PUB-05 needs no edge predicate, not silently dismissed. If a reviewer disagrees, the place to add one is here.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: The 1.0.0.0 changelog, written from what the rehearsal found, and the version it names</name>
  <files>CHANGELOG.md, Directory.Build.props, e2e/compose.yaml</files>
  <read_first>
    - `.planning/phases/06-catalog-install-and-first-public-release/06-04-SUMMARY.md` — the whole file, and especially what the catalog walkthrough recorded: what was confusing, wrong, or missing; how the dashboard rendered the changelog field; whether the Pages chain fired on its own. This is the input to the section this task writes. It cannot be written from anything else.
    - `CHANGELOG.md` — the current file: the empty `## Unreleased` heading and the dated `## [0.9.0.0]` section below it.
    - `Directory.Build.props` — the three version properties, currently `0.9.0.0`.
    - `e2e/compose.yaml` — the plugin bind mount, currently naming `EmbyAuth_0.9.0.0`.
    - `scripts/package.sh` — `changelog_entry` and the heading form it matches.
    - `tests/scripts/package.bats` — the test plan 06-04 added asserting the repository's own changelog holds a dated section for the version in `Directory.Build.props`. It is why the three changes below are one commit.
    - `docs/development.md` — the Releases procedure, including the list of what a version change touches.
    - `~/.claude/CLAUDE.md` §Writing — write the end state, not the route to it; do not hard-wrap prose at a fixed column.
  </read_first>
  <action>
    Rename nothing and reorder nothing in the existing `0.9.0.0` section. It shipped; the manifest entry for it was frozen at release time and a later edit to this file cannot change what the catalog says about it, but an edit here would still make the repository disagree with the catalog. Leave it exactly as it is.

    Add `## [1.0.0.0] - <YYYY-MM-DD>`, using the date the commit is made, directly under the `## Unreleased` heading and above the `0.9.0.0` section. Leave `## Unreleased` in place with no subsections under it.

    Write the section body from `06-04-SUMMARY.md` and from nothing else. Its content is whatever the `0.9.0.0` catalog install and update actually turned up — a README step that was wrong or missing, a label that did not match, a rendering that surprised, a defect that was fixed between the two releases — plus one line stating that this is the first stable release.

    If the rehearsal was clean, say that plainly and stop. A short entry is the correct outcome of a clean rehearsal. Do not pad it with restatements of what `0.9.0.0` already said, do not manufacture a "Changed" subsection out of nothing, and do not move content out of `0.9.0.0`'s section to give this one more to say — `0.9.0.0` shipped the code it shipped, and rewriting history between two installable versions is how a changelog stops being usable.

    Remember what this text is for: it is the blurb beside `1.0.0.0` in the Jellyfin catalog, read by an administrator deciding whether to take the update, and read by one who is installing for the first time and sees this version rather than `0.9.0.0`. Write for both.

    If `06-04-SUMMARY.md` recorded that the dashboard rendered the changelog field as literal markdown characters rather than as formatted text, keep this section's body to plain prose and plain bullet lines, with no `###` subheadings. That is the cosmetic adjustment the rehearsal exists to inform, and it is a decision about this section only — `0.9.0.0`'s shipped text stays as it is.

    Set all three version properties in `Directory.Build.props` to `1.0.0.0`, and change the plugin bind-mount target in `e2e/compose.yaml` back to `EmbyAuth_1.0.0.0`. These two changes and the changelog section are one commit, for the reason `docs/development.md` now records: `mise run test` runs `tests/scripts/package.bats`, which asserts the repository's own changelog holds a dated section for the version in `Directory.Build.props`, so splitting them leaves a red commit on the branch.

    Run `mise run test`, then `mise run e2e`, then `mise run lint`.

    Batch every change to each file into as few edits as possible.
  </action>
  <acceptance_criteria>
    - `[ "$(rg -c '^## \[1\.0\.0\.0\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md)" = "1" ]` — exactly one dated section for this version, in the form `changelog_entry` matches.
    - `[ "$(rg -c '^## \[0\.9\.0\.0\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md)" = "1" ]` — the shipped section is still there, exactly once.
    - `git diff HEAD~1 -- CHANGELOG.md | rg -q '^-.*0\.9\.0\.0'; test $? -eq 1` — the commit removed no line belonging to the shipped `0.9.0.0` section.
    - `rg -q '^## Unreleased$' CHANGELOG.md` matches, and its line number is less than the line number of the `## [1.0.0.0]` heading, which is less than the line number of the `## [0.9.0.0]` heading.
    - `[ "$(rg -c '<Version>1\.0\.0\.0</Version>|<AssemblyVersion>1\.0\.0\.0</AssemblyVersion>|<FileVersion>1\.0\.0\.0</FileVersion>' Directory.Build.props)" = "3" ]` — all three version properties moved back.
    - `rg -q 'EmbyAuth_1\.0\.0\.0' e2e/compose.yaml` matches and `rg -q 'EmbyAuth_0\.9\.0\.0' e2e/compose.yaml; test $? -eq 1` — the mount moved with the version.
    - `scripts/package.sh check-tag v1.0.0.0` exits 0.
    - The `1.0.0.0` section body traces to `06-04-SUMMARY.md`: every factual claim in it appears in that summary, and the section contains no claim the summary does not support.
    - `mise run test` exits 0, `mise run e2e` exits 0, `mise run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>[ "$(rg -c '^## \[1\.0\.0\.0\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md)" = "1" ] &amp;&amp; [ "$(rg -c '^## \[0\.9\.0\.0\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md)" = "1" ] &amp;&amp; scripts/package.sh check-tag v1.0.0.0</automated>
    <fails_when>non-zero exit — the new dated section is absent, duplicated, or not in the form the extractor matches; or the shipped `0.9.0.0` section was removed or reworded out of that form; or `Directory.Build.props` does not name the version the tag will name</fails_when>
    <automated>! git diff HEAD~1 -- CHANGELOG.md | rg -q '^-.*0\.9\.0\.0'</automated>
    <fails_when>non-zero exit — the commit deleted a line belonging to the shipped `0.9.0.0` section, which would make the repository disagree with what the catalog already says about a version people can install</fails_when>
    <automated>mise run test &amp;&amp; mise run e2e &amp;&amp; mise run lint</automated>
    <fails_when>non-zero exit from any of the three tasks; an end-to-end failure across many files at once means the plugin no longer loads from the renamed mount</fails_when>
  </verify>
  <done>`CHANGELOG.md` holds an empty `## Unreleased`, a dated `## [1.0.0.0]` section written from the rehearsal's findings and nothing else, and the untouched shipped `## [0.9.0.0]` section below it; `Directory.Build.props` and the end-to-end plugin mount both name `1.0.0.0`; `scripts/package.sh check-tag v1.0.0.0` passes; and all three mise tasks are green.</done>
  <reversibility rating="reversible">A version string, a compose mount and a changelog section, all revertable in one commit while the tag has not been pushed.</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: the maintainer merges the version change and pushes v1.0.0.0</name>
  <decision>Version 1.0.0.0 is published, or it is not, right now</decision>
  <context>
    Everything this plan could prepare is prepared, and everything this release depends on has already run once for real. `0.9.0.0` went through the release workflow, the Pages chain published a manifest, and an administrator installed the plugin from the catalog on a clean server. `CHANGELOG.md` now holds a dated `1.0.0.0` section written from what that walkthrough found; `Directory.Build.props` names `1.0.0.0`; `scripts/package.sh check-tag v1.0.0.0` passes; `mise run test`, `mise run e2e` and `mise run lint` are green.

    This checkpoint exists for the same reason the last one did, and for one more. A release cannot be unpublished from anyone who downloaded it. The release workflow creates a real public release the moment the tag lands, with no staging step. And putting the two commands in your hands means no answer given at a checkpoint can be read by any agent as consent to publish. The additional reason here is that this is the release the milestone is named for — it is the version an administrator finding this plugin for the first time will install.

    So: no agent runs either command, and answering this checkpoint does not cause either to be run.

    Two things have to happen, in this order, for the same reason as last time — `scripts/release-gate.sh` refuses any tag whose commit is not identical to or behind the default branch, and a passing check run on an open pull request's head is not a merge.

    1. Merge the version-and-changelog change to `main`, and wait for `ci-success` to finish green on the merge commit.
    2. Then, on the merge commit: `git tag v1.0.0.0` and `git push origin v1.0.0.0`.

    What should happen next is what happened last time: `release.yml` publishes the release, then `pages.yml` rebuilds the manifest from every release — both of them now — and republishes it. `0.9.0.0` stays listed; the rebuild enumerates the live releases and does not filter, and the repository's own decision is that every released version stays in the manifest forever, because removing one breaks Jellyfin's update comparison and the only downgrade path an administrator has. If the Pages run does not appear, the manual dispatch recorded in `docs/development.md` is the fallback, and I will check for the run and use it when you resume.

    If something is wrong with this build, the fix is forward to `1.0.1.0` (D-14). The tag and the release are not deleted and re-pushed: reusing a version number would let two different zips call themselves the same thing, and the stateless rebuild picks up the new version with no extra work anyway.
  </context>
  <options>
    <option id="pushed">
      <name>I merged the change and pushed the tag</name>
      <pros>PUB-05 is satisfied, the manifest lists two versions so the update path can be observed on a real server, and the milestone's headline release exists.</pros>
      <cons>Irreversible for anyone who downloads it. Both `0.9.0.0` and `1.0.0.0` stay in the manifest permanently.</cons>
    </option>
    <option id="holding">
      <name>Not yet; I am holding the push</name>
      <pros>Nothing further is published. `0.9.0.0` is already out and working, so the catalog is not in a broken state while you wait.</pros>
      <cons>PUB-05 stays open, and roadmap criterion 5 — the same server receiving the update — cannot be exercised with only one version in the manifest.</cons>
    </option>
    <option id="blocked">
      <name>Something needs fixing before the tag</name>
      <pros>A defect gets fixed before it becomes the version everyone installs.</pros>
      <cons>Needs its own work before this checkpoint can be reached again.</cons>
    </option>
  </options>
  <resume-signal>Reply `pushed`, `holding`, or `blocked`. If `blocked`, name what needs fixing. Whatever you reply, no agent merges anything or pushes any tag — only you do.</resume-signal>
  <reversibility rating="one-way">The tag push publishes a release at a public URL that administrators may add to a live server, and the repository's own keep-every-version decision forbids removing it from the manifest afterwards.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Both versions in one manifest, and the update the second one makes possible</name>
  <precondition>`gh release list` includes a release for the tag `v1.0.0.0` — the maintainer pushed the tag at the previous checkpoint. If it does not, the checkpoint was answered `holding` or `blocked` and this task has nothing to verify.</precondition>
  <files>none — this task verifies and records, and changes no tracked file</files>
  <read_first>
    - `.planning/phases/06-catalog-install-and-first-public-release/06-04-SUMMARY.md` — what the first release's published manifest entry looked like as it was served, so the `0.9.0.0` entry can be compared against it rather than only against the file.
    - `CHANGELOG.md` — both dated sections, which are the two texts the two published entries must carry.
    - `scripts/manifest.sh` — the `verify` action's argument shape and what its success line reports.
    - `.github/workflows/pages.yml` — the trigger and the guard, so an absent run is diagnosed against what the file says.
    - `docs/development.md` — the manual-dispatch recovery.
  </read_first>
  <action>
    Confirm the release exists and the release workflow succeeded: list the releases and confirm both `v0.9.0.0` and `v1.0.0.0` are present and neither is marked a pre-release, and confirm the Release workflow run for `v1.0.0.0` concluded `success`. If it did not, stop and report which gate refused.

    Then confirm the Pages run. If none appeared, start one with the documented manual dispatch and record that the fallback was needed a second time — two fallbacks in two releases is a different finding from one, and it means the `workflow_run` chain is not working for this repository rather than hitting a first-completion edge.

    Then check the published document from outside the workflow. Fetch the manifest URL and confirm it parses as a JSON array, holds exactly one package whose guid is the plugin's, and that package holds exactly two version entries. Confirm their order: `0.9.0.0` first, `1.0.0.0` second, ascending. Confirm each entry's `sourceUrl` points at its own release's zip and each `targetAbi` is the derived value.

    Then check the two changelogs. Extract each version's section body from `CHANGELOG.md` the same way `scripts/package.sh` does, and compare byte-for-byte against the corresponding published entry's `changelog` field. The `0.9.0.0` comparison is the one that carries a second claim: that entry was frozen when `0.9.0.0` was packaged, so it proves a later edit to `CHANGELOG.md` cannot rewrite what the catalog says about a shipped version. If the `0.9.0.0` entry differs from what `06-04-SUMMARY.md` recorded it as, stop and report it — the rebuild reads each release's own attached manifest, so a difference there means something changed that should not have.

    Then run `scripts/manifest.sh verify` against the live URL from a laptop and confirm it reports two version entries checked.

    Record in the SUMMARY: the full published manifest as served, whether the Pages chain fired on its own this time, and the update observation from the walkthrough below.

    Change no tracked file in this task.
  </action>
  <acceptance_criteria>
    - `gh release list` output contains both `v0.9.0.0` and `v1.0.0.0`.
    - `gh run list --workflow=release.yml --limit 5` shows a `success` conclusion for the `v1.0.0.0` run, and `gh run list --workflow=pages.yml --limit 5` shows a `success` conclusion for a run started after it.
    - `curl -fsSL https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json | jq -e 'length == 1 and (.[0].versions | map(.version)) == ["0.9.0.0", "1.0.0.0"]'` succeeds — both versions, in ascending order, and `0.9.0.0` was not dropped.
    - Each published entry's `changelog` compares byte-identical to its own version's section body extracted from `CHANGELOG.md`.
    - The published `0.9.0.0` entry is identical to the one `06-04-SUMMARY.md` recorded, field for field.
    - `scripts/manifest.sh verify https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json` exits 0 and reports 2 version entries.
    - `git status --porcelain` is empty except for planning artifacts.
  </acceptance_criteria>
  <verify>
    <automated>gh release list | rg -q 'v1\.0\.0\.0' &amp;&amp; gh release list | rg -q 'v0\.9\.0\.0'</automated>
    <fails_when>non-zero exit — either release is missing, which for `v1.0.0.0` means the release workflow refused before publishing and for `v0.9.0.0` means a release was deleted, which the phase's keep-every-version rule forbids</fails_when>
    <automated>curl -fsSL https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json | jq -e 'length == 1 and (.[0].versions | map(.version)) == ["0.9.0.0", "1.0.0.0"]'</automated>
    <fails_when>non-zero exit — the URL returns nothing or a non-2xx status (the Pages deploy for this release has not happened), the body does not parse, the document lists fewer than both versions, or the two entries are in the wrong order</fails_when>
    <automated>scripts/manifest.sh verify https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json | rg -q '2'</automated>
    <fails_when>non-zero exit, or the success line does not report two version entries — a checksum mismatch against either release's zip, or a document holding fewer entries than the two releases that exist</fails_when>
    <human-check>On the same clean Jellyfin 12.1 server that installed `0.9.0.0` from the catalog during the rehearsal — not a fresh one — open Dashboard, Plugins. Confirm the server offers the update to `1.0.0.0`. Take it, restart, and confirm the plugin loads and reports `1.0.0.0`. Then, on a second genuinely fresh server if one is available, add the manifest URL and confirm `1.0.0.0` is what a first-time installer is offered. Record whether the update appeared without any manual prompting and how long after the Pages deploy it took to show. This is roadmap criterion 5 and the half of PUB-04 no local test can cover.</human-check>
  </verify>
  <done>Both releases exist, the manifest lists `0.9.0.0` and `1.0.0.0` in ascending order with each carrying its own frozen changelog, an independent `verify` run against the live URL reports two checked entries, and the server that installed the first version has received and applied the update to the second.</done>
  <reversibility rating="reversible">This task reads and records; it changes no tracked file and publishes nothing.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a checkpoint answer → an irreversible publication | The gap between "the maintainer replied" and "the maintainer consented to publish" is where an agent could get this catastrophically wrong. |
| the rebuild → the already-published `0.9.0.0` entry | A republish rewrites the whole document. Everything an administrator already relies on is re-derived every time. |
| `06-04-SUMMARY.md` → `CHANGELOG.md` → the catalog | A record of what a walkthrough found becomes the text a stranger reads before installing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Repudiation | consent to publish `v1.0.0.0` | critical | mitigate | The merge and the tag push are performed by the maintainer personally. The checkpoint carries `gate="blocking-human"`, so it is never auto-approved in any mode, and its resume signal states that no reply causes either command to be run. A prohibition forbids scripting, scheduling or suggesting an agent-run path for either. |
| T-06-15 | Denial of Service | the `0.9.0.0` entry in the republished manifest | high | mitigate | An acceptance criterion asserts the published `versions` array equals both versions in ascending order, and a second asserts the `0.9.0.0` entry is field-for-field identical to what `06-04-SUMMARY.md` recorded. A rebuild that dropped it fails both. A prohibition forbids removing, hiding or filtering it. |
| T-06-16 | Tampering | a later edit to a shipped version's changelog | medium | mitigate | A comment-stripped diff check asserts the commit deleted no line of the shipped `0.9.0.0` section, and Task 2 compares the published `0.9.0.0` entry against both the file and the previously-recorded served value. The entry itself is frozen at package time, so the repository and the catalog are checked against each other rather than one being trusted. |
| T-06-12 | Repudiation | the `1.0.0.0` changelog | high | mitigate | A prohibition forbids inventing content or moving content out of `0.9.0.0`'s section, and an acceptance criterion requires every factual claim in the section to trace to `06-04-SUMMARY.md`. |
| T-06-02 | Tampering | the published manifest's checksums | high | mitigate | `scripts/manifest.sh verify` runs against the live URL twice — once in the deploy job, once from outside GitHub's network here — recomputing the MD5 of both releases' zips against the checksums the document records. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan runs no package-manager install and adds no dependency. RESEARCH.md's `## Package Legitimacy Audit` records no `[ASSUMED]`, `[SUS]` or `[SLOP]` package for this phase, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
New in this plan, so a drift check should not expect these to already exist:

- `## [1.0.0.0] - <YYYY-MM-DD>` — a new heading in `CHANGELOG.md`, above the shipped `## [0.9.0.0]` section.
- The tag `v1.0.0.0` and the GitHub release it creates — pushed by the maintainer, not by this plan.
- A two-entry manifest at `https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json`.
- `EmbyAuth_1.0.0.0` — the plugin bind-mount target in `e2e/compose.yaml`, restored to the name it held before plan 06-04.

Not produced by this plan, deliberately: any agent-run `git push`, `git tag`, `gh release create`, or merge to the default branch; any edit to the shipped `## [0.9.0.0]` section; any change to `README.md`, `CLAUDE.md`, `docs/development.md`, `scripts/`, `tests/` or `.github/workflows/`; and any deletion of the `0.9.0.0` release or tag.
</artifacts_this_phase_produces>

<verification>
- `mise run test`, `mise run e2e` and `mise run lint` are all green on the version-and-changelog commit, before it is merged.
- `prek run` is green before the commit.
- After the tag push: both releases exist, the release workflow run concluded `success`, a Pages run concluded `success`, the live manifest holds both versions in ascending order each carrying its own changelog, and `scripts/manifest.sh verify` against the live URL reports two checked entries.
- The update has been taken on the same server that installed `0.9.0.0`, and the plugin reports `1.0.0.0` after the restart.
</verification>

<success_criteria>
- Version `1.0.0.0` is tagged and published through the release workflow, in the `v<version>` form `scripts/package.sh check-tag` requires.
- The manifest lists both versions, `0.9.0.0` unchanged and still installable.
- The same server that installed `0.9.0.0` from the catalog received the update to `1.0.0.0`.
</success_criteria>

<output>
Create `.planning/phases/06-catalog-install-and-first-public-release/06-05-SUMMARY.md` when done.

Record in it: the full published manifest as served, whether the Pages chain fired on its own for this release, how the update presented itself on the server that already had `0.9.0.0` and how long after the deploy it appeared, and whether the published `0.9.0.0` entry was identical to the one recorded after the rehearsal. The milestone close reads the last of these — it is the evidence that the keep-every-version decision holds in practice and not only in the script.
</output>
