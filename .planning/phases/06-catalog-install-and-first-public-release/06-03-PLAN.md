---
phase: 06-catalog-install-and-first-public-release
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [DOCS-02, DOCS-04]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`README.md` gives the manifest URL `https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json` and the steps an administrator follows in the Jellyfin dashboard to add it as a plugin repository, install the plugin from the catalog, and take an update (DOCS-02)"
    - "The README's catalog route is the first install route offered; the existing download-and-unzip route stays as the alternative, because it is the only route for a server that cannot reach GitHub Pages"
    - "The README's Status line no longer says the plugin is not published to a plugin repository, and every remaining sentence about what has been tested still matches what was actually tested (the plugin never claims behavior it has not verified)"
    - "The catalog section points at the existing Compatibility section rather than restating it, so the tested-versions claim has one home"
    - "The Jellyfin version bump rule in `CLAUDE.md` names every pin a bump touches, including the `Jellyfin.Controller` reference in `tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj` and the `targetAbi` assertions in `tests/scripts/package.bats` (DOCS-04)"
    - "The README's catalog steps add the repository once and then install; the update steps do not instruct the administrator to add the repository a second time, so following the document twice leaves one repository entry rather than two"
    - "The bump rule lists the `Jellyfin.Controller` reference in the plugin project and the one in the test project as two separate entries even though both currently hold the same version, because they are two files that can drift apart"
    - "Every entry in the bump rule names a file plus the identifier inside it, so no entry is a bare version number a reader would have to go looking for"
    - "Not applicable — DOCS-04 performs no length, equality, or normalization comparison on any text. The bump rule is ASCII prose naming file paths and package identifiers, read by a human and greppable by `rg`; no code compares it to anything, so no byte, code-point, grapheme-cluster, or normalization-form question arises"
    - "The bump rule lists the pins in the order a bump touches them: the two package references in the plugin project, the SQLite pin, the Jellyfin image tag, the target framework, then the test project's reference and the `targetAbi` assertions"
    - statement: "An administrator who interrupts the catalog install part-way through and repeats the README's steps ends with exactly one installed plugin version and no half-written plugin folder"
      verification: backstop
  artifacts:
    - path: "README.md"
      provides: "the manifest URL and the catalog install and update steps"
      contains: "github.io"
    - path: "CLAUDE.md"
      provides: "the completed Jellyfin version bump pin list"
      contains: "package.bats"
  key_links:
    - from: "README.md"
      to: ".github/workflows/pages.yml"
      via: "the manifest URL the README gives is the URL that workflow publishes to"
      pattern: "manifest.json"
    - from: "CLAUDE.md"
      to: "tests/scripts/package.bats"
      via: "the bump rule names the hardcoded targetAbi assertions a Jellyfin bump must update"
      pattern: "targetAbi"
  prohibitions:
    - requirement_id: DOCS-02
      category: transparency
      verification: judgment
      statement: "MUST NOT state or imply that the plugin has been run on a production Jellyfin server, that any Jellyfin version other than the tested image is supported, or that the catalog route has been exercised against a server it has not been exercised against. The plugin never claims behavior it has not verified, and an install page is exactly where a reader forms their expectation of what was tested."
    - requirement_id: DOCS-02
      category: safety
      verification: judgment
      statement: "MUST NOT give a manifest URL that is not the canonical one this repository publishes. An administrator pastes this string into a server that will download and execute whatever it names, without credentials and with no second check beyond an MD5 the same document supplies. A near-miss URL in a README is a supply-chain hand-off, not a typo."
---

<objective>
Give an administrator the one string they need and the steps that use it, and finish the pin list a Jellyfin version bump has to walk.

Purpose: DOCS-02 and DOCS-04 are the two documentation halves of this phase, and they are independent of every script and workflow in it. The README gains the catalog route because the whole point of publishing a manifest is that nobody has to download a zip by hand. The `CLAUDE.md` bump rule gains its two missing entries because a Jellyfin bump that updates four of six pins produces a build that compiles and a test suite that asserts the old ABI.

Output: a rewritten Install section and a corrected Status line in `README.md`, and a completed version bump rule in `CLAUDE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-catalog-install-and-first-public-release/06-CONTEXT.md
@.planning/phases/06-catalog-install-and-first-public-release/06-RESEARCH.md
@.planning/phases/06-catalog-install-and-first-public-release/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The manifest URL and the catalog install and update steps in the README</name>
  <files>README.md</files>
  <read_first>
    - `README.md` — the whole file. Line 10 is the Status line whose publication clause stops being true; lines 12-16 are Requirements; lines 18-24 are the Compatibility section Phase 5 wrote, which the new steps point at rather than restate; lines 26-41 are the Install section this task rewrites, including the paragraph at line 41 about the per-release manifest asset and unauthenticated downloads; lines 43-58 are Configure and Migrate users, which the catalog route must hand off to unchanged.
    - `e2e/85-catalog-install.bats` — the file plan 06-01 wrote. The sequence it drives through Jellyfin's API is the same sequence a human performs in the dashboard; read it so the written steps describe the flow that was actually proven rather than a remembered one.
    - `.planning/phases/06-catalog-install-and-first-public-release/06-01-SUMMARY.md` — what the end-to-end run actually observed, including the plugin folder name Jellyfin created and the response casing.
    - `.planning/phases/06-catalog-install-and-first-public-release/06-CONTEXT.md` — the Claude's Discretion entry covering the wording and placement of these steps, and the canonical-references note that the plugin never claims behavior it has not verified.
    - `.planning/phases/06-catalog-install-and-first-public-release/06-RESEARCH.md` — the confirmed GitHub Pages project-site URL shape and the administrator-facing dashboard path.
    - `~/.claude/CLAUDE.md` §Writing — write the end state, not the route to it; do not hard-wrap prose at a fixed column.
  </read_first>
  <action>
    Rewrite the Install section so the catalog route comes first and the manual route stays as the alternative.

    The catalog route is a numbered procedure. Open the Jellyfin dashboard at Plugins, then Repositories, then Add Repository. Give the repository a name of the administrator's choosing and the URL `https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json`, exactly and in a code span so it can be copied without picking up punctuation. Save. Open the Catalog tab, find **Emby Auth**, and install it. Restart Jellyfin. Then hand off to the existing Configure section by name, the way the current Install section's ending hands off today.

    Add a short Update subsection under the same heading: once the repository is added, Jellyfin's own plugin page offers each newer version the manifest lists, and installing it and restarting is the whole update. Say explicitly that the repository is added once and is not added again to take an update — an administrator who repeats the add step ends with two entries pointing at the same document, and the fix is to remove one, not to live with it.

    Keep the manual download-and-unzip route, under its own subheading, with its existing numbered steps and its `mise install` / `mise run package` build alternative unchanged. Say in one sentence why it is still here: it is the route for a server that cannot reach GitHub Pages. Keep the existing sentence that Jellyfin downloads a repository manifest and the zip without GitHub credentials, and make it apply to the catalog route it now explains rather than to the per-release asset it explains today.

    Point the catalog route at the Compatibility section by link, for the tested Jellyfin and Emby versions and for what `targetAbi` does and does not promise. Do not restate any of it. Phase 5 wrote that section precisely so the tested-versions claim has one home, and a second copy in the Install section is the drift this repository's own rules warn about.

    Edit the Status line at line 10: remove the clause saying the plugin is not published to a plugin repository, which the Install section now answers directly, and leave the two claims about what was tested exactly as they read today. Do not add a claim that the plugin has been installed on any production server, and do not add a version-support statement of any kind — the Compatibility section's framing of `targetAbi` as a floor with no ceiling is the only correct statement this repository has about untested Jellyfin versions.

    Keep the README short. Everything that is not a step or the URL belongs in `docs/`.

    Write each paragraph as one line. Do not hard-wrap.

    Batch every change to this file into as few edits as possible.
  </action>
  <acceptance_criteria>
    - `rg -q 'https://tkensiski\.github\.io/jellyfin-plugin-emby-auth/manifest\.json' README.md` matches — the canonical manifest URL is present, exactly.
    - `rg -c 'github\.io' README.md` returns exactly 1 — one URL, so there is no near-miss second copy that could drift.
    - `rg -q 'Repositories' README.md` matches and `rg -q 'Catalog' README.md` matches — the dashboard path an administrator follows is named.
    - `rg -q 'Not published to a plugin repository' README.md; test $? -eq 1` — the clause that stops being true is gone.
    - `rg -q 'Update' README.md` matches and the Update text does not instruct the reader to add the repository again; the words describing the add step appear only in the install procedure.
    - `rg -q '#compatibility' README.md` matches at least twice — the Compatibility section is linked from the Status line and from the catalog route rather than restated.
    - `rg -q 'production' README.md` matches only inside the existing "Not tested on a production server" sentence; no new sentence claims a production install.
    - `rg -q 'mise run package' README.md` matches — the manual build route survived the rewrite.
    - `mise run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rg -q 'https://tkensiski\.github\.io/jellyfin-plugin-emby-auth/manifest\.json' README.md &amp;&amp; [ "$(rg -c 'github\.io' README.md)" = "1" ]</automated>
    <fails_when>non-zero exit — the canonical manifest URL is absent, misspelled, or present more than once, and a second near-miss copy is exactly the hand-off this section exists to make safe</fails_when>
    <automated>rg -q 'Repositories' README.md &amp;&amp; rg -q 'Catalog' README.md &amp;&amp; rg -q 'mise run package' README.md</automated>
    <fails_when>non-zero exit — the dashboard steps or the manual build alternative went missing from the rewrite</fails_when>
    <automated>! rg -q 'Not published to a plugin repository' README.md</automated>
    <fails_when>non-zero exit — the Status line still carries the publication clause the Install section now contradicts</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit from any of the six lint steps</fails_when>
    <human-check>Read `README.md` end to end as a Jellyfin administrator who has never seen this repository. Confirm the manifest URL is the one the Pages workflow publishes to, that the catalog steps match the Jellyfin 12.1 dashboard's actual labels, that the update steps never ask for the repository to be added twice, and that no sentence claims the plugin has run anywhere other than the two container images Compatibility names. DOCS-02 is a prose requirement; this reading is its verification, the same way DOCS-01, DOCS-03 and DOCS-05 shipped.</human-check>
  </verify>
  <done>`README.md` leads its Install section with the catalog route, gives the canonical manifest URL exactly once, states the add-once-then-update flow, keeps the manual route as the named alternative for a server that cannot reach GitHub Pages, links rather than restates the Compatibility claims, and no longer says the plugin is unpublished.</done>
  <reversibility rating="reversible">Documentation edits to one file, revertable in one commit. Nothing outside the repository reads the README.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The Jellyfin version bump rule names every pin</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` — the whole file. The `## Rules` list, and specifically the bullet beginning "A Jellyfin version bump changes four pins together", which is the sentence this task completes.
    - `src/Jellyfin.Plugin.EmbyAuth/Jellyfin.Plugin.EmbyAuth.csproj` — the `Jellyfin.Controller`, `Jellyfin.Model` and `Microsoft.Data.Sqlite.Core` package references and their current versions, so the rule names them as they actually appear.
    - `tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj` — the `Jellyfin.Controller` reference at line 9, the first of the two missing pins.
    - `tests/scripts/package.bats` — the two assertions on the `targetAbi` value, the second of the two missing pins. Plan 06-01 added tests to this file, so find the assertions by reading them rather than by the line numbers CONTEXT.md recorded.
    - `Directory.Build.props` — the target framework, which the rule already names.
    - `e2e/compose.yaml` — the `jellyfin/jellyfin` image tag, which the rule already names.
    - `.planning/phases/06-catalog-install-and-first-public-release/06-CONTEXT.md` — the Claude's Discretion entry covering the exact pin list wording.
    - `~/.claude/CLAUDE.md` §Writing — do not hard-wrap prose at a fixed column.
  </read_first>
  <action>
    Rewrite the version bump bullet so it names every pin a Jellyfin bump touches, and correct the count at the start of the sentence to match what it now lists.

    List the pins in the order a bump walks them: the `Jellyfin.Controller` and `Jellyfin.Model` package references in `src/Jellyfin.Plugin.EmbyAuth/Jellyfin.Plugin.EmbyAuth.csproj`; `Microsoft.Data.Sqlite.Core` in the same project; the `jellyfin/jellyfin` image tag in `e2e/compose.yaml`; the target framework in `Directory.Build.props`; the `Jellyfin.Controller` package reference in `tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj`; and the two hardcoded `targetAbi` assertions in `tests/scripts/package.bats`.

    Give the test project's `Jellyfin.Controller` reference its own entry even though it currently holds the same version as the plugin project's. They are two package references in two files, and the whole reason this rule exists is that a bump that updates one and not the other produces a build that compiles against one ABI and a test suite that asserts the other.

    Name a file for every entry. An entry that is a bare package name and version leaves the reader to find it, which is what the incomplete rule already did for the two missing pins.

    Say in the same bullet what connects the `targetAbi` assertions to the `Jellyfin.Controller` pin: `scripts/package.sh` derives the `targetAbi` value from the plugin project's `Jellyfin.Controller` version with a fourth part appended, so the two `package.bats` assertions hold the derived value and go red on a bump. That sentence is why they are pins rather than tests that happen to mention a version.

    Keep the existing sentence about `Microsoft.Data.Sqlite.Core` having to equal the version inside the pinned image, and why a higher version stops the plugin loading. It is the one entry with a constraint rather than just a location.

    Write the bullet as one line. Do not hard-wrap. Keep the bullet in its current position in the `## Rules` list.
  </action>
  <acceptance_criteria>
    - `rg -q 'Jellyfin.Plugin.EmbyAuth.Tests.csproj' CLAUDE.md` matches — the test project's package reference is named by file.
    - `rg -q 'package\.bats' CLAUDE.md` matches and `rg -q 'targetAbi' CLAUDE.md` matches — the two assertions are named.
    - `rg -q 'four pins' CLAUDE.md; test $? -eq 1` — the count at the start of the sentence was corrected rather than left describing the old list.
    - `rg -q 'Jellyfin\.Model' CLAUDE.md` matches, `rg -q 'Microsoft\.Data\.Sqlite\.Core' CLAUDE.md` matches, `rg -q 'compose\.yaml' CLAUDE.md` matches, and `rg -q 'Directory\.Build\.props|target framework' CLAUDE.md` matches — none of the four original entries was lost in the rewrite.
    - `rg -c 'Jellyfin\.Controller' CLAUDE.md` returns at least 2 — the plugin project's reference and the test project's are two entries, not one.
    - `rg -q 'jellyfin\.deps\.json' CLAUDE.md` matches — the SQLite version constraint survived the rewrite.
    - `mise run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rg -q 'Jellyfin.Plugin.EmbyAuth.Tests.csproj' CLAUDE.md &amp;&amp; rg -q 'package\.bats' CLAUDE.md &amp;&amp; [ "$(rg -c 'Jellyfin\.Controller' CLAUDE.md)" -ge 2 ]</automated>
    <fails_when>non-zero exit — one of the two previously-missing pins is absent, or the two `Jellyfin.Controller` references were collapsed into a single entry that a bump could satisfy by updating only one file</fails_when>
    <automated>rg -q 'Jellyfin\.Model' CLAUDE.md &amp;&amp; rg -q 'Microsoft\.Data\.Sqlite\.Core' CLAUDE.md &amp;&amp; rg -q 'jellyfin\.deps\.json' CLAUDE.md &amp;&amp; ! rg -q 'four pins' CLAUDE.md</automated>
    <fails_when>non-zero exit — an original entry or the SQLite constraint was dropped in the rewrite, or the stale count of four is still in the sentence</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit from any of the six lint steps</fails_when>
    <human-check>Read the bump rule in `CLAUDE.md` and walk it against the repository, opening each named file. Confirm every pin a Jellyfin bump would have to change is in the list, that each entry names a file you can find, and that nothing in the list has moved since it was written. DOCS-04 is a prose requirement; this walk is its verification, the same way DOCS-01, DOCS-03 and DOCS-05 shipped.</human-check>
  </verify>
  <done>The Jellyfin version bump rule in `CLAUDE.md` names every pin with the file it lives in, lists the plugin project's and the test project's `Jellyfin.Controller` references separately, names the two `targetAbi` assertions in `tests/scripts/package.bats` and explains what derives their value, keeps the `Microsoft.Data.Sqlite.Core` constraint, and states a count that matches what it lists.</done>
  <reversibility rating="reversible">One bullet in one file, revertable in one commit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `README.md` → an administrator's Jellyfin server | A URL a reader copies into a server that will fetch and execute whatever it names, unauthenticated. |
| `CLAUDE.md` → a future Jellyfin bump | An instruction list a later contributor follows. An incomplete list produces a build that compiles against one ABI and asserts another. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Spoofing | the manifest URL in `README.md` | high | mitigate | The README carries the canonical `https://` project-site URL exactly once, asserted by an exact-string check and a count of one, so a near-miss second copy cannot be added silently. GitHub Pages enforces HTTPS unconditionally for `*.github.io` URLs and no custom domain is in scope for this phase. |
| T-06-11 | Tampering | a Jellyfin bump that updates some pins and not others | medium | mitigate | The bump rule names every pin with its file, lists the two `Jellyfin.Controller` references separately, and names the derived `targetAbi` assertions. `tests/scripts/package.bats` fails on a partial bump, so the rule has a machine backstop rather than only a reader. |
| T-06-12 | Repudiation | a README claim about what was tested | medium | mitigate | No new sentence claims a production install or support for any Jellyfin version beyond the tested image; the catalog route links to the Compatibility section rather than restating it, so there is one statement of what was tested and it is the one Phase 5 wrote against measured evidence. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan runs no package-manager install and adds no dependency of any kind. RESEARCH.md's `## Package Legitimacy Audit` records no `[ASSUMED]`, `[SUS]` or `[SLOP]` package for this phase, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
New in this plan, so a drift check should not expect these to already exist:

- The manifest URL string `https://tkensiski.github.io/jellyfin-plugin-emby-auth/manifest.json` as README content. The URL itself is published by `.github/workflows/pages.yml`, which plan 06-02 creates; it is live only after the maintainer enables Pages and the first release exists.
- A catalog install procedure and an Update subsection in `README.md`'s Install section.
- Two new entries in `CLAUDE.md`'s Jellyfin version bump rule, naming `tests/Jellyfin.Plugin.EmbyAuth.Tests/Jellyfin.Plugin.EmbyAuth.Tests.csproj` and the `targetAbi` assertions in `tests/scripts/package.bats`.

Not produced by this plan, deliberately: any change to the Status line's two testing claims, any new statement about supported Jellyfin versions, any change to the Compatibility section Phase 5 wrote, and any change to a file under `docs/`. `docs/development.md` is plan 06-02's.
</artifacts_this_phase_produces>

<verification>
- `mise run lint` is green.
- `mise run test` is green — this plan changes no code, so a failure there would mean something unintended moved.
- `prek run` is green before each commit.
- The two `<human-check>` readings are harvested at end of phase, per the repository's precedent for prose requirements.
</verification>

<success_criteria>
- An administrator with a Jellyfin 12.1 server can install and update this plugin using only what the README says, with no zip download and no build.
- A contributor bumping Jellyfin can find every pin from the rule alone, and the test suite catches them if they miss one.
</success_criteria>

<output>
Create `.planning/phases/06-catalog-install-and-first-public-release/06-03-SUMMARY.md` when done.

Record in it: the exact dashboard labels used for the catalog steps and whether they were confirmed against a running Jellyfin 12.1 or taken from research, and the final pin count the `CLAUDE.md` rule states. Plan 06-04's browser walkthrough reads the first; plan 06-04's version change reads the second.
</output>
