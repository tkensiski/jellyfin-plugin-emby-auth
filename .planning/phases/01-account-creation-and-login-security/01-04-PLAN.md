---
phase: 01-account-creation-and-login-security
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - docs/settings.md
  - docs/how-it-works.md
  - CHANGELOG.md
  - docs/images/settings-page.png
autonomous: false
requirements: [AUTH-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A search of src/, tests/, e2e/, docs/, and README.md finds no JellyfinPasswordFirst and no 'Check the saved Jellyfin password first' (AUTH-02, ROADMAP criterion 4)."
    - "docs/settings.md lists exactly two migration-behavior choices, and both say Emby decides the login."
    - "docs/how-it-works.md lists six login steps, numbered 1 to 6 with no gap, and the account-creation-window text plan 01-02 added is still present and attached to the right step."
    - "CHANGELOG.md records that an install whose settings file still holds the removed migration-behavior value loses its Emby server URL and Emby API key, and must have them re-entered (D-07)."
    - "docs/images/settings-page.png shows the settings page as it is after this phase — the migration-behavior help text in the image matches the text in Configuration/configPage.html."
  artifacts:
    - path: "docs/settings.md"
      provides: "The migration-behavior table with two rows"
      contains: "KeepEmbyInCharge"
    - path: "docs/how-it-works.md"
      provides: "Six login steps and the account-creation-window description"
      contains: "Emby user list"
    - path: "CHANGELOG.md"
      provides: "The D-07 settings-loss notice"
      contains: "Emby API key"
    - path: "docs/images/settings-page.png"
      provides: "A screenshot of the settings page that matches the shipped page"
  key_links:
    - from: "docs/settings.md"
      to: "src/Jellyfin.Plugin.EmbyAuth/Configuration/PluginConfiguration.cs"
      via: "the API value column must name only enum members that still exist"
      pattern: "MoveAfterFirstLogin|KeepEmbyInCharge"
    - from: "docs/images/settings-page.png"
      to: "src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html"
      via: "the field description rendered in the image is the string in the page source; plan 01-03 changed it, so the image is stale until retaken"
      pattern: "fieldDescription"
  prohibitions:
    - statement: "MUST NOT leave documentation that describes a behavior the plugin no longer has — an administrator who reads a removed option as available would plan a migration around a setting that does not exist."
      status: resolved
      verification: test
    - statement: "MUST NOT ship a settings-page screenshot whose visible text contradicts the shipped page."
      status: resolved
      verification: judgment
---

<objective>
Finish AUTH-02 in the documentation, record the settings-loss consequence, and refresh the settings-page screenshot.

Purpose: plans 01-01 through 01-03 removed the saved-password migration behavior from the code, the settings page, the unit tests, and the end-to-end tests. `docs/settings.md` still lists it as a choice and `docs/how-it-works.md` still describes it as login step 2, so ROADMAP criterion 4 is not yet met. D-07 also requires a `CHANGELOG.md` statement of what an install holding the removed value loses, and `CLAUDE.md` requires a new screenshot whenever the settings page changes — plan 01-03 changed a help string that is visible in the current image.

Output: a two-row migration-behavior table; six renumbered login steps; a new `CHANGELOG.md`; a refreshed `docs/images/settings-page.png`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-account-creation-and-login-security/01-CONTEXT.md
@.planning/phases/01-account-creation-and-login-security/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the migration behavior from the documentation and renumber the login steps</name>
  <files>docs/settings.md, docs/how-it-works.md</files>
  <read_first>
    - docs/settings.md — the file being modified; the migration-behavior table is around lines 27-33 and the row to delete is line 33.
    - docs/how-it-works.md — the file being modified, as changed by plan 01-02. Read the whole file. The step to delete is line 8; the steps to renumber are the ones after it; line 41 references a step in `migration.md`, not in this file, and must not change.
    - src/Jellyfin.Plugin.EmbyAuth/Configuration/PluginConfiguration.cs — as changed by plan 01-03; the two surviving `MigrationMode` members and their XML doc summaries are the source of truth for the table wording.
    - src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html — as changed by plan 01-03; the two surviving option labels must match the table's first column exactly.
    - .planning/phases/01-account-creation-and-login-security/01-02-SUMMARY.md — records where the account-creation-window text was placed, which decides whether renumbering moves it.
    - docs/migration.md — read only, to confirm nothing there references a login-step number in `how-it-works.md`.
  </read_first>
  <action>
    Bring the documentation into line with the shipped behavior. Keep the register of the surrounding prose — short sentences, present tense, no marketing words.

    **`docs/settings.md`.** Delete the third row of the migration-behavior table, the one whose API-value column names the removed enum member. Leave the two remaining rows and the table header unchanged. Check the surrounding prose in that section for any sentence that counts the choices or refers to "the third choice"; if one exists, correct it to describe two choices.

    **`docs/how-it-works.md`.** Delete numbered login step 2 in full — the step about the saved password. Renumber the steps that followed it so the list runs 1 to 6 with no gap: the Emby user list becomes step 2, the Emby check step 3, the account rules step 4, the password copy step 5, and the move to Default step 6. Keep every step's bold title and body text otherwise unchanged.

    The account-creation-window text that plan 01-02 added stays. If it was written as a sub-point of the account-rules step, it moves with that step to its new number; if it was written as its own subsection after the list, it does not move. Either way its content is unchanged by this task — do not soften it, and do not add a claim that the window is eliminated.

    Do not touch `README.md`: the grep this session found no occurrence of the removed value or its label there, and `CLAUDE.md` says to keep the README short.
  </action>
  <verify>
    <automated>! rg -n 'JellyfinPasswordFirst|Check the saved Jellyfin password first' src tests e2e docs README.md --glob '!**/obj/**' --glob '!**/bin/**'</automated>
    <fails_when>non-zero exit — rg printed at least one file:line match, so ROADMAP criterion 4 is not met</fails_when>
    <automated>[ "$(rg -c '^[0-9]\. \*\*' docs/how-it-works.md)" = "6" ]</automated>
    <fails_when>non-zero exit — the login-step list does not have exactly six numbered bold steps</fails_when>
    <automated>[ "$(rg -o '^[0-9]+\. \*\*' docs/how-it-works.md | grep -o '^[0-9]*' | tr -d '\n')" = "123456" ]</automated>
    <fails_when>non-zero exit — the step numbers are not the contiguous sequence 1 2 3 4 5 6, so the renumber left a gap or a duplicate</fails_when>
    <automated>[ "$(rg -c '^\| .* \| \`[A-Za-z]+\` \|' docs/settings.md)" = "5" ]</automated>
    <fails_when>non-zero exit — the two settings tables do not hold two migration-behavior rows plus three account-access rows</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit; a linter reports an error in the changed files</fails_when>
  </verify>
  <acceptance_criteria>
    - Docs: `! rg -n 'JellyfinPasswordFirst|Check the saved Jellyfin password first' src tests e2e docs README.md --glob '!**/obj/**' --glob '!**/bin/**'` succeeds — the ROADMAP criterion 4 search over all five trees finds nothing.
    - Docs: `docs/settings.md` migration-behavior table has two data rows, and their API-value column names only `MoveAfterFirstLogin` and `KeepEmbyInCharge`.
    - Docs: `docs/how-it-works.md` login steps are numbered 1 to 6 with no gap and no duplicate.
    - Docs: `rg -q 'CreateUserAsync' docs/how-it-works.md` still succeeds — the account-creation-window text plan 01-02 added survived the renumber.
    - Docs: `! rg -q -i 'eliminat|closes this window|prevents this window|cannot happen' docs/how-it-works.md` still succeeds.
    - Docs: every migration-behavior label in `docs/settings.md` appears verbatim as an `<option>` label in `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html`.
    - CLI: `mise run lint` exits 0.
  </acceptance_criteria>
  <done>No tree in the repository describes the removed migration behavior. The login-step list is contiguous and the account-creation-window text is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md with the settings-loss notice, and start the demo environment for the screenshot</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/01-account-creation-and-login-security/01-CONTEXT.md — D-07 states exactly what an install holding the removed value loses and why, with the Jellyfin source reference.
    - .planning/ROADMAP.md — Phase 6 requirement REL-04 says each release gets a `CHANGELOG.md` entry whose text is also carried in the plugin manifest, so the file's shape must suit that use.
    - Directory.Build.props — the current version is the single source of truth for the version heading.
    - scripts/dev-env.sh — the `up` path, its printed credentials, and the demo Emby users it creates.
    - CLAUDE.md — "Keep `README.md` and `docs/` accurate when behavior changes" and "When the settings page changes, take a new screenshot".
  </read_first>
  <action>
    **Create `CHANGELOG.md` at the repository root.** The file does not exist yet — `ls CHANGELOG.md` returned nothing this session. Use Keep a Changelog headings so Phase 6's REL-04 can lift an entry into the plugin manifest without reformatting: a short title, then a version section for the unreleased work, then `### Changed` and `### Removed` groups.

    Record three things, in the repository's plain register:
    - Removed: the saved-password migration behavior. On the Emby login method Emby now checks every login, and the saved Jellyfin hash never accepts one.
    - Changed: a failure while creating or saving an account now refuses the login instead of returning a server error, and a failed cleanup delete is logged with the account name.
    - An upgrade note, marked clearly, stating that a server whose stored plugin settings still hold the removed migration-behavior value cannot load those settings. Jellyfin replaces the settings file with a default one, so the server loses its Emby server URL and its Emby API key and refuses every login on the Emby login method until an administrator enters them again in Dashboard > Plugins > Emby Auth. State that this affects only servers that had selected that behavior.

    Do not write a release date and do not invent a released version number — Phase 6 owns the release process. Do not put a password, an API key, or any real server URL in the file.

    **Start the demo environment.** Run `scripts/dev-env.sh up`. It publishes the plugin, brings up Emby and Jellyfin in Docker, completes both startup wizards, configures the plugin, and creates the demo Emby users. Then reproduce the state the current screenshot shows, using the credentials the script prints:
    1. Set the migration behavior to "Keep Emby in charge until the migration task runs" and the account access to "Jellyfin defaults, with Emby's remote access setting", then save.
    2. Log in to Jellyfin once as `bob` with `bob-demo-pass` and once as `carol` with `carol-demo-pass`, so both appear in the Migration list as ready. `dave` is already precreated on the Emby login method with a random Jellyfin password and appears as not ready.
    3. Confirm the Jellyfin URL and the settings page path from the script's own output.

    Leave the environment running for the next task, and put the Jellyfin URL and the settings page path in your task output so the checkpoint can quote them.
  </action>
  <verify>
    <automated>rg -q -i 'Emby API key' CHANGELOG.md</automated>
    <fails_when>non-zero exit — the upgrade note does not mention the API key an affected install loses</fails_when>
    <automated>rg -q -i 'Emby server URL' CHANGELOG.md</automated>
    <fails_when>non-zero exit — the upgrade note does not mention the server URL an affected install loses</fails_when>
    <automated>rg -q '^### Removed' CHANGELOG.md</automated>
    <fails_when>non-zero exit — the removal of the migration behavior is not recorded under its own heading</fails_when>
    <automated>scripts/dev-env.sh status</automated>
    <fails_when>non-zero exit, or the output does not show both the Emby and the Jellyfin container running</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit; a linter reports an error in the changed files</fails_when>
  </verify>
  <acceptance_criteria>
    - File: `CHANGELOG.md` exists at the repository root and is tracked by git after the commit — `git ls-files -- CHANGELOG.md` prints the path.
    - Docs: the file has a `### Removed` group naming the saved-password migration behavior and a `### Changed` group naming the account-creation failure handling.
    - Docs: the upgrade note states that an affected server loses its Emby server URL and its Emby API key and refuses Emby-method logins until an administrator re-enters them.
    - Docs: `! rg -q -i 'x-emby-token|api[_ ]key[:=] ?[A-Za-z0-9]' CHANGELOG.md` succeeds — no real key or key-looking value was written into the file.
    - CLI: `scripts/dev-env.sh status` exits 0 and reports both containers running.
    - Output: the task output names the Jellyfin URL and the settings page path taken from the script's own printed output, not guessed.
  </acceptance_criteria>
  <done>`CHANGELOG.md` records the removal, the failure-handling change, and the D-07 settings loss. The demo environment runs with the Migration list in the same state the current screenshot shows.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Refresh the settings-page screenshot</name>
  <action>Capture a new settings-page screenshot and save it over docs/images/settings-page.png</action>
  <instructions>
    I have already: published the plugin, started Emby and Jellyfin in Docker, completed both startup wizards, configured the plugin, set the migration behavior and account access to the values the current screenshot shows, and logged in `bob` and `carol` so the Migration list reports two users ready and one needing a login.

    The one step I cannot do is take the picture — this repository has no headless-browser tool, and the screenshot is a rendered browser view.

    Why it must be retaken rather than left alone: the current `docs/images/settings-page.png` renders the migration-behavior help text "With the second and third choice, users stay on the Emby login method until you run the migration." Plan 01-03 changed that string because there is no third choice any more, so the shipped image now contradicts the shipped page. `CLAUDE.md` requires a new screenshot when the settings page changes.

    1. Open the Jellyfin URL printed by `scripts/dev-env.sh up` in a browser and sign in with the admin credentials it printed.
    2. Go to Dashboard > Plugins > Emby Auth.
    3. Confirm the Migration behavior select shows "Keep Emby in charge until the migration task runs", the Account access select shows "Jellyfin defaults, with Emby's remote access setting", and the Migration section lists bob and carol as ready and dave as needing one login.
    4. Capture the same region the current image covers: from the "Emby server URL" label at the top down to the end of the "Run migration now" help text, at the same width, with the dark theme.
    5. Save it over `docs/images/settings-page.png`, replacing the file.

    Tell me when the file is saved and I will verify it and tear the environment down.
  </instructions>
  <verification>
    After you save the file I will check: `git status --porcelain docs/images/settings-page.png` reports the file as modified; the new file is a PNG whose pixel dimensions are within ten percent of the previous image's; and I will read the image back and confirm the visible migration-behavior help text matches the string in `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` and that no option for the removed behavior is visible. Then I will run `scripts/dev-env.sh down`, run `mise run lint`, and commit.
  </verification>
  <resume-signal>Type "saved" when the new screenshot is in place, or "skip" with your reason if you want to keep the current image.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols and files this phase creates or removes. A drift check must not flag the created ones as unacknowledged new symbols, nor the removed ones as unexplained deletions.

**Created by this plan (01-04):**
- `CHANGELOG.md` — new file at the repository root. No code symbols.

**Created by earlier plans in this phase:**
- `FakeUserManager`, `FakeCryptoProvider` in `tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs` (plan 01-01).
- `EmbyAuthenticationProviderTests` in `tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs` (plan 01-01).
- `EmbyAuthenticationProvider` constructor parameter `Func<PluginConfiguration?> configurationSource` (plan 01-01).
- `LogDeleteFailed` on `EmbyAuthenticationProvider` (plan 01-01).
- The account-creation-window subsection in `docs/how-it-works.md` (plan 01-02).

**Removed by earlier plans in this phase:**
- `MigrationMode.JellyfinPasswordFirst` enum member (plan 01-03). <!-- planner-discipline-allow: JellyfinPasswordFirst -->
- `EmbyAuthenticationProvider.SavedPasswordMatches` (plan 01-03). <!-- planner-discipline-allow: SavedPasswordMatches -->
- `EmbyAuthenticationProvider.LogSavedPasswordUnreadable` (plan 01-03). <!-- planner-discipline-allow: LogSavedPasswordUnreadable -->

**Removed by this plan:**
- One table row in `docs/settings.md` and one numbered login step in `docs/how-it-works.md`. No code symbols.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions

1. **The screenshot is stale, and that was measured, not assumed.** The current `docs/images/settings-page.png` was read this session and visibly renders the help string plan 01-03 changes. That is why this plan carries a `checkpoint:human-action` rather than leaving the image alone. If the maintainer decides the image is close enough, the checkpoint's "skip" path records that decision instead of silently shipping a stale image.

2. **Table-row counting in `docs/settings.md`.** The automated gate counts rows whose second column is a backticked API value. It assumes both tables keep that shape. If the account-access table is reformatted, adjust the expected count and say so in the SUMMARY rather than deleting the gate.

3. **`CHANGELOG.md` format and Phase 6.** This plan creates the file in Keep a Changelog shape so REL-04 in Phase 6 can lift an entry into the plugin manifest. Phase 6 has not been planned, so the exact field the manifest needs is not yet fixed. Re-shaping the file later is cheap; leaving D-07 unrecorded is not.

4. **The demo login state.** Reproducing "bob ready, carol ready, dave needs one login" assumes `scripts/dev-env.sh up` still precreates `dave` on the Emby login method and creates `alice`, `bob`, `carol`, `dave`, `erin` on Emby. Read from `scripts/dev-env.sh` this session. If the script has changed, match whatever state it produces and note the difference.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → the administrator | The documented behavior is what an operator plans a migration around; a stale option is a wrong plan. |
| `CHANGELOG.md` → the upgrading administrator | The only place that warns an operator their settings will be replaced on upgrade. |
| The demo environment → the repository | The screenshot is captured from a running server whose settings page shows an Emby API key field. |

## STRIDE Threat Register

ASVS enforcement level 1; blocking severity `high`. Derived from `01-RESEARCH.md` `## Security Domain`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-Stale | Repudiation | `docs/settings.md`, `docs/how-it-works.md`, `docs/images/settings-page.png` | medium | mitigate | Task 1's repo-wide search gate over `src/`, `tests/`, `e2e/`, `docs/`, and `README.md` fails the task if any reference survives. Task 3 replaces the screenshot whose visible help text plan 01-03 invalidated. An administrator who reads a removed option as available would plan a migration around a setting that does not exist. |
| T-01-Silent | Repudiation | the upgrade path for a server holding the removed value | medium | mitigate | `CHANGELOG.md` states plainly that the server loses its Emby URL and API key and refuses Emby-method logins until they are re-entered. Without that note the failure looks like a plugin bug and an operator could reinstall rather than re-enter the settings. |
| T-01-KeyLeak | Information Disclosure | the settings-page screenshot and `CHANGELOG.md` | high | mitigate | The Emby API key field renders as a masked password input on the settings page, which the current image already shows masked. The checkpoint instructions capture the same region, and the verification step reads the image back before it is committed. `CHANGELOG.md` carries a gate that no key-looking value was written into it. The demo environment's key belongs to a throwaway container that `scripts/dev-env.sh down` destroys. |
| T-01-Window | Repudiation | the account-creation-window text surviving the renumber | medium | mitigate | Task 1's acceptance criteria re-assert both the presence of the window text and the absence of an "eliminated" claim after the renumber, so a careless edit cannot quietly drop the AUTH-03 documentation that plan 01-02 added. |
| T-01-SC | Tampering | npm/pip/cargo/NuGet installs | n/a | accept | No package-manager install task in this phase; `01-RESEARCH.md` `## Package Legitimacy Audit` records zero new external packages. No legitimacy checkpoint is required. |

**Canon referral:** login-response user enumeration on the refusal path is canon OWASP surface — covered by `/gsd-secure-phase`; not minted as a prohibition here.
</threat_model>

<verification>
- `! rg -n 'JellyfinPasswordFirst|Check the saved Jellyfin password first' src tests e2e docs README.md --glob '!**/obj/**' --glob '!**/bin/**'` succeeds — ROADMAP criterion 4 over all five trees. <!-- planner-discipline-allow: JellyfinPasswordFirst --> <!-- planner-discipline-allow: Check the saved Jellyfin password first -->
- `docs/how-it-works.md` login steps are numbered 1 to 6 with no gap.
- `git ls-files -- CHANGELOG.md` prints the path.
- `git status --porcelain docs/images/settings-page.png` reports the file as modified before the commit, or the checkpoint's "skip" reason is recorded in the SUMMARY.
- `scripts/dev-env.sh down` exits 0 and removes the demo containers.
- `mise run lint` exits 0.
- `mise run test` exits 0.
- `prek run` exits 0 before each commit.
</verification>

<success_criteria>
- A search of `src/`, `tests/`, `e2e/`, `docs/`, and `README.md` finds no trace of the removed migration behavior (ROADMAP criterion 4, AUTH-02 complete).
- `CHANGELOG.md` exists and records the removal, the failure-handling change, and the settings loss D-07 accepted.
- `docs/images/settings-page.png` matches the shipped settings page, or the maintainer explicitly chose to keep the current image and the reason is recorded.
- The demo environment is torn down and the working tree holds no stray artifact from it.
</success_criteria>

<output>
Create `.planning/phases/01-account-creation-and-login-security/01-04-SUMMARY.md` when done.
</output>
