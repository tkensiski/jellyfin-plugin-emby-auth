---
phase: 01-account-creation-and-login-security
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs
  - docs/how-it-works.md
autonomous: true
requirements: [AUTH-03, AUTH-01, TEST-01]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new account receives the Emby-verified hash and EmbyAuthenticationProvider.ProviderId in the UpdateUserAsync call directly after CreateUserAsync — the recorded IUserManager call sequence for a first login is exactly CreateUserAsync then UpdateUserAsync, with no other call between (AUTH-03, D-01)."
    - "docs/how-it-works.md describes the brief moment between CreateUserAsync and that save, names the cause (Jellyfin has no create-with-password call), states that the account is on the Default login method with no password during it, and does not claim the window is eliminated (AUTH-03)."
    - "dotnet test runs unit tests for EmbyAuthenticationProvider covering the account checks, the Emby user-list check, the Emby login, account creation, and the existing-account save (TEST-01, ROADMAP criterion 5)."
    - "Every login that Emby accepts saves a fresh hash and records its fingerprint — a second login with the same password still sends the password to Emby and still calls UpdateUserAsync, with no equality short-circuit on the already-saved hash (AUTH-01, D-08; probe edge AUTH-01/adjacency)."
    - "A blank password is refused before any HTTP request reaches Emby, and a null resolvedUser takes the create-account path (probe edge AUTH-01/empty; probe edge TEST-01/empty)."
    - "The fingerprint is recorded only after the hash is saved: when UpdateUserAsync throws, no fingerprint is recorded for that user, so a later move to Default cannot accept a hash Emby never verified (AUTH-01, probe edge AUTH-01/ordering)."
    - "A second login for the same Emby user takes the existing-account path: CreateUserAsync is called exactly once across two logins (probe edge AUTH-03/idempotency)."
    - "A typed name that differs from the Emby user name only in case is accepted; a typed name that differs in any other way (including a trailing space) is refused before the password reaches Emby (probe edge TEST-01/adjacency)."
    - statement: "During the window between CreateUserAsync and the hash-setting UpdateUserAsync, a concurrent blank-password login on the Default login method could open the new account; this phase documents the residual race rather than closing it, because the public Jellyfin 12.1 IUserManager API has no create-with-password call."
      verification: backstop
  artifacts:
    - path: "tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs"
      provides: "Full TEST-01 coverage of EmbyAuthenticationProvider — account checks, Emby user-list check, Emby login, account creation and update"
      contains: "EmbyAuthenticationProvider.ProviderId"
    - path: "docs/how-it-works.md"
      provides: "The AUTH-03 description of the account-creation window"
      contains: "CreateUserAsync"
  key_links:
    - from: "tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs"
      to: "src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs"
      via: "a real EmbyVerifiedPasswords over a per-test temporary file, so the ordering truth (record only after the hash is saved) is observable"
      pattern: "verifiedPasswords|EmbyVerifiedPasswords"
    - from: "tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs"
      to: "src/Jellyfin.Plugin.EmbyAuth/LoginDecision.cs"
      via: "the typed-name vs Emby-name comparison that decides Deny / CreateAccount / UseAccount"
      pattern: "LoginAction|ProviderId"
  prohibitions:
    - statement: "MUST NOT describe the account-creation window as eliminated, closed, or prevented; the documentation states the residual risk plainly and names its cause."
      status: resolved
      verification: judgment
    - statement: "MUST NOT add a branch that skips the hash write or the fingerprint write when the saved hash already verifies the typed password — the write cost belongs to PERF-02 in Phase 4, which measures it first."
      status: resolved
      verification: test
---

<objective>
Finish TEST-01 coverage of `EmbyAuthenticationProvider`, prove AUTH-03's save-directly-after-create ordering, prove AUTH-01's "only Emby decides" behaviour at the unit level, and document the account-creation window.

Purpose: plan 01-01 built the test seam and closed the failure paths. Everything else the requirement list names for `EmbyAuthenticationProvider` is still untested: the account checks, the Emby user-list check, the Emby login, and the two happy paths. AUTH-03 also has a documentation half — `docs/how-it-works.md` must describe the brief moment when a new account exists with no password, because the plugin cannot eliminate it.

Output: the remaining unit tests in `EmbyAuthenticationProviderTests.cs`, and a new paragraph in `docs/how-it-works.md` describing the account-creation window.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-account-creation-and-login-security/01-CONTEXT.md
@.planning/phases/01-account-creation-and-login-security/01-RESEARCH.md
@.planning/phases/01-account-creation-and-login-security/01-PATTERNS.md
@.planning/phases/01-account-creation-and-login-security/01-01-SUMMARY.md
@.claude/rules/plugin.md
</context>

<tdd_discipline>
`workflow.tdd_mode` is enabled. Tasks 1 and 2 carry `tdd="true"`. These tasks add tests for behaviour that already works, so the RED step is different from plan 01-01's: after writing each test and seeing it green, **break the code once and watch the test fail**, then restore — the repository rule in `CLAUDE.md` names exactly this step. Record in the SUMMARY which line you broke for each group of tests and which tests went red. Commit the tests as `test(01-02): ...`; the restoring edit is not a separate commit because the tree returns to its pre-break state.

Task 3 is documentation and carries no `tdd` attribute.
</tdd_discipline>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover every refusal path of Authenticate</name>
  <files>tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs</files>
  <read_first>
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs — the file being modified, as created by plan 01-01; reuse its `CreateProvider` factory, its `PluginConfiguration` constant, and its stub-response helpers rather than adding parallel ones.
    - src/Jellyfin.Plugin.EmbyAuth/EmbyAuthenticationProvider.cs — the type under test; `Authenticate` lines 60-113 hold every refusal path in this task.
    - src/Jellyfin.Plugin.EmbyAuth/EmbyUserDirectory.cs — `GetStatusAsync` and the four `EmbyUserStatus` values, plus the 60-second cache and 30-second retry delay that a `ManualTimeProvider` holds still.
    - src/Jellyfin.Plugin.EmbyAuth/LoginDecision.cs — the `Deny` / `UseAccount` / `CreateAccount` rules.
    - src/Jellyfin.Plugin.EmbyAuth/EmbyAuthSettings.cs — the problem strings an invalid configuration produces.
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyUserDirectoryTests.cs — the `[Theory]`/`[InlineData]` shape and the stub JSON shapes to reuse.
    - .planning/codebase/TESTING.md — the `<Type>Tests` naming pattern and the one-test-file-per-tested-type rule.
  </read_first>
  <behavior>
    Add to `EmbyAuthenticationProviderTests`, each case asserting `await Assert.ThrowsAsync<AuthenticationException>(...)`:

    - `RefusesTheLogin_AndDoesNotContactEmby_WhenThePasswordIsBlank` — `[Theory]` over `""` and `null`. Also asserts `handler.Requests` is empty, so nothing reached Emby.
    - `RefusesTheLogin_AndDoesNotContactEmby_WhenTheAccountIsDisabled` — `resolvedUser` has `PermissionKind.IsDisabled` set; `handler.Requests` is empty.
    - `RefusesTheLogin_AndDoesNotContactEmby_WhenTheAccountIsAnAdministrator` — `resolvedUser` has `PermissionKind.IsAdministrator` set; `handler.Requests` is empty.
    - `RefusesTheLogin_WithTheSettingsProblem_WhenTheSettingsAreInvalid` — the settings delegate returns a `PluginConfiguration` with an empty `EmbyServerUrl`. Asserts the thrown message equals the `EmbyAuthSettings` problem string for a missing URL, not the generic invalid-login constant, and that `handler.Requests` is empty.
    - `DoesNotSendThePasswordToEmby_WhenNoEnabledEmbyUserHasTheTypedName` — `[Theory]` over three user-list stub shapes: the typed name is present but disabled, the typed name is absent, and the user-list request answers HTTP 500 (unavailable). Each asserts exactly one recorded request and that its URI path ends in `Users`, so the password never reached `Users/AuthenticateByName`.
    - `RefusesTheLogin_WhenEmbyRefusesThePassword` — the authenticate stub answers HTTP 401.
    - `RefusesTheLogin_WhenEmbyReturnsADifferentUserName` — the user list holds `alice`, the authenticate response names `bob`; `LoginDecision.Decide` returns `Deny`. Asserts `FakeUserManager.Calls` is empty — no account was created or written.
    - `RefusesTheLogin_WhenTheJellyfinAccountUsesAnotherLoginMethod` — `resolvedUser` exists with `AuthenticationProviderId` set to `DefaultLoginMethod.ProviderId`; asserts `FakeUserManager.Calls` is empty.
  </behavior>
  <action>
    Extend the existing test class — do not create a second test file for the same type (`.planning/codebase/TESTING.md`: one test file per tested type).

    Where a case needs a `resolvedUser`, build it the `AccountAccessPolicyTests.cs:12-18` way (`new User(name, providerId, resetProviderId)` then `AddDefaultPermissions()` and `AddDefaultPreferences()`), set the permission under test with the `User` permission API, and set `AuthenticationProviderId` explicitly to `EmbyAuthenticationProvider.ProviderId` unless the case is specifically about another login method.

    For the unavailable-user-list row, remember `EmbyUserDirectory` caches a failed read for 30 seconds against the clock; because `ManualTimeProvider` never advances on its own, one failed user-list response is enough and no second request is made.

    Prove each group with the break-then-restore step from `<tdd_discipline>`: for the account-check group break the blank-password guard at `EmbyAuthenticationProvider.cs:62`; for the directory group break the status comparison at `:89`; for the decision group break the `LoginAction.Deny` guard at `:99`. Confirm the expected tests go red, then restore the line exactly.

    Assert on `StubHttpMessageHandler.Requests` for every "did not reach Emby" claim. Do not assert on log text for these cases — `NullLogger` is the default in the shared factory, and only the plan 01-01 double-failure case needs `CapturingLogger`.
  </action>
  <verify>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx --filter "FullyQualifiedName~EmbyAuthenticationProviderTests"</automated>
    <fails_when>non-zero exit, or the summary prints "Zero tests ran"</fails_when>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx</automated>
    <fails_when>non-zero exit, or the summary line "failed: 0" is absent from the test run summary</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit; `dotnet format --verify-no-changes` prints a formatting diff, or an analyzer warning is reported as an error</fails_when>
  </verify>
  <acceptance_criteria>
    - Source: `rg -c 'public async Task' tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs` returns at least 12 — the four plan 01-01 failure-path tests plus the eight added here.
    - Source: `rg -q 'Requests\)' tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs` succeeds — at least one assertion reads the recorded request list.
    - Source: `rg -q 'DefaultLoginMethod.ProviderId' tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs` succeeds — the other-login-method refusal is covered.
    - Behavior: a login with a blank password throws `AuthenticationException` and the stub handler recorded zero requests.
    - Behavior: a login for a name that is not an enabled Emby user throws `AuthenticationException` and the stub handler recorded exactly one request, to the Emby user-list path.
    - Behavior: a login where Emby returns a different user name throws `AuthenticationException` and `FakeUserManager.Calls` is empty.
    - Test: `dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx` exits 0 and reports `failed: 0`.
    - Documented: the SUMMARY names, for each of the three groups, the line that was broken and which tests went red.
  </acceptance_criteria>
  <done>Every refusal path in `Authenticate` — blank password, disabled account, administrator, invalid settings, three Emby user-list statuses, an Emby refusal, a name mismatch, and another login method — has a unit test that was proved to fail when its guard was broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover account creation and the existing-account save, and prove the AUTH-03 ordering</name>
  <files>tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs</files>
  <read_first>
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs — the file being modified, as extended by Task 1.
    - src/Jellyfin.Plugin.EmbyAuth/EmbyAuthenticationProvider.cs — `Authenticate` lines 105-113, `CreateAccountAsync` 168-207, and `SavePasswordAsync` 209-225. The unconditional `verifiedPasswords.Record` call at line 111 is what the ordering test observes.
    - src/Jellyfin.Plugin.EmbyAuth/EmbyVerifiedPasswords.cs — `Record` and `Matches`, and the fact that the file holds a SHA-256 fingerprint rather than the hash.
    - src/Jellyfin.Plugin.EmbyAuth/AccountAccessPolicy.cs — `ApplyToNewAccount` and `ApplyToExistingAccount`, so the remote-access assertion targets the right permission.
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/AccountAccessPolicyTests.cs — the existing assertions for the same policy, to avoid duplicating them here.
    - .planning/phases/01-account-creation-and-login-security/01-CONTEXT.md — D-01 (create then save, nothing between), D-08 (no write-skipping branch).
  </read_first>
  <behavior>
    Add to `EmbyAuthenticationProviderTests`:

    - `CreatesTheAccount_WithTheEmbyVerifiedHashAndTheEmbyLoginMethod` — `resolvedUser` is `null`; the Emby authenticate response names `alice` with remote access allowed. Asserts: the returned `ProviderAuthenticationResult.Username` is `alice`; `FakeUserManager.LastCreated` was created with the Emby user name; `LastUpdated` is the same `User` instance as `LastCreated`; `LastUpdated.Password` equals the hash `FakeCryptoProvider` derives from the typed password; `LastUpdated.AuthenticationProviderId` equals `EmbyAuthenticationProvider.ProviderId`; and the account's remote-access permission matches the Emby response.
    - `SavesTheHashInTheCallDirectlyAfterCreateUser` — same setup; asserts `FakeUserManager.Calls` equals exactly the two-element sequence `CreateUserAsync`, `UpdateUserAsync`. This is the AUTH-03 "directly after" assertion: any third `IUserManager` call inserted between the two breaks it.
    - `CreatesTheAccountOnlyOnce_WhenTheSameEmbyUserLogsInTwice` — first login with `resolvedUser` `null`, second login passing the created `User` as `resolvedUser`. Asserts `FakeUserManager.Calls` contains `CreateUserAsync` exactly once and `UpdateUserAsync` twice.
    - `AsksEmbyEveryTime_AndRewritesTheHash_EvenWhenTheSavedHashAlreadyMatches` — an existing Emby-method `resolvedUser` whose `Password` is already the hash `FakeCryptoProvider` derives from the typed password, and whose fingerprint is already recorded. Two logins with that same password. Asserts the stub handler recorded two requests to `Users/AuthenticateByName`, and `FakeUserManager.Calls` contains `UpdateUserAsync` twice. This is D-08 and AUTH-01: no equality short-circuit.
    - `RecordsTheVerifiedFingerprint_OnlyAfterTheHashIsSaved` — two cases in one `[Theory]` or two `[Fact]`s: on success, a fresh `EmbyVerifiedPasswords` built over the same temporary file reports `Matches(user.Id, savedHash)` true; with `UpdateUserThrows` set, the login is refused and a fresh `EmbyVerifiedPasswords` over that same file reports `Matches` false for that user. This is the AUTH-01 ordering guarantee: a hash that was never saved is never fingerprinted, so a later move to Default cannot accept it.
    - `MatchesTheEmbyUserNameIgnoringCaseOnly` — `[Theory]`: typed `Alice` against an Emby user named `alice` is accepted and the account is created under the Emby user name; typed `alice ` (one trailing space) is refused with exactly one recorded request, to the user-list path.
  </behavior>
  <action>
    Extend the existing test class with the happy paths and the AUTH-01/AUTH-03 ordering assertions above.

    Read the saved hash for an assertion through `FakeUserManager.LastUpdated.Password`, and derive the expected value by calling the same `FakeCryptoProvider` the test passed into `CreateProvider` — never by hard-coding a hash string.

    For `RecordsTheVerifiedFingerprint_OnlyAfterTheHashIsSaved`, have `CreateProvider` expose the temporary fingerprint file path so the test can build a second `EmbyVerifiedPasswords` over it after the login. Reading the state back through a fresh instance, rather than through the instance the provider holds, is what makes the assertion about the file rather than about in-memory state.

    For the two-login cases, queue four stub responses in order: user list, authenticate, authenticate, and nothing else — the `ManualTimeProvider` never advances, so the 60-second user-list cache still holds on the second login and no second user-list request is made.

    Prove this group with the break-then-restore step: remove the `user.Password = passwordHash` assignment at `EmbyAuthenticationProvider.cs:182` and confirm the hash assertions go red; restore it. Then move the `verifiedPasswords.Record` call at `:111` to before the create/save call and confirm `RecordsTheVerifiedFingerprint_OnlyAfterTheHashIsSaved` goes red; restore it.

    Do not add a branch that skips the hash write or the fingerprint write when the saved hash already verifies the typed password — that is deferred to PERF-02 in Phase 4, which measures the cost first, and this task's `AsksEmbyEveryTime_AndRewritesTheHash_EvenWhenTheSavedHashAlreadyMatches` test is the guard against it being added.
  </action>
  <verify>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx --filter "FullyQualifiedName~EmbyAuthenticationProviderTests"</automated>
    <fails_when>non-zero exit, or the summary prints "Zero tests ran"</fails_when>
    <automated>dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx</automated>
    <fails_when>non-zero exit, or the summary line "failed: 0" is absent from the test run summary</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit; `dotnet format --verify-no-changes` prints a formatting diff, or an analyzer warning is reported as an error</fails_when>
  </verify>
  <acceptance_criteria>
    - Source: `rg -q 'EmbyAuthenticationProvider.ProviderId' tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs` succeeds — the login-method assertion is present.
    - Source: `rg -q 'Calls' tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs` succeeds — the call-sequence assertion is present.
    - Source: `! rg -q '"\$2[aby]?\$' tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs` succeeds — no hard-coded password-hash literal was introduced.
    - Behavior: a first login for an Emby user with no Jellyfin account returns that user name, and the recorded `IUserManager` call sequence is exactly `CreateUserAsync` then `UpdateUserAsync`.
    - Behavior: the recorded `UpdateUserAsync` argument carries the hash the fake crypto provider derives from the typed password and `EmbyAuthenticationProvider.ProviderId` as its login method.
    - Behavior: two logins with the same password for the same existing Emby-method account produce two `Users/AuthenticateByName` requests and two `UpdateUserAsync` calls.
    - Behavior: when `UpdateUserAsync` throws, a fresh `EmbyVerifiedPasswords` over the test's temporary file reports no match for that user.
    - Test: `dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx` exits 0 and reports `failed: 0`.
    - Documented: the SUMMARY names both broken lines and which tests went red for each.
  </acceptance_criteria>
  <done>The create-account and existing-account paths are covered, the AUTH-03 call ordering is asserted, and AUTH-01's "only Emby decides, and the saved hash always follows the accepted password" behaviour is asserted at the unit level.</done>
</task>

<task type="auto">
  <name>Task 3: Document the account-creation window in docs/how-it-works.md</name>
  <files>docs/how-it-works.md</files>
  <read_first>
    - docs/how-it-works.md — the file being modified; read the whole file. Step 5 (line 11) is where account creation is described, step 6 (line 12) is the password copy, and the `## Security notes` and `## Limits` sections are the two candidate homes for the new text.
    - .planning/phases/01-account-creation-and-login-security/01-CONTEXT.md — the `<specifics>` block states the distinction the wording must keep: an account without a password is safe on the Emby login method (the plugin refuses a blank password) and open on the Default login method.
    - src/Jellyfin.Plugin.EmbyAuth/EmbyAuthenticationProvider.cs — `CreateAccountAsync` lines 168-207, so the description matches what the code does, including the cleanup delete and what happens when it fails.
    - .planning/ROADMAP.md — Phase 1 success criteria 1 and 2 are the exact claims this text must be consistent with.
    - .claude/rules/plugin.md — the `## Blank passwords` section, which the new text must not contradict.
  </read_first>
  <action>
    Add the AUTH-03 description of the account-creation window. Keep the README short and the detail here, per `CLAUDE.md`.

    The text must state, in this repository's plain register:
    - Jellyfin's only account-creation call takes a name and no password, so a new account exists for a brief moment on the Default login method with no password before the plugin's next call sets the Emby-verified hash and the Emby login method. Name that as the cause: there is no create-with-password call.
    - During that moment the Default login method would accept a blank password for the new account.
    - The plugin makes the moment as short as it can: it computes the hash before creating the account, and the save is the very next call.
    - If that save fails, the plugin deletes the new account and refuses the login. If the delete also fails, the account stays on the Default login method without a password; the plugin records the account name in the Jellyfin log at Error level so an administrator can remove it or set a password.
    - Jellyfin's own user creation has the same moment.

    Do not write that the window is eliminated, closed, prevented, or impossible — it is not, and D-01 chose to keep Jellyfin's own steps rather than remove it. Do not add a mitigation the code does not implement.

    Place the description as a numbered sub-point under step 5 or as its own short subsection immediately after the login steps, and add a one-line entry in `## Limits` that points to it. Keep the existing step numbering intact: this task must not renumber or delete any existing step. Plan 01-04 owns the removal of step 2.
  </action>
  <verify>
    <automated>rg -q 'CreateUserAsync' docs/how-it-works.md</automated>
    <fails_when>non-zero exit — the new text does not name the Jellyfin call that creates the account</fails_when>
    <automated>rg -q -i 'no password|without a password' docs/how-it-works.md</automated>
    <fails_when>non-zero exit — the text does not state that the account has no password during the window</fails_when>
    <automated>! rg -q -i 'eliminat|closes this window|prevents this window|cannot happen' docs/how-it-works.md</automated>
    <fails_when>non-zero exit — the text claims the window is eliminated or impossible, which contradicts D-01 and ROADMAP criterion 2</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit; a linter reports an error in the changed files</fails_when>
    <human-check>
      Read the account-creation section of `docs/how-it-works.md` and confirm it: (1) names the window between the account-creation call and the hash save, (2) says the account has no usable password during it and that the Default login method would accept a blank password then, (3) names the cause as Jellyfin having no create-with-password call, (4) describes the cleanup delete and what happens when that delete also fails, and (5) does not claim the window is eliminated. This is the manual-only verification recorded in `01-VALIDATION.md`: a string match proves the text exists, not that it is correct.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Docs: `docs/how-it-works.md` contains a paragraph naming the window between the account-creation call and the call that saves the hash.
    - Docs: that paragraph states the new account is on the Default login method with no password during the window, and that a blank password would open it then.
    - Docs: that paragraph names the cause — Jellyfin has no call that creates an account with a password.
    - Docs: that paragraph states that a failed save deletes the account and refuses the login, and that when the delete also fails the account stays on Default without a password and the account name is logged at Error.
    - Docs: `! rg -q -i 'eliminat|closes this window|prevents this window|cannot happen' docs/how-it-works.md` succeeds.
    - Docs: `rg -c '^[0-9]\. \*\*' docs/how-it-works.md` returns 7 — the existing seven login steps are intact and unrenumbered.
    - CLI: `mise run lint` exits 0.
  </acceptance_criteria>
  <done>`docs/how-it-works.md` describes the brief moment before the hash is saved, names its cause, states the risk plainly, and describes both failure outcomes. AUTH-03's documentation half is complete.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Symbols and files this phase creates. Nothing below exists on `main` today, so a drift check must not flag them as unacknowledged new symbols.

**Created by plan 01-01 and consumed here:**
- `FakeUserManager` and `FakeCryptoProvider` in `tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs`, including `FakeUserManager.Calls`, `LastCreated`, `LastUpdated`, `LastDeletedId`, `CreateUserThrows`, `UpdateUserThrows`, `DeleteUserThrows`.
- `EmbyAuthenticationProviderTests` in `tests/Jellyfin.Plugin.EmbyAuth.Tests/EmbyAuthenticationProviderTests.cs`.
- `EmbyAuthenticationProvider` constructor parameter `Func<PluginConfiguration?> configurationSource`.
- `LogDeleteFailed` on `EmbyAuthenticationProvider`.

**Created by this plan (01-02):**
- New test methods on `EmbyAuthenticationProviderTests` (no new types).
- A new account-creation-window subsection and a new `## Limits` entry in `docs/how-it-works.md` (no new symbols).

**Created by later plans in this phase:**
- `CHANGELOG.md` — new file at the repository root (plan 01-04).

**Removed by later plans in this phase:**
- `MigrationMode.JellyfinPasswordFirst` enum member (plan 01-03). <!-- planner-discipline-allow: JellyfinPasswordFirst -->
- `EmbyAuthenticationProvider.SavedPasswordMatches` (plan 01-03). <!-- planner-discipline-allow: SavedPasswordMatches -->
- `EmbyAuthenticationProvider.LogSavedPasswordUnreadable` (plan 01-03). <!-- planner-discipline-allow: LogSavedPasswordUnreadable -->
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions

1. **The `User` permission API used to set `IsDisabled` and `IsAdministrator` in a test.** `01-RESEARCH.md` verified `User.HasPermission(PermissionKind)` is what the provider reads, but did not compile a test that *sets* a permission. `AccountAccessPolicyTests.cs` and `AccountAccessPolicy.cs` show the setter this codebase uses; follow that, and do not reach into the `Permissions` collection directly if a method exists.

2. **Reading `EmbyVerifiedPasswords` state back through a second instance over the same file.** The plan assumes `Matches` loads from the file on first use, which `EmbyVerifiedPasswords.Record` implies (it calls `Load()` under a lock). Not compiled in research. If `Matches` cannot observe a file written by another instance, assert on the file contents instead and record the change in the SUMMARY.

3. **The trailing-space refusal path.** `.claude/rules/plugin.md` records that Emby 4.10.0.40 accepted a typed name with a trailing space, which is why the plugin requires an exact match ignoring case. The unit test asserts the plugin's own behaviour (refused at the user-list check), not Emby's — the Emby-side behaviour is covered by the e2e suite.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jellyfin login request → `EmbyAuthenticationProvider.Authenticate` | Untrusted user name and password cross here. |
| `EmbyAuthenticationProvider` → Emby (`EmbyClient`) | A password crosses to an external service; only names on the cached Emby user list cross. |
| `EmbyAuthenticationProvider` → Jellyfin user store (`IUserManager`) | The account row and the hash cross here. |
| `EmbyVerifiedPasswords` file → a later move to the Default login method | A fingerprint recorded here is what later authorises a move; a wrong record would let a hash Emby never verified open an account. |
| `docs/how-it-works.md` → the administrator | The documented security model is what an operator relies on; an overstated claim is itself a risk. |

## STRIDE Threat Register

ASVS enforcement level 1; blocking severity `high`. Derived from `01-RESEARCH.md` `## Security Domain`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-Bypass | Spoofing | the saved Jellyfin hash on an Emby-method account | high | mitigate | AUTH-01: while a user is on the Emby login method, the saved hash plays no part in accepting a login. Task 2's `AsksEmbyEveryTime_AndRewritesTheHash_EvenWhenTheSavedHashAlreadyMatches` asserts two logins with the same password each reach `Users/AuthenticateByName`, so no equality short-circuit can be added without a red test. Plan 01-03 removes the one setting that made the saved hash decisive. |
| T-01-Fingerprint | Elevation of Privilege | `EmbyVerifiedPasswords.Record` ordering | high | mitigate | A fingerprint is what later authorises a move to the Default login method (`.claude/rules/plugin.md`). Task 2's `RecordsTheVerifiedFingerprint_OnlyAfterTheHashIsSaved` asserts that a hash whose save failed is never fingerprinted, so a failed write cannot leave a record that a later move would trust. |
| T-01-Race | Spoofing / Elevation of Privilege | the window between `CreateUserAsync` and the hash-setting save | medium | mitigate | Cannot be closed with the public Jellyfin 12.1 `IUserManager` API; D-01 rejected the direct row insert. Task 2's `SavesTheHashInTheCallDirectlyAfterCreateUser` pins the two-call sequence so no third call can be inserted into the window, and Task 3 documents the residual race honestly (ASVS V7 — do not claim a control you do not have). |
| T-01-Overclaim | Repudiation | the documented security model in `docs/how-it-works.md` | medium | mitigate | Task 3's negative gate plus its `<human-check>` block the wording "eliminated / closed / prevented / cannot happen". An operator who believes the window is closed would not check the Error log for a half-made account. |
| T-01-Info | Information Disclosure | test assertions on hashes | low | mitigate | Expected hashes are derived by calling the fake crypto provider, never hard-coded, so no hash literal enters the repository. The e2e log check (`e2e/90-jellyfin-log.bats`) remains the guard for real secrets. |
| T-01-SC | Tampering | npm/pip/cargo/NuGet installs | n/a | accept | No package-manager install task in this phase; `01-RESEARCH.md` `## Package Legitimacy Audit` records zero new external packages. No legitimacy checkpoint is required. |

**Canon referral:** login-response user enumeration on the refusal path is canon OWASP surface — covered by `/gsd-secure-phase`; not minted as a prohibition here.
</threat_model>

<verification>
- `dotnet test --solution Jellyfin.Plugin.EmbyAuth.slnx` exits 0 and reports `failed: 0`.
- `mise run test` exits 0.
- `mise run lint` exits 0.
- `mise run e2e` exits 0 — required by `CLAUDE.md` for any change under `src/`; this plan changes no `src/` file, so run it once at the end of the plan as a regression check rather than per task.
- `prek run` exits 0 before each commit.
</verification>

<success_criteria>
- `EmbyAuthenticationProvider` has unit tests for the account checks, the Emby user-list check, the Emby login, account creation, and the existing-account save (ROADMAP criterion 5).
- The AUTH-03 call ordering is asserted, not assumed.
- `docs/how-it-works.md` describes the account-creation window without overstating the plugin's control over it (ROADMAP criterion 2).
- Every added test was proved to fail when the behaviour it covers was broken.
</success_criteria>

<output>
Create `.planning/phases/01-account-creation-and-login-security/01-02-SUMMARY.md` when done.
</output>
