---
phase: 05-public-repository
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/release-gate.sh
  - tests/scripts/release-gate.bats
  - .github/workflows/release.yml
  - docs/development.md
autonomous: true
requirements: [REL-01]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The gate is a tracked shell script at `scripts/release-gate.sh` with bats tests in `tests/scripts/`, called from a step in `.github/workflows/release.yml` — not inline workflow YAML, and not a new action on `scripts/package.sh`, which keeps packaging and GitHub API concerns in separate files (D-01)"
    - "`scripts/release-gate.sh check <sha>` exits 0 only when the commit carries exactly one `ci-success` check run whose `status` is `completed` and whose `conclusion` is `success` (D-03)"
    - "Every other state exits 1 with a message on stderr naming the state: no matching check run, more than one matching check run, `in_progress`, `cancelled`, `failure`, `skipped`, `timed_out`, or any other conclusion (D-02, D-03)"
    - "A `gh api` call that itself fails (403, 404, network) exits 1 with a message distinct from the no-matching-check-run message, so a permission error is never read as a clean absence (RESEARCH.md Pitfall 1)"
    - "`.github/workflows/release.yml` runs the gate after the tag check and before `mise run test`, so a tag on a commit whose CI did not pass stops the workflow before any release is created (REL-01)"
    - "The `release` job's `permissions:` block grants `checks: read` alongside `contents: write`, and each grant carries an inline comment naming the step that needs it"
    - "`docs/development.md`'s Releases section states that the workflow refuses a tag whose commit has no passing `ci-success` check run, and that the fix is to push the tag again once CI is green (D-02)"
    - "`tests/scripts/release-gate.bats` asserts the argument string the script hands to `gh`, so the endpoint path and the `check_name` filter are exercised, not just the response handling (D-04)"
    - "The `check_name` filter is carried in the request's query string, so `gh` sends a GET. Measured against the live repository: the field-flag form returns HTTP 404 because `gh` switches the method to POST, while the query-string form returns HTTP 422 for an unknown SHA — and a 404 lands in the gate's API-call-failed branch, which would refuse every release"
    - "Four layered guards stop the query-string form from regressing: a source assertion over `scripts/release-gate.sh`, an argv assertion in the fake-`gh` test, a 422 refusal-state test, and one test that calls the real `gh` and proves the response is not a 404 — the last is the only check that exercises real `gh`'s method selection, which a fake structurally cannot reproduce"
  artifacts:
    - path: "scripts/release-gate.sh"
      provides: "the ci-success release gate, one explicit action"
      contains: "check_name=ci-success"
    - path: "tests/scripts/release-gate.bats"
      provides: "fake-`gh`-on-PATH coverage of every check-run state and of the argument construction"
      contains: "release-gate.sh"
    - path: ".github/workflows/release.yml"
      provides: "the gate step and the checks: read grant"
      contains: "release-gate.sh check"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/release-gate.sh"
      via: "a plain run: step invoking the script with ${{ github.sha }}"
      pattern: "release-gate\\.sh check"
    - from: "scripts/release-gate.sh"
      to: "the GitHub check-runs REST endpoint"
      via: "gh api \"repos/$GH_REPO/commits/$sha/check-runs?check_name=ci-success\""
      pattern: "check-runs\\?check_name=ci-success"
    - from: "tests/scripts/release-gate.bats"
      to: "scripts/release-gate.sh"
      via: "a fake gh executable placed first on PATH that prints fixture JSON and records its argv"
      pattern: "PATH="
  prohibitions:
    - requirement_id: REL-01
      category: safety
      verification: judgment
      statement: "MUST NOT weaken the release gate so a release proceeds anyway — the gate step must not tolerate its own failure, the script must not be wrapped in a construct that discards its exit status, and no code path may treat a failed API call, a missing check run, or a non-success conclusion as a pass."
---

<objective>
Build the REL-01 release gate end to end: a new `scripts/release-gate.sh` that refuses a release when the tagged commit has no passing `ci-success` check run, the bats coverage that proves every refusal state, and the `release.yml` step and permission grant that put the gate on the release path.

Purpose: a `v*` tag is the only trigger for a release, and today nothing checks whether the tagged commit's CI ever passed. Branch protection cannot supply that check on this repository (HTTP 403 until it is public), so the gate must be self-contained.
Output: `scripts/release-gate.sh`, `tests/scripts/release-gate.bats`, a gate step plus `checks: read` in `.github/workflows/release.yml`, and a corrected Releases procedure in `docs/development.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-repository/05-CONTEXT.md
@.planning/phases/05-public-repository/05-RESEARCH.md
@.planning/phases/05-public-repository/05-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a tag on a commit without passing CI is refused" — one path only</name>
  <files>scripts/release-gate.sh, tests/scripts/release-gate.bats, .github/workflows/release.yml</files>
  <read_first>
    - `scripts/package.sh` — the script shape every new script in `scripts/` copies: `#!/usr/bin/env bash` + `set -euo pipefail`, the `Usage:`/`Environment:` header comment block, `REPO_ROOT` from `${BASH_SOURCE[0]}`, a `usage()` that prints to stderr, a `main()` with a `case` dispatch on `${1:-}` and `usage; exit 2` for both a missing and an unknown action, and worker functions that `return 1` after an `>&2` message rather than calling `exit`
    - `tests/scripts/package.bats` — the bats shape: `REPO_ROOT="$(cd "$BATS_TEST_DIRNAME/../.." && pwd)"` in `setup_file`, and the `run` / `[ "$status" -eq N ]` / `[[ "$output" == *"..."* ]]` assertion triple
    - `.github/workflows/release.yml` — the current step list, the `permissions:` block at the job level, and the `env:` block shape on the tag-check and release-create steps
    - `.planning/phases/05-public-repository/05-RESEARCH.md` — Pattern 1 (server-side `check_name` filtering, including the measured table showing the field-flag form returns 404 and the query-string form returns 422), Pitfall 1 (the `checks: read` grant), Code Examples
    - `~/.claude/rules/shell.md` and `.claude/rules/e2e.md` — shell conventions this repo lints with `shellcheck -x` and `shfmt`
  </read_first>
  <behavior>
    - Test 1: with a fake `gh` that prints a check-runs body whose `check_runs` array is empty, `scripts/release-gate.sh check deadbee` exits 1 and stderr names that no `ci-success` check run was found on that commit.
    - Test 2: with a fake `gh` that prints one check run named `ci-success` with `status` `completed` and `conclusion` `success`, `scripts/release-gate.sh check deadbee` exits 0 and prints nothing to stderr.
    - Test 3: the fake `gh` records its whole argument list to a file; after the passing run, that recorded list contains `api`, the path `repos/owner/repo/commits/deadbee/check-runs`, and `check_name=ci-success` inside the same argument as the path rather than as a separate argument. The test also asserts the recorded list carries no field flag at all. This is the assertion that proves the request itself is built correctly, not only that the response is read correctly (D-04).
    - Test 4: one call to the **real** `gh`, not the fake, against a well-formed SHA that GitHub does not know. It asserts the response is not an HTTP 404. This is the only check in the file that exercises real `gh`'s method selection — the fidelity gap a fake structurally cannot cover, because a fake reproduces argument recording and fixture output but chooses no HTTP method. The test skips cleanly when `gh` is absent or unauthenticated.
  </behavior>
  <action>
    Write the bats file first and watch it fail, then write the script, then wire the workflow. Do not reorder.

    RED — create `tests/scripts/release-gate.bats`. Resolve `REPO_ROOT` in `setup_file` exactly as `tests/scripts/package.bats` does, and in the same function capture the absolute path of the real `gh` with `command -v gh` into an exported variable, before anything touches `PATH`. Test 4 needs that path because by the time a test body runs, the fake is first on `PATH`.

    In `setup`, create `"$BATS_TEST_TMPDIR/bin"`, write an executable file named `gh` into it, and prepend that directory to `PATH` for the test. Per D-04 the seam is this fake binary on `PATH` — the script itself gets no environment variable that exists only for tests. The fake `gh` appends its arguments to `"$BATS_TEST_TMPDIR/gh-argv"`, one argument per line (`printf '%s\n' "$@"`), and then prints the fixture body held in a per-test variable. One argument per line is what makes Test 3's "in the same argument as the path" assertion checkable at all — a flattened `"$@"` cannot tell a query string from a second argument. Export `GH_REPO=owner/repo` and `GH_TOKEN=sentinel-api-key-gh` in `setup` because the real `gh` reads both and the workflow supplies both. Use that exact fixture prefix: the repository's other test fixtures already use it, and plan 05-03's gitleaks allowlist clears it by value, so this line does not become a new secret-scanner finding.

    Test 4 is the one test that must not see any of that. Inside it, `skip` when the captured real-`gh` path is empty, and `skip` when `gh auth status` reports no authenticated account; a skip is the correct outcome offline, in a pre-commit run with no credentials, and on a fork, and the test is written to be allowed to skip for exactly that reason. Then unset `GH_REPO` and `GH_TOKEN` for the duration of the call — the fixture values would point `gh` at a repository that does not exist and at a token that does not authenticate — and invoke the captured real binary directly with the same request shape the script builds: the path `repos/{owner}/{repo}/commits/<sha>/check-runs` with the filter in the query string. `gh` expands the two placeholders from the current git remote, so the test works in a fork without naming this repository. Use the well-formed but unknown SHA `0123456789abcdef0123456789abcdef01234567`; its leading run of hex digits is already cleared by plan 05-03's allowlist, so it adds no secret-scanner finding. Assert the call exits non-zero and that its combined output does not contain `404`, and record the status GitHub actually returned in the SUMMARY — the measured value is 422, and the load-bearing half of the assertion is the absence of the 404.

    Write the four tests in the `<behavior>` block. Run bats and confirm Tests 1, 2, and 3 fail because the script does not exist. Test 4 calls `gh` and never calls the script, so it passes or skips from the start; that is expected, because it is a fidelity probe against the tool rather than a test of code this task writes.

    GREEN — create `scripts/release-gate.sh`. Copy `scripts/package.sh`'s header, `REPO_ROOT`, `usage()`, and `main()` `case`-dispatch shape. The one action is `check`, taking one argument, the commit SHA; `main` validates `$# -ne 2` before dispatching and falls through to `usage; exit 2` for a missing or unknown action. The header comment block documents `Usage: scripts/release-gate.sh check SHA` and, under `Environment:`, `GH_REPO` and `GH_TOKEN` as the two variables `gh` itself reads.

    The `check` worker refuses, with a distinct stderr message and `return 1`, in each of these cases, and returns success in no other case:
    - `GH_REPO` is unset or empty — refuse before any request, because an empty owner/repo would build a malformed URL.
    - the `gh api` call itself fails — capture it with an `if ! runs_json="$(gh api ...)"` form so a 403, a 404, a 422, or a network error produces its own message. That message must not be the same sentence used for a missing check run; a permission error read as a clean absence is the failure mode RESEARCH.md Pitfall 1 names. 422 is the state GitHub returns for a SHA it does not know, which a force-push or a deleted commit produces, and Task 2 tests it separately from 403.
    - the `check_runs` array length is not exactly 1.
    - `status` is not `completed`, or `conclusion` is not `success`. The message names the observed `status`/`conclusion` pair so the maintainer can tell `cancelled` from `failure`.

    Build the request as `gh api "repos/$GH_REPO/commits/$sha/check-runs?check_name=ci-success"` — the whole call on one physical line, with no line continuation, so the source assertion below sees the complete invocation. Pass the filter in the query string and pass no field flag of any kind, not even a commented-out one. Measured against the live repository: carrying the filter in a field flag makes `gh` send the request as a POST and it returns HTTP 404, while the query-string form returns HTTP 422 for an unknown SHA. A 404 lands in the API-call-failed branch below, so the gate would fail closed on every single release and never open. The filter still runs server-side, and the endpoint's `filter` parameter still defaults to `latest`, so a re-run of `ci-success` on the same SHA does not produce a second array element.

    Read the response with the repository's pinned `jq` (`jq '.check_runs | length'`, `jq -r '.check_runs[0].status'`, `jq -r '.check_runs[0].conclusion'`). Deliberately do not filter inside `gh`: the bats fake stands in for `gh`, so a filter applied inside `gh` would have to be re-implemented inside the fake, and the fake would stop emitting the response body the real endpoint returns. Keeping the parse one process to the right is what preserves fake fidelity.

    Run bats and confirm all four tests pass, Test 4 either passing or skipping. Then break the `conclusion` comparison once, confirm Test 2 goes red, and restore the file byte-for-byte — the repository rule is to watch the test fail before trusting it. Then do the same for the request shape: move the filter out of the query string and into a field flag, confirm Test 3 goes red, and restore the file byte-for-byte. That second proof is what makes the guard real rather than decorative.

    WIRE — edit `.github/workflows/release.yml`. Insert a step named `Require a passing ci-success check run on the tagged commit` directly after `Check that the tag matches the plugin version` and directly before `Build and run unit tests`. Give it an `env:` block in the same shape as the existing release-create step, carrying `GH_TOKEN: ${{ github.token }}`, `GH_REPO: ${{ github.repository }}`, and `SHA: ${{ github.sha }}`, and a one-line `run:` invoking `scripts/release-gate.sh check "$SHA"`. Keep it a plain `run:` step with no failure-tolerance key and no shell construct that discards the script's exit status, so a non-zero exit fails the job. In the same file, add `checks: read` to the `release` job's `permissions:` block — job-level `permissions:` fully replaces the workflow-level block, so `checks` is absent today and the call would return 403. Put an inline comment on each of the two grants naming the step that needs it: `contents: write` for the release-create step, `checks: read` for the gate step. Leave the workflow-level `contents: read` at the top of the file unchanged.
  </action>
  <acceptance_criteria>
    - `bats tests/scripts/release-gate.bats` exits 0 and its summary line reports at least 4 tests. A skipped Test 4 still counts toward that total and is not a failure.
    - `scripts/release-gate.sh` is executable (`test -x scripts/release-gate.sh`).
    - `rg -q 'check-runs\?check_name=ci-success' scripts/release-gate.sh` matches — guard 1, the source assertion: the filter is in the query string.
    - `rg -c 'gh api' scripts/release-gate.sh` returns exactly 1 — one call, so the assertion below sees all of it.
    - `rg -q 'gh api.*\\$' scripts/release-gate.sh; test $? -eq 1` — the call is on one physical line, with no continuation that could hide an argument from the next check.
    - `! rg -v '^\s*#' scripts/release-gate.sh | rg -q -- 'gh api.*(-f|--field|--raw-field)[= ]'` — guard 1 again, the negative half: outside comments, the call passes no field flag, so there is no HTTP method for `gh` to switch. Comment lines are stripped first so a note about the rejected form cannot invalidate its own gate.
    - The recorded-argv assertion in Test 3 checks the query string and the absence of a field flag, not only the endpoint path — guard 2.
    - `rg -q 'gh auth status' tests/scripts/release-gate.bats` matches and `rg -q '\bskip\b' tests/scripts/release-gate.bats` matches — guard 4 refuses to run rather than fail when `gh` is absent or unauthenticated.
    - `rg -q '404' tests/scripts/release-gate.bats` matches — guard 4 asserts the un-faked call is not a 404.
    - `rg -q 'release-gate\.sh check' .github/workflows/release.yml` matches.
    - `rg -q '^\s*checks:\s*read' .github/workflows/release.yml` matches.
    - `rg -q '^\s*continue-on-error' .github/workflows/release.yml; test $? -eq 1` — the gate step tolerates no failure of its own.
    - In `.github/workflows/release.yml` the gate step's line number is greater than the line number of the `scripts/package.sh check-tag` step and less than the line number of the `mise run test` step.
    - `shellcheck -x scripts/release-gate.sh tests/scripts/release-gate.bats` exits 0 and `shfmt -d scripts tests/scripts` prints no diff.
    - `actionlint` exits 0.
  </acceptance_criteria>
  <precondition>`mise install` has been run in the repository, so the pinned `bats`, `jq`, `shellcheck`, `shfmt`, and `actionlint` binaries resolve on `PATH`.</precondition>
  <verify>
    <automated>bats tests/scripts/release-gate.bats</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 4 tests, or any line begins with "not ok"</fails_when>
    <automated>rg -q 'check-runs\?check_name=ci-success' scripts/release-gate.sh && ! rg -v '^\s*#' scripts/release-gate.sh | rg -q -- 'gh api.*(-f|--field|--raw-field)[= ]'</automated>
    <fails_when>non-zero exit — either the filter left the query string, or a field flag was reintroduced on the `gh api` call, which makes `gh` send a POST and return 404 and makes the gate refuse every release</fails_when>
    <automated>shellcheck -x scripts/release-gate.sh tests/scripts/release-gate.bats && shfmt -d scripts tests/scripts && actionlint</automated>
    <fails_when>non-zero exit, or `shfmt -d` prints a unified diff for either new file</fails_when>
    <automated>rg -q 'release-gate\.sh check' .github/workflows/release.yml && rg -q '^\s*checks:\s*read' .github/workflows/release.yml</automated>
    <fails_when>non-zero exit — either the gate step or the checks: read grant is absent from release.yml</fails_when>
  </verify>
  <done>`scripts/release-gate.sh check <sha>` refuses a commit with no `ci-success` check run and accepts one whose `ci-success` run is completed/success; the bats file proves both, asserts the argument string sent to `gh` carries the filter in the query string and no field flag, and proves against the real `gh` that the request shape is not a 404; `release.yml` calls the gate between the tag check and the test build, with `checks: read` granted and both grants commented.</done>
  <reversibility rating="reversible">A shell script, its tests, and a workflow step are all deletable in one commit; nothing outside the repository changes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every remaining refusal state, and the usage pair</name>
  <files>tests/scripts/release-gate.bats, scripts/release-gate.sh</files>
  <read_first>
    - `tests/scripts/release-gate.bats` — the file Task 1 created, including the fake-`gh` helper and the fixture-variable convention it established
    - `scripts/release-gate.sh` — the file Task 1 created, so the new tests assert the messages it actually prints
    - `tests/scripts/package.bats` — the usage-and-unknown-action test pair every script in `scripts/` is expected to have
    - `.planning/phases/05-public-repository/05-CONTEXT.md` — D-03's enumeration of the states that must refuse, including why `cancelled` is reachable rather than theoretical
  </read_first>
  <behavior>
    - Test 5: one `ci-success` check run with `status` `in_progress` and `conclusion` `null` exits 1; stderr names `in_progress`.
    - Test 6: `status` `completed`, `conclusion` `cancelled` exits 1; stderr names `cancelled`. This state is reachable because `ci.yml`'s concurrency group sets `cancel-in-progress: true`, so a commit superseded on `main` leaves a cancelled run.
    - Test 7: `status` `completed`, `conclusion` `failure` exits 1.
    - Test 8: `status` `completed`, `conclusion` `skipped` exits 1.
    - Test 9: `status` `completed`, `conclusion` `timed_out` exits 1.
    - Test 10: `status` `completed`, `conclusion` `neutral` (a conclusion outside the enumerated set) exits 1 — the script matches `success` exactly rather than excluding a fixed list.
    - Test 11: two check runs named `ci-success` in the array exits 1; stderr says how many were found.
    - Test 12: a fake `gh` that writes an HTTP 403 message to stderr and exits 1 makes the script exit 1 with its own API-call-failed message, and that message is not the no-matching-check-run message.
    - Test 13: a fake `gh` that writes an HTTP 422 message to stderr and exits 1 produces the same API-call-failed refusal, and not the no-matching-check-run refusal. 422 is a distinct reachable state, not a variant of 403: GitHub returns it for a SHA it does not know, which a force-push or a deleted commit produces, and it is the status the live endpoint returned during planning. Assert both directions, as Test 12 does.
    - Test 14: `scripts/release-gate.sh` with no action exits 2 and prints `Usage:`.
    - Test 15: `scripts/release-gate.sh` with an unknown action exits 2 and prints `Usage:`.
    - Test 16: `scripts/release-gate.sh check` with no SHA exits 2 and prints `Usage:`.
  </behavior>
  <action>
    Extend `tests/scripts/release-gate.bats` with the twelve tests in the `<behavior>` block, reusing the fake-`gh` helper Task 1 established rather than adding a second mechanism. Each check-run fixture is the same JSON object shape returned by the check-runs endpoint, differing only in `status` and `conclusion`. The two-run fixture (Test 11) holds two objects in the `check_runs` array. Numbering continues from Task 1, which left the file at four tests, so this task takes it to sixteen.

    Test 12's fake `gh` writes a plausible HTTP 403 error line to stderr and exits 1 without printing a body; assert the script's exit status is 1 and that the captured output contains the script's own API-call-failed wording and does not contain the no-matching-check-run wording. Assert both directions explicitly — a test that only checks the exit status cannot tell the two refusals apart, and telling them apart is the whole point of the case.

    Test 13 is the same shape with an HTTP 422 error line, and it carries the same two assertions in the same both-directions style. Write it as its own test rather than folding it into Test 12: 403 and 422 arrive from different causes — a missing `checks: read` grant against a commit that exists, versus a commit GitHub cannot find at all — and a single test covering both would stop distinguishing them the moment one branch changed. 422 is also the status the live endpoint returned during planning, so it is the state a real misconfiguration is most likely to surface as.

    Copy the usage and unknown-action pair from `tests/scripts/package.bats` verbatim in shape, substituting the script name, and add the third case where the action is present but its SHA argument is missing.

    Run bats. If any of the twelve passes on the first run without a corresponding branch in the script, that branch is missing or wrong — add it to `scripts/release-gate.sh` and re-run. Where the script already handles a case, prove the test has teeth: break that one comparison, watch the test go red, restore the file byte-for-byte.
  </action>
  <acceptance_criteria>
    - `bats tests/scripts/release-gate.bats` exits 0 and its summary line reports at least 16 tests. A skipped Task 1 Test 4 still counts toward that total.
    - Every state named in D-03 has a test: `rg -c 'in_progress|cancelled|failure|skipped|timed_out|neutral' tests/scripts/release-gate.bats` returns at least 6.
    - The 403 test and the 422 test each assert both the presence of the API-failure wording and the absence of the no-check-run wording; in each case both assertions exist in the same `@test` block.
    - `rg -q '422' tests/scripts/release-gate.bats` matches — guard 3, the unknown-SHA refusal state, has its own test.
    - `rg -c '@test' tests/scripts/release-gate.bats` returns at least 16.
    - `shellcheck -x tests/scripts/release-gate.bats scripts/release-gate.sh` exits 0 and `shfmt -d scripts tests/scripts` prints no diff.
  </acceptance_criteria>
  <verify>
    <automated>bats tests/scripts/release-gate.bats</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 16 tests, or any line begins with "not ok"</fails_when>
    <automated>shellcheck -x tests/scripts/release-gate.bats scripts/release-gate.sh && shfmt -d scripts tests/scripts</automated>
    <fails_when>non-zero exit, or `shfmt -d` prints a unified diff</fails_when>
    <automated>mise run test</automated>
    <fails_when>non-zero exit — the new bats file is picked up by `bats tests/scripts` and must not break the existing suite</fails_when>
  </verify>
  <done>Every state D-03 enumerates has a test that proves the gate refuses it, the failed-API-call refusal is provably distinct from the no-check-run refusal for both a 403 and a 422, and the script carries the usage and unknown-action convention every other script in `scripts/` has.</done>
</task>

<task type="auto">
  <name>Task 3: The Releases procedure states what the gate refuses</name>
  <files>docs/development.md</files>
  <read_first>
    - `docs/development.md` — the Releases section's three numbered steps, and the task table at the top of the file
    - `.github/workflows/release.yml` — the step list as Task 1 left it, so the documented order matches the file
    - `.planning/phases/05-public-repository/05-CONTEXT.md` — D-02, the fail-closed-with-no-grace-window decision, which is what the maintainer needs to read here
  </read_first>
  <action>
    Update the Releases section of `docs/development.md`. Its step 3 currently says the workflow checks the tag against the version, runs `mise run test` and `mise run package`, and creates a GitHub release. Restate it so the gate appears in the real order: the workflow checks the tag matches the version, then requires the tagged commit to carry a `ci-success` check run that completed with conclusion `success`, then runs `mise run test` and `mise run package`, then creates the release.

    Add a following sentence stating the refusal behavior in plain terms: the workflow stops before creating any release when the tagged commit has no `ci-success` check run, when that run is still in progress, or when it finished with any conclusion other than `success` — including `cancelled`, which happens when a later push to `main` cancelled the earlier run. State the fix: wait for CI on that commit to finish green, then delete and push the tag again. There is no waiting period and no override inside the workflow.

    Keep the existing numbered-list style and the file's plain declarative tone. Do not restate the gate's implementation, and do not add a new heading — the Releases section stays one section.
  </action>
  <acceptance_criteria>
    - `rg -q 'ci-success' docs/development.md` matches.
    - `rg -q 'cancelled' docs/development.md` matches — the reachable state D-03 calls out is named, not only the obvious failure case.
    - The Releases section still lists exactly three numbered steps (the gate joins step 3's description rather than becoming a fourth step).
    - `rg -q 'release-gate' docs/development.md` — the document may name the script, but the sentence a maintainer acts on must be behavioral, not a script invocation; no step instructs a human to run the gate by hand.
    - No sentence claims the workflow waits, retries, or re-runs CI.
  </acceptance_criteria>
  <verify>
    <automated>rg -q 'ci-success' docs/development.md && rg -q 'cancelled' docs/development.md</automated>
    <fails_when>non-zero exit — either the check-run name or the cancelled state is missing from the Releases section</fails_when>
    <automated>rg -q 'waits for CI|re-runs CI|retries CI' docs/development.md; test $? -eq 1</automated>
    <fails_when>exit 1 from the `test` — a sentence claiming the workflow waits for or re-runs CI contradicts D-02</fails_when>
    <human-check>Read the Releases section end to end. A maintainer who has never seen the gate can tell, from these three steps alone, why a pushed tag produced no release and what to do about it.</human-check>
  </verify>
  <done>`docs/development.md`'s Releases section describes the release path in the order the workflow actually runs it, names the states that refuse a tag including `cancelled`, and tells the maintainer to re-push the tag once CI is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pushed git tag → `release.yml` | A `v*` tag is the only trigger for a release. Anything the tag points at reaches the publish path. |
| `release.yml` job → GitHub REST API | The job's `GITHUB_TOKEN` crosses into the API with whatever scopes the `permissions:` block grants. |
| GitHub check-runs response → `scripts/release-gate.sh` | Untrusted-shaped JSON (empty array, null conclusion, error body) decides whether a release is published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `.github/workflows/release.yml` `release` job | high | mitigate | `scripts/release-gate.sh check` runs before `mise run test` and `mise run package`; it exits 0 only for exactly one `ci-success` check run at `completed`/`success`, so a release cannot be built from a commit whose suite never passed |
| T-05-02 | Spoofing | `scripts/release-gate.sh`, the `gh api` call | high | mitigate | An API call that fails produces its own refusal message and exit 1, tested explicitly for a 403 (Task 2 Test 12) and for a 422 (Task 2 Test 13); a 403 caused by a missing `checks: read` grant can never be read as "no `ci-success` run yet" and silently become a pass |
| T-05-22 | Tampering | the HTTP method `gh` chooses for the check-runs call | high | mitigate | Passing the filter as a field flag makes `gh` send a POST, which returns 404 and — measured, not assumed — drives the gate into its API-call-failed branch on every release, so the gate never opens. The filter is carried in the query string, and four layered guards hold it there: a source assertion over the script, the recorded-argv assertion in Task 1 Test 3, the 422 refusal-state test, and one un-faked call to the real `gh` that asserts the absence of a 404. Only the last exercises real `gh`'s method selection; a fake reproduces argv and fixture output but chooses no method, so a fake-only suite would stay green while the real command 404s |
| T-05-03 | Elevation of Privilege | `release` job `permissions:` block | medium | mitigate | Exactly `checks: read` is added to the existing `contents: write`; no broader scope, and each grant carries an inline comment naming its consuming step |
| T-05-04 | Tampering | the check-runs response shape | medium | mitigate | The script matches `conclusion == "success"` exactly rather than excluding a known-bad list, so an unenumerated conclusion (`neutral`, `action_required`, `stale`) refuses; Task 2 Test 10 proves it |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package-manager dependency. `05-RESEARCH.md`'s Package Legitimacy Audit records gitleaks as the phase's only new tool, resolved through mise's `aqua` backend, and it is introduced in plan 05-03, not here. |
</threat_model>

<verification>
- `bats tests/scripts/release-gate.bats` passes with at least 16 tests.
- `mise run test` passes, so the new bats file does not break `bats tests/scripts`.
- `mise run lint` passes, so `shellcheck`, `shfmt`, and `actionlint` accept the new script, the new test file, and the edited workflow.
- `rg -n 'release-gate\.sh check' .github/workflows/release.yml` shows the gate step between the tag check and `mise run test`.
- The request shape is guarded four ways: the source assertion over `scripts/release-gate.sh`, the recorded-argv assertion in Test 3, the 422 refusal-state test, and the one un-faked `gh` call that proves the response is not a 404.
- The gate's live pass path is proven for the first time by Phase 6's rehearsal release; inside this phase the evidence is the bats suite, the un-faked request-shape probe, and `actionlint` on the workflow.
</verification>

<success_criteria>
- `scripts/release-gate.sh` exists, is executable, takes one required action `check` with one required SHA argument, and prints usage and exits 2 for a missing or unknown action.
- Every state D-03 enumerates refuses with exit 1 and a state-naming stderr message; only `completed`/`success` on exactly one matching run exits 0.
- The `gh api` call carries `check_name=ci-success` in its query string and passes no field flag, so `gh` sends a GET; four layered guards keep it that way.
- A failed `gh api` call refuses with a message that a test proves is distinct from the no-check-run message, for both a 403 and a 422.
- `.github/workflows/release.yml` invokes the gate between the tag check and `mise run test`, tolerates no failure of that step, and grants `checks: read` with an explanatory comment.
- `docs/development.md` tells a maintainer why a tag produced no release and what to do.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan:

- `scripts/release-gate.sh` — new tracked script. Actions: `check SHA`. Reads `GH_REPO` and `GH_TOKEN` from the environment (both consumed by `gh` itself). One `gh api` call, on one line, with `check_name=ci-success` in the query string and no field flag. Exit codes: 0 pass, 1 refuse, 2 usage.
- `tests/scripts/release-gate.bats` — new bats file, 16 tests, picked up automatically by the existing `bats tests/scripts` line in `[tasks.test]`. Fifteen of them use the fake `gh` on `PATH`; one calls the real `gh` against a SHA GitHub does not know and asserts the response is not a 404, and that one skips when `gh` is absent or unauthenticated.
- `.github/workflows/release.yml` — new step `Require a passing ci-success check run on the tagged commit`; new job permission `checks: read`; inline comments on both permission grants.
- `docs/development.md` — Releases section restated with the gate and its refusal states.

Created elsewhere in phase 05 (for cross-plan reference): `.gitleaks.toml`, `tests/scripts/gitleaks.bats`, `scripts/pre-public-audit.sh` (action `run`), `tests/scripts/pre-public-audit.bats`, the `gitleaks = "8.30.1"` pin and the `gitleaks git` entry in `[tasks.lint]`, the `--persona=pedantic` flag on the existing zizmor entry, `name:` on all five workflow jobs, a `concurrency:` block in `release.yml`, the `^\.gitleaks\.toml$` alternative in `.pre-commit-config.yaml`'s lint `files` pattern, and the `## Compatibility` section in `README.md`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-public-repository/05-01-SUMMARY.md` when done
</output>
