---
phase: 02-safe-failures-for-the-fingerprint-file-and-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .mise.toml
  - .pre-commit-config.yaml
  - tests/js/package.json
  - tests/js/package-lock.json
  - tests/js/testHelpers.js
  - tests/js/configPage.test.js
  - src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html
autonomous: false
requirements: [UI-01, TEST-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`mise run test` installs the pinned JavaScript dependencies and runs the settings-page tests as part of the existing `test` task, so CI's existing `test` job covers them with no workflow change and `ci-success` is unchanged (TEST-04, D-09)."
    - "The settings-page tests execute the shipping `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` itself through jsdom with `runScripts: 'dangerously'` — there is no extraction step, no build step, and no second copy of the page script (TEST-04, D-10)."
    - "When `getPluginConfiguration` rejects on `pageshow`, `#EmbyAuthMigrationSummary` holds a non-empty failure message and the `.button-submit` element's `disabled` property is `true` (UI-01, D-06, D-08)."
    - "After a failed settings load, activating the Save button sends no configuration update — `updatePluginConfiguration` is never called (UI-01, D-06)."
    - "The load-failure `.catch` writes the same fixed message and disables Save whatever the rejection value is — a rejection carrying an `Error` and a rejection carrying no value produce the same page state (probe edge UI-01/empty)."
    - "Activating a disabled Save button twice after a failed load still sends no configuration update — the count of `updatePluginConfiguration` calls stays zero (probe edge UI-01/idempotency, D-06)."
    - "A `pageshow` that succeeds after a failed one populates the four inputs from the returned configuration and sets the `.button-submit` element's `disabled` property back to `false` (probe edge UI-01/concurrency, sequential half; CONTEXT.md discretion resolved — a later successful load re-enables Save)."
    - "The load-failure message is written as text, not markup: after a failed load `#EmbyAuthMigrationSummary` has zero child elements, and the message contains neither the configured Emby URL nor the configured API key even when the rejection value carries both (probe edge UI-01/encoding, T-02-01, D-08)."
    - statement: "The Jellyfin dashboard fires `pageshow` once per navigation to the settings page, so the page holds no guard against two overlapping `getPluginConfiguration` calls and this phase adds none. The guarantee this phase establishes is sequential: a later successful load re-enables Save and replaces the message. An overlapping pair whose failed load settled after a successful one would re-disable Save over good data."
      verification: backstop
  artifacts:
    - path: "tests/js/package.json"
      provides: "The jsdom devDependency, pinned exact, and the CommonJS module type for the settings-page test suite"
      contains: "jsdom"
    - path: "tests/js/package-lock.json"
      provides: "The committed lockfile that `npm --prefix tests/js ci` enforces, so a local run and a CI run resolve the same tree"
      contains: "jsdom"
    - path: "tests/js/testHelpers.js"
      provides: "buildDom(), flush(), and the ApiClient/Dashboard stub factories shared by every settings-page test"
      contains: "buildDom"
    - path: "tests/js/configPage.test.js"
      provides: "The TEST-04 suite that drives the shipping settings page through jsdom"
      contains: "configPage.html"
    - path: ".mise.toml"
      provides: "The node tool pin and the two `[tasks.test]` lines that install the dependencies and run the settings-page tests"
      contains: "node --test"
    - path: ".gitignore"
      provides: "The node_modules exclusion, so the installed dependency tree never enters the repository"
      contains: "node_modules/"
    - path: ".pre-commit-config.yaml"
      provides: "A `test` hook file pattern that also matches the new tests/js/ files"
      contains: "tests/js/"
    - path: "src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html"
      provides: "The UI-01 load-failure handler: an inline message and a disabled Save button"
      contains: "button-submit"
  key_links:
    - from: "tests/js/testHelpers.js"
      to: "src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html"
      via: "buildDom() reads the shipping page from disk and parses it with runScripts: 'dangerously', so the tests execute the artifact the plugin ships"
      pattern: "configPage.html"
    - from: ".mise.toml"
      to: "tests/js/configPage.test.js"
      via: "the `[tasks.test]` run array, which CI's existing `test` job invokes as `mise run test`"
      pattern: "node --test"
  prohibitions:
    - statement: "MUST NOT leave the settings page looking normal and empty after a failed load. A failure must be visible to the administrator, and Save must be unavailable, before any save can start."
      status: resolved
      verification: test
    - statement: "MUST NOT interpolate any configured settings value, or any rejection value that may carry one, into a message shown on the settings page. The Emby URL can embed a user name and password."
      status: resolved
      verification: test
    - statement: "MUST NOT write a settings-page test that already passes against the unfixed page. Every error-path test is observed red against the current handler before the handler is added."
      status: resolved
      verification: judgment
---

<objective>
Build the repository's first JavaScript test harness and use it to close UI-01: a failed settings load shows a message and turns Save off, so blank inputs cannot replace the saved settings.

Purpose: `configPage.html`'s `pageshow` chain has a `.finally` and no `.catch` (`:90-92`), so a failed load leaves the page looking normal with four empty inputs; the submit handler then copies those blanks over the stored configuration (`:109-117`). Nothing in this repository can execute that page today — there is no `package.json`, no Node pin, and no JavaScript test of any kind. The harness has to exist before the fix can be proved, which is exactly the work order the ROADMAP names for this phase.

Output: a pinned Node toolchain, a `tests/js/` suite that runs the shipping settings page through jsdom, and the UI-01 load-failure handler proved by it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-CONTEXT.md
@.planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-RESEARCH.md
@.planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-PATTERNS.md
@.claude/rules/plugin.md
</context>

<tdd_discipline>
`workflow.tdd_mode` is enabled. Tasks 2 and 3 carry `tdd="true"`.

**The RED commit uses a skipped test, exactly as Phase 01 did.** This repository's pre-commit `test` hook runs the whole suite, so a commit that leaves a test failing cannot be made. Phase 01 resolved this with `[Fact(Skip="...")]` in the RED commit and an unskip in the GREEN commit; the Node test runner's equivalent is the per-test options object `{ skip: '<reason>' }` as the second argument to `test(...)`.

Per behaviour the sequence is:

1. Write the test unskipped. Run `node --test`. **Observe it red against the unfixed page** and record the failure text in the SUMMARY. `02-RESEARCH.md` Pitfall 4 warns that this particular red surfaces as an unhandled promise rejection with a jsdom-internal stack trace (`callTheUserObjectsOperation`, `EventTarget-impl.js`), not as an assertion failure. That is the correct signal — the page has no `.catch`, so the rejection is genuinely unhandled. Do not "fix" the harness in response to it.
2. Add `{ skip: 'RED — unskipped in the GREEN commit' }` and commit as `test(02-01): ...`.
3. Apply the page change, remove the skip option, run `node --test` green, and commit as `feat(02-01): ...`.

Record in the SUMMARY, per behaviour, the observed red output and the commit pair.
</tdd_discipline>

<decisions_resolved>
`02-CONTEXT.md` left five choices to the planner. Three are resolved here; the other two are resolved in the plans that own them.

- **jsdom, not happy-dom (D-10 implementation).** No concrete reason to prefer the lighter one surfaced. happy-dom's inline-script execution model differs from a browser's parser-driven synchronous execution, and it does not offer the same `beforeParse` injection point combined with the dangerous-script mode. That exact combination is the mechanism `02-RESEARCH.md` built and ran end-to-end against a byte-identical copy of this repository's settings page; switching would mean re-verifying the event-dispatch and disabled-button semantics from nothing, for a dependency that is installed once and never shipped. Weight is not a factor for a suite that runs in seconds.
- **Test file names and the split.** `tests/js/configPage.test.js` holds every test, and `tests/js/testHelpers.js` holds the doubles. That mirrors the C# suite's two conventions exactly: one test file per tested artifact, and one shared file of doubles, the role `TestDoubles.cs` plays. Split the test file only if it grows past a comfortable read, and say so in a SUMMARY when you do.
- **The exact wording of each message.** Fixed by this plan and by plan 02-02, inside D-08's rule that no message repeats the URL or the API key. Both strings are given in the task actions.

Resolved elsewhere: whether a later successful load re-enables Save (Task 3 of this plan — it does), and whether this phase exposes a read-failure flag on `EmbyVerifiedPasswords` (plan 02-03 — it does not; Phase 3 adds it).
</decisions_resolved>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the jsdom package before the first npm install in this repository</name>
  <read_first>
    - .planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-RESEARCH.md — the `## Package Legitimacy Audit` section and its note on why the tool returned `SUS`.
  </read_first>
  <what-built>
    Nothing yet. This checkpoint runs **before** the install, because Task 2 adds the first npm dependency this repository has ever had.

    The audit in `02-RESEARCH.md` returned the verdict `SUS` for `jsdom` with the single reason `too-new`. That verdict reflects the `latest` dist tag, `30.1.0`, which was published one day before the research ran; the tool cannot scope its check to a specific pinned version. Every other signal was checked and is unambiguous:

    - the package exists on npm and is not deprecated;
    - `npm view jsdom scripts.postinstall` is empty at every version, so nothing executes on install;
    - the repository is `github.com/jsdom/jsdom`, the canonical project, with 283 published versions since 2011;
    - 91,064,755 downloads per week.

    The pin Task 2 will use is `jsdom@28.1.0`, published 2026-02-15 — about seven months old, and therefore not subject to the `too-new` signal at all. The registry facts above were re-verified live on 2026-09-18, the same day the research ran, so the pin is inside `02-RESEARCH.md`'s seven-day freshness window.
  </what-built>
  <how-to-verify>
    You are confirming two things a tool cannot decide: that this repository should gain a JavaScript dependency at all, and that this specific package is the real one.

    1. Open https://www.npmjs.com/package/jsdom and confirm the repository link points to `github.com/jsdom/jsdom`, and that the weekly download count is in the tens of millions.
    2. On the same page, open the **Versions** tab and confirm `28.1.0` is listed with a publish date of 2026-02-15 or thereabouts.
    3. Decide on the dependency itself. `02-CONTEXT.md` D-09 rates this **costly** to reverse: undoing it later means removing the `node` tool pin, the `tests/js/package.json` and its lockfile, and a `mise run test` line that CI's `test` job runs — three coordinated removals, not one.

    Nothing has been installed. If you refuse, this plan stops here and the settings-page tests need a different approach.
  </how-to-verify>
  <acceptance_criteria>
    - Decision: the reviewer states "approved" or names the objection. The checkpoint carries `gate="blocking-human"`, so it is never auto-approved, in any mode.
    - Documented: the SUMMARY records the decision and the date it was given.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with `jsdom@28.1.0`, or describe the objection.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: One end-to-end path — a failed settings load reaches a message on the page</name>
  <reversibility rating="costly">Undoing the JavaScript toolchain later means removing the `node` tool pin, `tests/js/` with its lockfile, and a `mise run test` line CI already runs, together (D-09).</reversibility>
  <files>.gitignore, tests/js/package.json, tests/js/package-lock.json, tests/js/testHelpers.js, tests/js/configPage.test.js, .mise.toml, src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html</files>
  <read_first>
    - src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html — read the whole file. The `pageshow` handler is lines 81-93, the element to disable is the `button-submit` element at line 40, the message element is `#EmbyAuthMigrationSummary` at line 48, and the two handlers that already have a `.catch` are `loadEmbyAuthMigration` (lines 62-79) and the `EmbyAuthRunMigration` click handler (lines 95-104). Copy their shape.
    - .mise.toml — the whole file. The `[tools]` block is lines 1-10, one tool per line with an exact version. `[tasks.test]` is lines 28-33. The comment at line 16 states the rule this task must not break: CI runs exactly these tasks, so a local run matches CI.
    - .gitignore — four lines today, one directory per line with a trailing slash.
    - .planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-RESEARCH.md — Patterns 1 to 3 hold the verified jsdom construction options, the event-dispatch calls, and the flush helper; Pitfalls 1 to 4 hold the four failure modes reproduced during research.
    - .planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-PATTERNS.md — the `configPage.html` section, which quotes every existing line that writes to `#EmbyAuthMigrationSummary`, and the `tests/js` section, which maps the C# stub conventions onto the JavaScript stubs.
    - tests/Jellyfin.Plugin.EmbyAuth.Tests/TestDoubles.cs — the stub conventions to mirror: one small hand-written stub per collaborator, implementing only the members the code under test actually calls, with a flag that flips a method into its failure behaviour, and a stub that records what happened and exposes it as a list.
    - .claude/rules/plugin.md — the `## API and settings page` and `## Settings` sections.
  </read_first>
  <behavior>
    One test in `tests/js/configPage.test.js`, and only one in this task:

    - **A failed settings load shows a message on the page.** Build a jsdom window over the real `configPage.html` with an `ApiClient` stub whose `getPluginConfiguration` returns a rejected promise. Dispatch `pageshow` on `#EmbyAuthConfigPage`. After the promise chain settles, `#EmbyAuthMigrationSummary` holds the load-failure message.

    Observed red against the unfixed page: an unhandled promise rejection attributed to this test, per `02-RESEARCH.md` Pitfall 4.
  </behavior>
  <action>
    This is the tracer slice: one path from the mise task, through the pinned Node runtime, through the installed jsdom dependency, through the shipping HTML file, to an assertion on a DOM node. Wire that whole path for **one** behaviour. Task 3 expands out from it. Do not write the save-failure handler, the migration-list tests, or the Run-migration-now tests here — plan 02-02 owns those.

    **`.gitignore`** — append `node_modules/` as a fifth line, matching the existing one-directory-per-line, trailing-slash form. Do this first, before any install, so the installed tree is never a candidate for staging.

    **`tests/js/package.json` and `tests/js/package-lock.json`** — create the directory, initialise the package, set the module type to `commonjs`, and install `jsdom` at exactly `28.1.0` with npm's save-exact flag so the manifest records a bare version with no range operator. Commit both the manifest and the lockfile. Set the package `name` to `jellyfin-plugin-emby-auth-page-tests`, set `private` to `true`, and give it no `scripts` block — the mise task is the only entry point, and a second one would be a place for the two to drift apart.

    **`.mise.toml`** — add `node = "24.21.0"` to `[tools]`, on its own line in the same exact-version form as the nine tools already there. `24.21.0` is the current 24.x release and Node's Active LTS line; it satisfies jsdom 28.1.0's declared engine floor. Then add two entries to the `[tasks.test]` run array, after the existing `bats tests/scripts` entry and in this order: `npm --prefix tests/js ci`, then `node --test`.

    Both entries matter and both have a reason that is easy to get wrong:

    - The install line is not optional. CI checks out a clean tree and `node_modules/` is ignored, so without it the test file cannot resolve its dependency. Use the lockfile-enforcing install verb, not the resolving one, so a CI run and a local run get the same tree. It runs on every invocation of the task; that is the cost of the rule stated in the comment at line 16 of this file, that a local run of a task matches CI exactly. Do not add a conditional guard around it.
    - The test line is exactly `node --test`, with **no path argument and no glob**. `02-RESEARCH.md` Pitfall 1 reproduced the failure: a directory argument makes Node treat the path as a module specifier and report one failing test named after the directory. With no argument, Node searches down from the working directory, which for a mise task is the repository root, finds `tests/js/configPage.test.js`, and skips the installed dependency tree on its own.

    Update the `[tasks.test]` description so it names the settings-page tests alongside the unit tests and the script tests.

    **`tests/js/testHelpers.js`** — the shared harness. This file plays the role `TestDoubles.cs` plays for the C# suite: the one file every test file imports for its doubles. Export:

    - `buildDom(options)` — reads the settings page from disk and returns `{ window, document, api, dashboard }`. Resolve the page path from `__dirname`, not from the working directory, so the helper does not depend on where Node was started. Construct the jsdom instance with `runScripts` set to `dangerously`, a `url` of `http://localhost/web/configurationpage` (any non-blank URL), and a `beforeParse(window)` callback that assigns the two stub globals. Do not pass the `resources` option: the page loads no subresource, and jsdom implements no global fetch — every call the page makes goes through the `ApiClient` stub.
    - `stubApiClient(options)` and `stubDashboard()` — the two stub factories, mirroring the C# convention of one hand-written stub per collaborator with only the members the code under test calls. `ApiClient` needs `getUrl`, `getJSON`, `ajax`, `getPluginConfiguration`, and `updatePluginConfiguration`; `Dashboard` needs `showLoadingMsg`, `hideLoadingMsg`, and `processPluginConfigurationUpdateResult`. Each stub records what happened and exposes it as a list or a count, mirroring the capturing-logger shape: `api.getConfigCalls`, `api.updateCalls` (the configuration objects passed), `api.migrationStatusCalls`, `api.runMigrationCalls`, `dashboard.showLoadingCalls`, `dashboard.hideLoadingCalls`, `dashboard.updateResults`.
    - The failure flags, mirroring the C# convention where a stub carries a property that flips a method into throwing: `getConfigFails`, `updateConfigFails`, `migrationStatusFails`, `runMigrationFails`. Each flag makes the matching method return a rejected promise. `getConfigFails` and `updateConfigFails` accept either `true` or an `Error` instance; given an `Error`, the stub rejects with that exact value, which is what Task 3 needs to prove the message does not echo a rejection value.
    - `config` and `users` options, with defaults. Default the configuration to values that differ from the page's first `<option>` in both selects and from an empty string in both inputs, so a "the load populated the inputs" assertion cannot pass by accident: an Emby server URL, an API key, the `KeepEmbyInCharge` migration mode, and the `NoLibraries` account access. Default `users` to an empty array.
    - `flush()` — awaits three macrotask boundaries, each a promise around `setTimeout` with a zero delay. `dispatchEvent` is synchronous but the page's handlers are promise chains two and three levels deep, so an assertion made immediately after dispatch reads pre-resolution state. Research measured two boundaries as sufficient for the two-level `pageshow` chain; three covers the deeper submit chain plan 02-02 adds. If an assertion ever reads pre-resolution state, raise the count and record the change in the SUMMARY.
    - `firePageshow(document, window)`, `fireSubmit(document, window)`, `clickSave(document)`, and `clickRunMigration(document, window)` — the four event helpers. `pageshow` is dispatched on `#EmbyAuthConfigPage`, not on the window, because that is the element the page listens on. `submit` is dispatched on `#EmbyAuthConfigForm` and must be constructed cancelable, so the handler's `preventDefault` call has an effect. `clickSave` calls the DOM `click` method on the `button-submit` element rather than dispatching an event — `02-RESEARCH.md` Pitfall 2 reproduced why this matters, and Task 3 depends on it. Never call the form's own `submit` method: per the HTML specification it fires no `submit` event, and jsdom additionally logs an unimplemented-navigation warning.

    **`tests/js/configPage.test.js`** — import `node:test` and `node:assert/strict` and the helpers, and write the single test named in `<behavior>`. Take the test description style from the C# suite: one behaviour per test, named as a sentence about the behaviour. Run it unskipped against the unfixed page first and record the red output, then commit it skipped, per `<tdd_discipline>`.

    **`src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html`** — insert a `.catch` between the existing `.then` and the existing `.finally` of the `pageshow` chain. Follow the shape the file already uses twice: read the message element into a local variable at the top of the handler, and inside the `.catch` assign a fixed string to that element's text-content property. Also set the `disabled` property of the `button-submit` element to `true` inside the same `.catch`, which is D-06's second half: the administrator cannot start a save that would copy four empty inputs over the stored settings. `document.querySelector('.button-submit')` is unambiguous — line 40 holds the only element with that class.

    The message text is: `Jellyfin cannot load the plugin settings. Save is turned off until the settings load. See the Jellyfin log.`

    It is a string literal. It must never be built from, or concatenated with, the rejection value, `config.EmbyServerUrl`, or `config.EmbyApiKey`. The configured Emby URL can embed a user name and password, per `.claude/rules/plugin.md` §Settings, and a rejected request's own error text is the most likely place for that URL to reappear.

    Do not change the `.finally` that hides the loading message, do not change the success branch in this task, and do not touch the submit handler at all.

    Before finishing, prove the test has teeth as `CLAUDE.md` requires: remove the new `.catch` block, confirm the test goes red, restore it exactly, and record both in the SUMMARY.
  </action>
  <verify>
    <automated>npm --prefix tests/js ci</automated>
    <fails_when>non-zero exit; npm reports that the lockfile is absent or out of sync with the manifest</fails_when>
    <automated>node --test</automated>
    <fails_when>non-zero exit, or the summary reports `tests 0`, or a reported test name is a directory path rather than the description written in `configPage.test.js`</fails_when>
    <automated>mise run test</automated>
    <fails_when>non-zero exit; any of the four run entries exits non-zero, or the node summary line reports `fail` greater than 0</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit; `dotnet format --verify-no-changes` prints a formatting diff, or actionlint or zizmor reports a finding</fails_when>
    <automated>mise run e2e</automated>
    <fails_when>non-zero exit; any bats file reports a failing test — required by `CLAUDE.md` because this task changes a file under `src/`</fails_when>
    <automated>git status --porcelain tests/js/node_modules</automated>
    <fails_when>the output is non-empty, which means the installed dependency tree is visible to git and the `.gitignore` entry is wrong or missing</fails_when>
  </verify>
  <acceptance_criteria>
    - Config: `rg -q '^node = "24\.21\.0"$' .mise.toml` succeeds — the tool pin is exact, matching the nine tools already pinned.
    - Config: `rg -q '"node --test",' .mise.toml` succeeds and `rg -q '"npm --prefix tests/js ci",' .mise.toml` succeeds — both run entries are present.
    - Config: `rg -q 'node --test [^"]' .mise.toml` fails — the test entry carries no path argument and no glob (Pitfall 1).
    - Config: `rg -q '^node_modules/$' .gitignore` succeeds.
    - Config: `git ls-files tests/js/node_modules` prints nothing — no part of the installed tree is tracked.
    - Source: `rg -q '"jsdom": "28\.1\.0"' tests/js/package.json` succeeds — the version is exact, with no range operator.
    - Source: `test -f tests/js/package-lock.json` succeeds — the lockfile is committed, so the install verb in the mise task can enforce it.
    - Source: `rg -q 'runScripts' tests/js/testHelpers.js` succeeds and `rg -q 'beforeParse' tests/js/testHelpers.js` succeeds — the page is executed, and the stubs are injected before parsing.
    - Source: `rg -q '__dirname' tests/js/testHelpers.js` succeeds — the page path is resolved relative to the helper, not to the working directory.
    - Source: `rg -q 'catch' src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` returns at least 3 matches — the two handlers that already had one, plus the new one.
    - Source: `! rg -q 'innerHTML' src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` succeeds — every DOM write on the page still assigns text, not markup.
    - Behavior: with a rejecting `getPluginConfiguration` stub, after `pageshow` and a flush, `#EmbyAuthMigrationSummary` text matches `/cannot load the plugin settings/` and the `button-submit` element's `disabled` property is `true`.
    - Behavior: with a resolving stub, after `pageshow` and a flush, the four inputs hold the values the stub returned and the `button-submit` element's `disabled` property is `false`.
    - Test: `node --test` exits 0 and its summary reports `fail 0` with `pass` greater than 0.
    - Test: `mise run test` exits 0.
    - Documented: the SUMMARY records the red output observed against the unfixed page, and records that removing the new `.catch` block turned the test red again before it was restored.
  </acceptance_criteria>
  <done>`mise run test` installs the pinned dependency and runs a jsdom test that loads the shipping settings page, drives `pageshow` with a failing configuration load, and reads the failure message off the page. The administrator now sees a message and a Save button that is turned off, instead of a normal-looking page with four empty fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand the load-failure slice to the rest of UI-01</name>
  <files>tests/js/configPage.test.js, tests/js/testHelpers.js, src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html, .pre-commit-config.yaml</files>
  <read_first>
    - tests/js/configPage.test.js — the file being extended, as Task 2 created it. Reuse its imports and its single existing test's shape; do not introduce a second helper module.
    - tests/js/testHelpers.js — the helper being extended. `clickSave` and the `getConfigFails` option already exist; check whether the `Error`-valued form of that flag was implemented in Task 2 before adding it.
    - src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html — the file being modified, with Task 2's `.catch` in place. The success branch of the `pageshow` chain is where the re-enable belongs.
    - .pre-commit-config.yaml — the whole file. The `test` hook's `files` pattern is line 15 and today matches `.cs`, `.csproj`, `.slnx`, `.props`, `.html`, `global.json`, `.mise.toml`, `scripts/`, and `tests/scripts/`.
    - .planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-RESEARCH.md — Pitfall 2 and Pattern 3, which are why the disabled-Save test calls the button's `click` method.
    - .claude/rules/plugin.md — the `## Settings` section: a settings message never repeats the configured values.
  </read_first>
  <behavior>
    Four tests added to `tests/js/configPage.test.js`:

    - **Save is turned off after a failed settings load.** After a failed load, the `button-submit` element's `disabled` property is `true`, and activating it with the DOM `click` method leaves the recorded `updatePluginConfiguration` call list empty.
    - **Activating Save twice after a failed load still sends nothing.** Two `click` calls; the recorded update-call list is still empty. (Probe edge UI-01/idempotency.)
    - **A later successful load turns Save back on.** A failing `pageshow`, then the same window's stub switched to succeed, then a second `pageshow`: the four inputs hold the returned values and the `button-submit` element's `disabled` property is `false`. (Probe edge UI-01/concurrency, sequential half.)
    - **The load-failure message repeats neither the configured URL nor the API key.** The `getConfigFails` flag carries an `Error` whose own message embeds a URL of the `user:password@host` form and a distinctive API-key string. After the failed load, the message element's text contains neither the password substring nor the API-key substring, and the message element has zero child elements. (Probe edges UI-01/encoding and UI-01/empty; threat T-02-01.)

    For each of the four, observe it red before the behaviour it covers exists, per `<tdd_discipline>`.
  </behavior>
  <action>
    **`tests/js/testHelpers.js`** — if Task 2 implemented `getConfigFails` as a plain boolean only, extend it so a value that is an `Error` instance is used as the rejection value verbatim, and a value of `true` rejects with a generic `Error`. Add a way for a test to switch a live stub from failing to succeeding without building a second window — a plain mutable property on the returned `api` object is enough; do not add a second factory. Keep the helper file to the doubles and the event helpers; assertions live in the test file.

    **`src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html`** — in the success branch of the `pageshow` chain, after the four input assignments and before the call that loads the migration status, set the `disabled` property of the `button-submit` element to `false`.

    This resolves the open question `02-CONTEXT.md` left to the planner: **a later successful load re-enables Save; a page reload is not required.** The reason is that by the time the success branch runs, the four inputs hold real values read back from the server, so a save can no longer write blanks over the stored settings — which is the entire hazard D-06 exists to prevent. Leaving the button off after a recovery would strand an administrator on a page that works.

    Record the residual this does not cover, which is already carried in this plan's `must_haves` as a backstop item: the page holds no guard against two overlapping configuration loads, and this phase adds none. The Jellyfin dashboard fires `pageshow` once per navigation, so the overlapping case needs a second navigation while the first load is still in flight.

    **`tests/js/configPage.test.js`** — add the four tests named in `<behavior>`.

    The first two must activate the button with the DOM `click` method. `02-RESEARCH.md` Pitfall 2 reproduced why: a turned-off submit button suppresses only the browser's own activation path — a real click, and the implicit submission from pressing Enter in a text field. An event object dispatched straight at the form bypasses that check entirely, so a test written that way passes even with the whole disabled-button line deleted. That test would silently cover nothing. jsdom implements the specification behaviour correctly, which is what makes the `click`-based test meaningful.

    For the fourth test, assert on substrings of the sentinel values, not on the whole message, and assert the child-element count is zero rather than inspecting markup — the page writes text, and an assertion phrased as a markup comparison would drift the moment the message wording changes.

    **`.pre-commit-config.yaml`** — append `|^tests/js/` to the `test` hook's `files` pattern.

    This is a deliberate addition that `02-PATTERNS.md` did not call for, and the reason is worth recording: the existing pattern matches `.html`, so editing the settings page already runs the suite, but it matches nothing under `tests/js/`, so editing a test or the lockfile would not run the tests those files are. The hook's own intent is that a change runs the suite it belongs to. One alternative in the pattern closes the gap.

    Then prove the handlers have teeth, as `CLAUDE.md` requires and as ROADMAP criterion 4 states: remove the line in the `.catch` that turns Save off, confirm the disabled-Save tests go red, and restore it; then remove the whole `.catch` block, confirm the message test goes red, and restore it. Record both in the SUMMARY.
  </action>
  <verify>
    <automated>node --test</automated>
    <fails_when>non-zero exit, or the summary reports fewer than 5 tests, or reports `fail` greater than 0</fails_when>
    <automated>mise run test</automated>
    <fails_when>non-zero exit; any of the four run entries exits non-zero</fails_when>
    <automated>mise run lint</automated>
    <fails_when>non-zero exit; a linter reports a finding in a changed file</fails_when>
    <automated>mise run e2e</automated>
    <fails_when>non-zero exit; any bats file reports a failing test — required because this task changes a file under `src/`</fails_when>
    <automated>rg -q 'tests/js/' .pre-commit-config.yaml</automated>
    <fails_when>non-zero exit — the `test` hook pattern was not extended, so a change to a JavaScript test file would not run the suite</fails_when>
    <automated>prek run --all-files</automated>
    <fails_when>non-zero exit; a hook reports a failure or modifies a file</fails_when>
  </verify>
  <acceptance_criteria>
    - Source: `rg -c '^test\(' tests/js/configPage.test.js` returns at least 5 — Task 2's test plus the four added here.
    - Source: `rg -q '\.click\(\)' tests/js/testHelpers.js` succeeds — Save is activated through the element's own activation behaviour, not a dispatched event (Pitfall 2).
    - Behavior: with the line that turns Save off removed from the `.catch`, both disabled-Save tests go red. This is the guard against Pitfall 2, and it is a proof rather than a text search because plan 02-02 legitimately dispatches a submit event elsewhere in this same file.
    - Source: `rg -q 'disabled = false' src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` succeeds — a successful load turns Save back on.
    - Source: `! rg -q 'innerHTML' src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` succeeds.
    - Behavior: after a failed load, two consecutive activations of the Save button leave the recorded `updatePluginConfiguration` call list empty.
    - Behavior: a failing load followed by a succeeding load leaves the four inputs holding the returned values and the `button-submit` element's `disabled` property `false`.
    - Behavior: when the load rejects with an `Error` whose own message embeds a `user:password@host` URL and an API key, the text of `#EmbyAuthMigrationSummary` contains neither the password substring nor the API-key substring, and that element has zero child elements.
    - Test: `node --test` exits 0, reports `fail 0`, and reports at least 5 tests.
    - Test: `mise run test` exits 0 and `mise run e2e` exits 0.
    - Documented: the SUMMARY names, for each of the two handler lines removed, which tests went red and confirms each line was restored.
  </acceptance_criteria>
  <done>UI-01 is complete and proved: a failed settings load shows a message, turns Save off, survives repeated Save activations, recovers on a later successful load, and leaks no configured value into the page. Editing a file under `tests/js/` now runs the suite locally.</done>
</task>

</tasks>

<!-- planner-discipline-allow: innerHTML -->

<artifacts_this_phase_produces>
## Artifacts this phase produces

Nothing below exists on `main` today. A drift check must not flag these as unacknowledged new symbols.

**Created by this plan (02-01):**
- `tests/js/package.json` — new file. Package name `jellyfin-plugin-emby-auth-page-tests`, `private: true`, `type: "commonjs"`, one devDependency `jsdom` at `28.1.0`.
- `tests/js/package-lock.json` — new file.
- `tests/js/testHelpers.js` — new file. Exports `buildDom`, `stubApiClient`, `stubDashboard`, `flush`, `firePageshow`, `fireSubmit`, `clickSave`, `clickRunMigration`. Stub recording surfaces: `api.getConfigCalls`, `api.updateCalls`, `api.migrationStatusCalls`, `api.runMigrationCalls`, `dashboard.showLoadingCalls`, `dashboard.hideLoadingCalls`, `dashboard.updateResults`. Stub failure flags: `getConfigFails`, `updateConfigFails`, `migrationStatusFails`, `runMigrationFails`.
- `tests/js/configPage.test.js` — new file. Five `node:test` cases after Task 3.
- `.mise.toml` — a new `[tools]` entry `node = "24.21.0"`, and two new `[tasks.test]` run entries, `npm --prefix tests/js ci` and `node --test`.
- `.gitignore` — a new `node_modules/` line.
- `.pre-commit-config.yaml` — the `test` hook's `files` pattern gains the `^tests/js/` alternative.
- `src/Jellyfin.Plugin.EmbyAuth/Configuration/configPage.html` — a new `.catch` on the `pageshow` chain and a new `disabled` assignment in its success branch. No new element and no new `id`: the message reuses the existing `#EmbyAuthMigrationSummary` (D-08), and the Save button is selected by its existing `button-submit` class.

**Created by later plans in this phase:**
- Further `node:test` cases in `tests/js/configPage.test.js`, and a `.catch` plus a `.finally` on the submit handler of `configPage.html` (plan 02-02).
- Two new `[Fact]` methods on `EmbyVerifiedPasswordsTests` (plan 02-03).

**Changed by later plans in this phase:**
- `EmbyVerifiedPasswords.Load()` returns a nullable dictionary, and the `LogReadFailed` message text changes (plan 02-03).

**Not created by this phase, and deliberately so:**
- No read-failure flag is exposed on `EmbyVerifiedPasswords`. See plan 02-03 for the decision and its reason.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions

1. **`node --test` with no path argument discovers only the files under `tests/js/`.** Research verified the no-argument form in a scratchpad layout, not in this repository's root. This repository holds no JavaScript file outside `tests/js/`, so discovery should find exactly that suite, but a local tree can also hold `bin/`, `obj/`, and `artifacts/`. Confirm the reported test names all come from `configPage.test.js`. If Node picks up anything else, replace the run entry with Node's quoted glob form naming the `tests/js` tree and record the change in the SUMMARY — do **not** pass a bare directory path, which Pitfall 1 reproduced as a hard failure.

2. **The macrotask flush count.** Research measured two boundaries as sufficient for the two-level `pageshow` chain. This plan specifies three so that plan 02-02's deeper submit chain is covered by the same helper. If a chain ever grows deeper still, an assertion reads pre-resolution state and the symptom is a test that passes under a debugger and fails in CI. Raise the count and record it.

3. **`npm --prefix tests/js ci` runs on every invocation of `mise run test`, including every pre-commit run.** This is the deliberate cost of the rule stated in `.mise.toml`'s own comment, that a local task run matches CI exactly. If the added time proves unacceptable in practice, that is a change to make on measurement, in a later phase, not a guard to add here.

4. **The `jsdom@28.1.0` pin's freshness.** `02-RESEARCH.md` gives the version pins a seven-day window because of jsdom's release cadence. The registry facts were re-verified live on 2026-09-18, the same day the research ran: the `latest` dist tag is `30.1.0`, `28.1.0` declares an engine floor of `^20.19.0 || ^22.12.0 || >=24.0.0`, and no version declares a postinstall script. The window has not passed.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jellyfin plugin API → the settings page script | A rejected request's error value crosses into the browser. It can carry the request URL, which is the configured Emby address, which can embed a user name and password. |
| Emby-controlled data → the settings page DOM | Emby user names reach `#EmbyAuthMigrationUsers` through the migration endpoint. |
| The npm registry → this repository's build and test runs | The first external JavaScript dependency this repository has ever had. |
| The administrator's browser → the stored plugin configuration | A Save writes the four input values over the stored settings. After a failed load those inputs are empty. |

## STRIDE Threat Register

ASVS enforcement level 1; blocking severity `high`. Derived from `02-RESEARCH.md` `## Security Domain`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | the load-failure message written to `#EmbyAuthMigrationSummary` | high | mitigate | The message is a string literal, never built from the rejection value or from `config.EmbyServerUrl` / `config.EmbyApiKey`. Task 3's fourth test rejects with an `Error` whose own text embeds a `user:password@host` URL and an API key, then asserts the page message contains neither substring — so an executor who later interpolates the rejection value turns that test red. |
| T-02-02 | Tampering (reflected markup) | the message element and the migration list | medium | mitigate | Every DOM write on the page assigns text, never markup — the established rule in `.claude/rules/plugin.md` §API and settings page. Task 3 asserts the message element has zero child elements; an acceptance criterion asserts the markup-assigning property appears nowhere in the file. Plan 02-02 extends the same assertion to a list entry rendered from an Emby user name. |
| T-02-03 | Tampering / Denial of Service | a Save after a failed load overwriting the stored Emby URL and API key with empty strings | high | mitigate | D-06: the `.catch` turns the Save button off, and Task 3 proves with the button's own activation behaviour that no update request is sent — including on a repeated activation. The button turns back on only after a load succeeds, at which point the inputs hold real values. |
| T-02-SC | Tampering | the first npm install in this repository (`jsdom`) | high | mitigate | `02-RESEARCH.md` `## Package Legitimacy Audit` ran the gate; the verdict `SUS` reflects the `latest` dist tag, not the `28.1.0` pin. Task 1 is a `gate="blocking-human"` checkpoint before the install and is never auto-approved. The pin is exact, the lockfile is committed, and the mise task uses the lockfile-enforcing install verb so a resolved tree cannot drift between a local run and CI. `npm view jsdom scripts.postinstall` is empty at every version, so nothing executes on install. |
| T-02-04 | Information Disclosure | the installed dependency tree entering the repository | low | mitigate | `.gitignore` gains `node_modules/` before the first install, and a verify command asserts git reports nothing under `tests/js/node_modules`. |

**Canon referral:** cross-site scripting through markup injection is canon OWASP surface — covered by `/gsd-secure-phase`; the bespoke project rule (a settings message never repeats a configured value) is minted as a prohibition instead.
</threat_model>

<verification>
- `node --test` exits 0 and reports `fail 0`.
- `mise run test` exits 0 — the full chain, including the two new run entries.
- `mise run lint` exits 0.
- `mise run e2e` exits 0 — required by `CLAUDE.md` because this plan changes `configPage.html`, which is under `src/`.
- `prek run` exits 0 before each commit.
- `git ls-files tests/js/node_modules` prints nothing.
</verification>

<success_criteria>
- Automated tests execute the shipping settings page JavaScript, and `mise run test` runs them, so CI's existing `test` job covers them with no change to `.github/workflows/ci.yml` and no change to `ci-success` (ROADMAP criterion 4, first half).
- A failed settings load shows a message and turns Save off, and a Save after that failed load writes nothing (ROADMAP criterion 2, UI-01).
- Removing the load-failure handler turns a test red, and that was observed rather than assumed.
- No page message can carry the configured Emby URL or API key, and a test enforces it.
</success_criteria>

<output>
Create `.planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-01-SUMMARY.md` when done.
</output>
