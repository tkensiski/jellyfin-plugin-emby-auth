---
phase: quick-260920-3lw
plan: 01
subsystem: testing
tags: [npm, jsdom, dependencies]
status: complete
dependency-graph:
  requires: []
  provides: [jsdom-classified-as-devdependency]
  affects: [tests/js/package.json, tests/js/package-lock.json]
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - tests/js/package.json
    - tests/js/package-lock.json
    - .planning/STATE.md
decisions:
  - "No test was written for this change, per the plan's tdd_discipline block — a dependency classification change produces no behavior a test can assert; the RED-then-GREEN gates in each task's <verify> block stood in for a failing test."
metrics:
  duration: ~10min
  completed: 2026-09-20
actuals:
  tokens: 4663
  tasks: 2
  commits: 1
---

# Phase quick-260920-3lw Plan 01: Move jsdom to devDependencies Summary

Reclassified `jsdom` from `dependencies` to `devDependencies` in `tests/js/package.json`, regenerated `tests/js/package-lock.json` with `npm install --prefix tests/js` so the two agree, and removed the now-resolved blocker entry from `.planning/STATE.md`.

## What Was Built

- `tests/js/package.json`: renamed the `dependencies` key to `devDependencies`. The pinned version, `jsdom@28.1.0`, is byte-identical to before.
- `tests/js/package-lock.json`: regenerated by `npm install --prefix tests/js`, not hand-edited. The root package entry now carries `devDependencies` instead of `dependencies`, and all 46 dependent package entries (including `node_modules/jsdom`) gained `"dev": true`. No `version`, `resolved`, or `integrity` line changed.
- `.planning/STATE.md`: removed the single `### Blockers/Concerns` entry that reported the jsdom classification mismatch (`[Phase 2, minor]: tests/js/package.json:4 declares jsdom under dependencies...`). The other two entries in that section (the research-flags entry and the Phase 3 FPRT-01 entry) are untouched. This edit was left uncommitted in the working tree, for the orchestrator's closing docs commit, per the plan.

## Verification Results

### Task 1 gates

- Structural gate on `package.json`: PASS — no `dependencies` key, `devDependencies.jsdom === "28.1.0"`.
- Structural gate on `package-lock.json`: PASS — root `devDependencies.jsdom === "28.1.0"`, `node_modules/jsdom` carries `"dev": true`.
- Drift gate (`git diff -U0` over the lock, counting added/removed `version`/`resolved`/`integrity` lines): `drift_lines=0`, run before any commit.
- `mise run test`: PASSED — 200 .NET unit tests, 8 `package.sh` bats tests, `npm --prefix tests/js ci` (accepted the regenerated lock), 58 settings-page node tests, all green.

### Task 2 gates

- `### Blockers/Concerns` entry count: PASS — exactly 2 entries remain.
- Surviving-entry identity check: PASS — both the research-flags entry and the FPRT-01 entry are present, opening text unchanged.
- `prek run` on the staged commit: PASSED — the test hook ran and passed; the lint hook reported "no files to check" since neither staged file matches its path filters.

### Commit

`b88045b` — `chore(tests): move jsdom to devDependencies`, two files changed (48 insertions, 2 deletions): `tests/js/package.json` and `tests/js/package-lock.json`. `.planning/STATE.md` was left modified and unstaged, per the plan, for the orchestrator's closing docs commit.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None. No new package or supply-chain surface was introduced; `jsdom@28.1.0` already passed its legitimacy checkpoint in Phase 02. The T-3LW-01 drift gate and T-3LW-03 blocker-entry-count gate both ran and passed as specified in the plan's threat register.

## Self-Check: PASSED

- FOUND: tests/js/package.json
- FOUND: tests/js/package-lock.json
- FOUND: commit b88045b (`git log --oneline` confirms it in history)
