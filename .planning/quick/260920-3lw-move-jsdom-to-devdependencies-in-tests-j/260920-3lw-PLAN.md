---
phase: quick-260920-3lw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/js/package.json
  - tests/js/package-lock.json
  - .planning/STATE.md
autonomous: true
requirements: [TEST-04]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "tests/js/package.json declares jsdom under devDependencies, pinned at the unchanged exact version 28.1.0, and carries no dependencies key at all."
    - "tests/js/package-lock.json was regenerated by npm, not hand-edited: its root package entry carries devDependencies instead of dependencies, and the node_modules/jsdom entry carries \"dev\": true."
    - "The regenerated lock changes classification only. No package version, resolved URL, or integrity hash in the lock differs from the committed one."
    - "npm --prefix tests/js ci still succeeds, proving package.json and package-lock.json agree; node --test still runs the settings-page suite green."
    - "mise run test passes end to end, and prek run passes."
    - ".planning/STATE.md no longer carries the jsdom classification entry under ### Blockers/Concerns, and the two unrelated entries in that section survive byte-identical."
  artifacts:
    - tests/js/package.json
    - tests/js/package-lock.json
    - .planning/STATE.md
  key_links:
    - "package.json devDependencies <-> package-lock.json packages[\"\"].devDependencies — npm ci fails hard when the two disagree, so the lock must be regenerated in the same change, never in a follow-up."
    - ".mise.toml [tasks.test] runs `npm --prefix tests/js ci` before `node --test`, so that install step is the enforcement point for the lock, in local runs and in CI alike."
    - "npm ci installs devDependencies by default; it would skip them only under --omit=dev or NODE_ENV=production, and neither exists anywhere in this repo."
---

<objective>
Move `jsdom` from `dependencies` to `devDependencies` in `tests/js/package.json`, regenerate `tests/js/package-lock.json` with npm so the two agree, and remove the now-resolved blocker entry from `.planning/STATE.md`.

Purpose: `.planning/phases/02-safe-failures-for-the-fingerprint-file-and-settings/02-01-SUMMARY.md:20` records the dependency as a devDependency, but `tests/js/package.json` declares it as a runtime dependency. The record and the file disagree. There is no security effect — the package is `private: true`, test-only, and never ships — so this is a correctness-of-record fix.

Output: a reclassified `package.json`, an npm-regenerated lock, and a `### Blockers/Concerns` section with the resolved entry gone.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@tests/js/package.json
@.mise.toml
</context>

<tdd_discipline>
No test is written for this change, and the executor MUST NOT invent one.

The repo rule is "write the test first", but a dependency classification change produces no behavior a test can assert. The proof is the existing suite continuing to pass across the reclassification — specifically that `npm --prefix tests/js ci` does not fail on an out-of-sync lock, and that `node --test` still runs the settings-page suite green. `mise run test` runs both, in that order, and is the verification of record here.

The structural gates in each task's `<verify>` block are the closest analogue to a failing-test-first step, and they were confirmed RED at planning time against the current tree:
- the `package.json` gate prints `FAIL: dependencies key still present` and exits 1
- the lock gate prints `FAIL: lock root still has dependencies` and exits 1
- the blocker-section entry count prints `3`, one more than the required `2`

Each turns green only after the corresponding edit lands. Do not re-derive them; do run them.
</tdd_discipline>

<tasks>

<task type="auto">
  <name>Task 1: Reclassify jsdom as a devDependency and regenerate the lock</name>
  <files>tests/js/package.json, tests/js/package-lock.json</files>
  <precondition>npm is on PATH (measured: 11.17.0) and `npm --prefix tests/js` can resolve jsdom@28.1.0 from the registry or the local npm cache. `mise run test` already runs `npm --prefix tests/js ci` on every invocation, so this holds in the normal loop; if the install fails to reach the registry, stop and report rather than editing the lock by hand.</precondition>
  <action>
Use the Edit tool on `tests/js/package.json` to rename the `dependencies` key to `devDependencies`. Keep the member `"jsdom": "28.1.0"` byte-identical — the pin does NOT change. `CLAUDE.md` requires exact pins, and this is a classification fix, not a version bump. The file must end with exactly one object holding `name`, `private`, `type`, and `devDependencies`, and no `dependencies` key at all.

Then regenerate the lock by running `npm install --prefix tests/js` from the repository root. This is a package-manager operation and is the REQUIRED route for `tests/js/package-lock.json`.

Do NOT hand-edit `package-lock.json` with Edit, Write, or any shell rewrite. A hand-edited lock silently drifts from what npm would produce: npm marks not only the `node_modules/jsdom` entry but every entry reachable only through jsdom, and reproducing that set by hand is guesswork that `npm ci` will not catch.

Expect a large lock diff. The committed lock holds zero `"dev": true` markers across 592 lines, so nearly every package entry gains one. Large is expected; changed resolutions are not — the drift gate below is what separates the two.

If npm also rewrites `tests/js/package.json` while normalizing, that is acceptable, but inspect `git diff tests/js/package.json` and confirm the only semantic change is the key rename.

Do NOT commit at the end of this task. Task 2 edits a second file that belongs in the same logical commit; the single commit happens after Task 2.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const p=JSON.parse(fs.readFileSync("tests/js/package.json","utf8"));if(p.dependencies){console.log("FAIL: dependencies key still present");process.exit(1);}if(p.devDependencies?.jsdom!=="28.1.0"){console.log("FAIL: jsdom not pinned at 28.1.0 under devDependencies");process.exit(1);}console.log("PASS");'</automated>
    <automated>node -e 'const fs=require("fs");const l=JSON.parse(fs.readFileSync("tests/js/package-lock.json","utf8"));const r=l.packages[""];if(r.dependencies){console.log("FAIL: lock root still has dependencies");process.exit(1);}if(r.devDependencies?.jsdom!=="28.1.0"){console.log("FAIL: lock root devDependencies missing jsdom 28.1.0");process.exit(1);}if(l.packages["node_modules/jsdom"].dev!==true){console.log("FAIL: node_modules/jsdom entry not marked dev");process.exit(1);}console.log("PASS");'</automated>
    <automated>N=$(git diff -U0 -- tests/js/package-lock.json | grep -cE '^[+-][[:space:]]*"(version|resolved|integrity)":' || true); echo "drift_lines=$N"; test "$N" = 0</automated>
    <automated>mise run test</automated>
  </verify>
  <done>
`tests/js/package.json` declares jsdom under `devDependencies` at 28.1.0 and has no `dependencies` key. `tests/js/package-lock.json` was rewritten by npm, its root entry carries `devDependencies`, and `node_modules/jsdom` carries `"dev": true`. The drift gate prints `drift_lines=0`. `mise run test` passes, meaning `npm --prefix tests/js ci` accepted the lock and `node --test` ran the settings-page suite green. Nothing is committed yet.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove the resolved blocker entry from STATE.md</name>
  <files>.planning/STATE.md</files>
  <action>
Use the Edit tool on `.planning/STATE.md` to delete exactly one line from the `### Blockers/Concerns` section: the single entry that reports the jsdom dependency classification as wrong. It is the third of the three entries in that section, and it is the only one that names `tests/js/package.json`. Task 1 resolved it, so the record must stop reporting it.

Leave the other two entries in that section untouched and byte-identical:
- the entry that opens `- Research flags for phase planning`
- the entry that opens `- [Phase 3]:` and describes FPRT-01

Confirm the line numbers before editing; the file may have shifted since planning, where the three entries sat at lines 123, 124, and 125.

Touch nothing else in `.planning/STATE.md`. In particular, do NOT add a row to `### Quick Tasks Completed` and do NOT change the `Last activity` line or the frontmatter — the orchestrator's own step owns both, and writing them here would collide with it.

Then commit, staging ONLY the two files Task 1 changed:

    git add tests/js/package.json tests/js/package-lock.json

Do NOT stage `.planning/STATE.md` in this commit. This repo splits a quick task into two commits — the code commit holds source and test files only, and a trailing `docs(quick-...)` commit sweeps up `.planning/STATE.md` together with this PLAN and the SUMMARY (see `325855e` and `fc0d37f` for the last quick task). The orchestrator's closing step owns that second commit, and it also appends the `### Quick Tasks Completed` row and the `Last activity` line. Leaving the blocker removal in the working tree keeps both STATE.md changes in one commit instead of two.

Let `prek run` execute as the pre-commit hook. It invokes `mise run lint` and `mise run test`; do not bypass it. Commit subject, imperative and 43 characters:

    chore(tests): move jsdom to devDependencies

No documentation change is needed. `docs/development.md:21` describes the suite as installing "the test dependency from the committed lockfile" and `CLAUDE.md` describes `tests/js/` as run "with jsdom against the shipping file"; neither states a classification, so both stay accurate.
  </action>
  <verify>
    <automated>test "$(sed -n '/^### Blockers\/Concerns$/,/^### Quick Tasks Completed$/p' .planning/STATE.md | grep -c '^- ')" = 2</automated>
    <automated>grep -q '^- Research flags for phase planning' .planning/STATE.md && grep -q '^- \[Phase 3\]: \*\*FPRT-01' .planning/STATE.md</automated>
    <automated>prek run</automated>
  </verify>
  <done>
The `### Blockers/Concerns` section holds exactly two entries, the research-flags entry and the FPRT-01 entry, both unchanged. The jsdom entry is gone. `### Quick Tasks Completed`, the `Last activity` line, and the frontmatter are untouched. `prek run` passes, and one commit holds exactly `tests/js/package.json` and `tests/js/package-lock.json`. The `.planning/STATE.md` edit stays uncommitted in the working tree for the orchestrator's closing docs commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> developer machine and CI | `npm install --prefix tests/js` resolves packages and writes `package-lock.json`; whatever it resolves is then installed by `npm ci` on every `mise run test`, locally and in CI |
| planning record -> future sessions | `.planning/STATE.md` blockers steer later planning; a wrongly removed or wrongly retained entry misdirects work that has not started yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3LW-01 | Tampering | `npm install --prefix tests/js` rewriting `tests/js/package-lock.json` | high | mitigate | Task 1 drift gate: `git diff -U0` over the lock must yield zero added or removed `version`, `resolved`, or `integrity` lines. A nonzero count means a resolution changed rather than only a classification — restore the lock with `git checkout -- tests/js/package-lock.json` and stop before committing. |
| T-3LW-02 | Denial of Service | settings-page suite after jsdom becomes a devDependency | low | accept | An installer run with `--omit=dev` or `NODE_ENV=production` would skip jsdom and break `node --test`. Measured at planning time: no `--omit` flag and no `NODE_ENV` assignment exists anywhere in the tree outside `node_modules`, and there is no `.npmrc` at the repo root or in `tests/js/`. `mise run test` in Task 1 and `prek run` in Task 2 are the standing proof. |
| T-3LW-03 | Repudiation | `.planning/STATE.md` blocker removal | low | mitigate | Task 2 deletes exactly one named line and asserts the surviving two by their opening text, so the edit cannot quietly drop an unrelated blocker. The removal ships in the same commit as the fix that resolves it, so the record and the change share one history entry. |
| T-3LW-SC | Tampering | npm package legitimacy | low | accept | No new package is introduced. jsdom stays at 28.1.0, and that exact pin already passed a package-legitimacy checkpoint in Phase 02 (approved 2026-09-19; recorded in `.planning/STATE.md` under Decisions). The legitimacy gate does not re-trigger for a classification change, and T-3LW-01 covers the only new supply-chain surface this task opens. |
</threat_model>

<verification>
Run in order, from the repository root:

1. The two structural gates in Task 1 print `PASS`.
2. The drift gate prints `drift_lines=0` — run it BEFORE any commit, since it reads the working tree.
3. `mise run test` passes: `dotnet test`, `bats tests/scripts`, `npm --prefix tests/js ci`, `node --test`.
4. The three STATE.md gates in Task 2 pass.
5. `prek run` passes.
6. `git show --stat HEAD` lists exactly `tests/js/package.json` and `tests/js/package-lock.json`.
7. `git status --short` still shows `.planning/STATE.md` as modified, waiting for the orchestrator's closing docs commit.
</verification>

<success_criteria>
- `tests/js/package.json` holds jsdom under `devDependencies` at exactly `28.1.0` and no `dependencies` key.
- `tests/js/package-lock.json` was produced by npm, carries `devDependencies` at its root entry and `"dev": true` on `node_modules/jsdom`, and has zero changed `version`, `resolved`, or `integrity` lines.
- `mise run test` and `prek run` both pass.
- `.planning/STATE.md` `### Blockers/Concerns` holds exactly the two unrelated entries.
- One code commit, subject `chore(tests): move jsdom to devDependencies`, holding only the two `tests/js/` files. The STATE.md edit is left for the orchestrator's closing docs commit.
</success_criteria>

<output>
Create `.planning/quick/260920-3lw-move-jsdom-to-devdependencies-in-tests-j/260920-3lw-SUMMARY.md` when done.
</output>
